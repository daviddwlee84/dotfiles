---
# zsh role defaults
#
# oh-my-zsh and its custom plugins (zsh-autosuggestions, zsh-syntax-highlighting,
# zsh-completions, zsh-vi-mode) are managed declaratively via chezmoi externals
# at the repo root: `.chezmoiexternal.toml.tmpl`. That file is the source of
# truth for upstream URLs and the weekly refresh cadence. Add new plugins there
# rather than here.
