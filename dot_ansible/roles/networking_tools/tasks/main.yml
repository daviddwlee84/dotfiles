---
# Networking CLI tools role
# Tools: nmap, arp-scan, mtr, iperf3, doggo, httpie, gping, trippy, bandwhich, speedtest, rustscan

# =============================================================================
# macOS - all via Homebrew
# =============================================================================

- name: Install networking tools (macOS)
  when: ansible_facts["os_family"] == "Darwin"
  community.general.homebrew:
    name:
      - nmap
      - arp-scan
      - mtr
      - iperf3
      - doggo
      - httpie
      - gping
      - trippy
      - bandwhich
      - rustscan
    state: present

- name: Install Ookla Speedtest CLI (macOS)
  when: ansible_facts["os_family"] == "Darwin"
  block:
    - name: Check if teamookla/speedtest tap is present
      ansible.builtin.command: brew tap
      register: speedtest_taps
      changed_when: false

    - name: Tap teamookla/speedtest
      ansible.builtin.command: brew tap teamookla/speedtest
      when: "'teamookla/speedtest' not in speedtest_taps.stdout_lines"

    - name: Check trust state of teamookla/speedtest tap (Homebrew >= 6.0 trust gate)
      ansible.builtin.command: brew tap-info teamookla/speedtest
      register: speedtest_tap_info
      changed_when: false
      failed_when: false

    - name: Trust teamookla/speedtest tap when Homebrew marks it untrusted
      when: "'Untrusted' in speedtest_tap_info.stdout"
      ansible.builtin.command: brew trust teamookla/speedtest
      changed_when: true

    - name: Install speedtest
      community.general.homebrew:
        name: speedtest
        state: present

# =============================================================================
# Linux (Debian/Ubuntu)
# =============================================================================

# --- system-level packages (apt) ---
- name: Install networking tools from apt (Debian/Ubuntu)
  when: ansible_facts["os_family"] == "Debian"
  become: true
  tags: [sudo]
  ansible.builtin.apt:
    name:
      - nmap
      - arp-scan
      - mtr
      - iperf3
      - httpie
      - tcpdump
    state: present

# --- system-level packages (yum/CentOS 7+) ---
# yum CLI for CentOS 7 compat — see pitfalls/centos7-ansible-yum-dnf-backend.md
- name: Install networking tools from yum (RedHat/CentOS) — yum CLI for CentOS 7 + Rocky/Alma compat
  when: ansible_facts["os_family"] == "RedHat"
  become: true
  tags: [sudo]
  ansible.builtin.shell: |
    set -e
    missing=()
    for p in nmap arp-scan mtr iperf3 tcpdump; do
      rpm -q "$p" >/dev/null 2>&1 || missing+=("$p")
    done
    # NOTE: httpie is in EPEL on CentOS 7 and pip-installed via uv elsewhere
    # if missing, so we skip it here to avoid hard-requiring EPEL.
    if [ -z "${missing[*]:-}" ]; then
      echo "ALL_INSTALLED"
      exit 0
    fi
    yum install -y "${missing[@]}"
  args:
    executable: /bin/bash
  register: networking_yum_result
  changed_when: "'ALL_INSTALLED' not in networking_yum_result.stdout"
  ignore_errors: true

# --- Architecture mapping for GitHub release downloads ---
- name: Set networking tools release architecture
  when: ansible_facts["os_family"] in ["Debian", "RedHat"]
  ansible.builtin.set_fact:
    net_arch_deb: >-
      {{
        'amd64' if target_architecture in ['x86_64', 'amd64']
        else 'arm64' if target_architecture in ['aarch64', 'arm64']
        else target_architecture
      }}

# --- doggo (modern DNS lookup) ---
- name: Check if doggo is installed
  when: ansible_facts["os_family"] in ["Debian", "RedHat"]
  ansible.builtin.command: doggo --version
  register: doggo_check
  changed_when: false
  failed_when: false

- name: Install doggo from GitHub releases (Debian/Ubuntu)
  when:
    - ansible_facts["os_family"] in ["Debian", "RedHat"]
    - doggo_check.rc != 0
    - target_architecture in ['x86_64', 'amd64', 'aarch64', 'arm64']
  block:
    - name: Ensure ~/.local/bin exists (doggo)
      ansible.builtin.file:
        path: "{{ ansible_facts['env']['HOME'] }}/.local/bin"
        state: directory
        mode: '0755'

    - name: Get latest doggo release
      ansible.builtin.uri:
        url: https://api.github.com/repos/mr-karan/doggo/releases/latest
        return_content: true
      register: doggo_release

    - name: Set doggo version (strip v prefix)
      ansible.builtin.set_fact:
        doggo_version: "{{ doggo_release.json.tag_name | regex_replace('^v', '') }}"

    - name: Set doggo arch mapping
      ansible.builtin.set_fact:
        doggo_arch: >-
          {{
            'x86_64' if target_architecture in ['x86_64', 'amd64']
            else 'arm64' if target_architecture in ['aarch64', 'arm64']
            else target_architecture
          }}

    - name: Download doggo tarball
      ansible.builtin.get_url:
        url: "https://github.com/mr-karan/doggo/releases/download/{{ doggo_release.json.tag_name }}/doggo_{{ doggo_version }}_Linux_{{ doggo_arch }}.tar.gz"
        dest: /tmp/doggo.tar.gz
        mode: '0644'
        timeout: 120
      retries: 3
      delay: 5

    - name: Create doggo temp directory
      ansible.builtin.file:
        path: /tmp/doggo
        state: directory
        mode: '0755'

    - name: Extract doggo
      ansible.builtin.unarchive:
        src: /tmp/doggo.tar.gz
        dest: /tmp/doggo
        remote_src: true

    - name: Find doggo binary
      ansible.builtin.find:
        paths: /tmp/doggo
        patterns: doggo
        file_type: file
        recurse: true
      register: doggo_find

    - name: Fail if doggo binary was not found in extracted archive
      when: doggo_find.matched | default(0) | int == 0
      ansible.builtin.fail:
        msg: "doggo binary was not found under /tmp/doggo after extraction"

    - name: Install doggo to ~/.local/bin
      ansible.builtin.copy:
        src: "{{ doggo_find.files[0].path }}"
        dest: "{{ ansible_facts['env']['HOME'] }}/.local/bin/doggo"
        mode: '0755'
        remote_src: true

    - name: Clean up doggo temp files
      ansible.builtin.file:
        path: "{{ item }}"
        state: absent
      loop:
        - /tmp/doggo.tar.gz
        - /tmp/doggo
        - /tmp/LICENSE
        - /tmp/README.md
  rescue:
    - name: Warn doggo installation failed
      ansible.builtin.debug:
        msg: "doggo installation failed — skipping. Error: {{ ansible_failed_result.msg | default('unknown') }}"

    - name: Clean up doggo temp files (rescue)
      ansible.builtin.file:
        path: "{{ item }}"
        state: absent
      loop:
        - /tmp/doggo.tar.gz
        - /tmp/doggo

# --- gping (graphical ping) ---
- name: Check if gping is installed
  when: ansible_facts["os_family"] in ["Debian", "RedHat"]
  ansible.builtin.command: gping --version
  register: gping_check
  changed_when: false
  failed_when: false

- name: Install gping from GitHub releases (Debian/Ubuntu)
  when:
    - ansible_facts["os_family"] in ["Debian", "RedHat"]
    - gping_check.rc != 0
    - target_architecture in ['x86_64', 'amd64', 'aarch64', 'arm64']
  block:
    - name: Ensure ~/.local/bin exists (gping)
      ansible.builtin.file:
        path: "{{ ansible_facts['env']['HOME'] }}/.local/bin"
        state: directory
        mode: '0755'

    - name: Set gping arch mapping
      ansible.builtin.set_fact:
        gping_arch: >-
          {{
            'x86_64' if target_architecture in ['x86_64', 'amd64']
            else 'arm64' if target_architecture in ['aarch64', 'arm64']
            else target_architecture
          }}

    - name: Get latest gping release
      ansible.builtin.uri:
        url: https://api.github.com/repos/orf/gping/releases/latest
        return_content: true
      register: gping_release

    - name: Download gping tarball
      ansible.builtin.get_url:
        url: "https://github.com/orf/gping/releases/download/{{ gping_release.json.tag_name }}/gping-Linux-musl-{{ gping_arch }}.tar.gz"
        dest: /tmp/gping.tar.gz
        mode: '0644'
        timeout: 120
      retries: 3
      delay: 5

    - name: Extract gping to ~/.local/bin
      ansible.builtin.unarchive:
        src: /tmp/gping.tar.gz
        dest: "{{ ansible_facts['env']['HOME'] }}/.local/bin"
        remote_src: true

    - name: Clean up gping tarball
      ansible.builtin.file:
        path: /tmp/gping.tar.gz
        state: absent
  rescue:
    - name: Warn gping installation failed
      ansible.builtin.debug:
        msg: "gping installation failed — skipping. Error: {{ ansible_failed_result.msg | default('unknown') }}"

    - name: Clean up gping temp files (rescue)
      ansible.builtin.file:
        path: /tmp/gping.tar.gz
        state: absent

# --- trippy (TUI traceroute) ---
- name: Check if trippy is installed
  when: ansible_facts["os_family"] in ["Debian", "RedHat"]
  ansible.builtin.command: trip --version
  register: trippy_check
  changed_when: false
  failed_when: false

- name: Set trippy target mappings
  when:
    - ansible_facts["os_family"] in ["Debian", "RedHat"]
    - trippy_check.rc != 0
  ansible.builtin.set_fact:
    trippy_gnu_target: >-
      {{
        'x86_64-unknown-linux-gnu' if target_architecture in ['x86_64', 'amd64']
        else 'aarch64-unknown-linux-gnu' if target_architecture in ['aarch64', 'arm64']
        else 'armv7-unknown-linux-gnueabihf' if target_architecture in ['armv7l', 'armv7', 'armhf']
        else ''
      }}
    trippy_musl_target: >-
      {{
        'x86_64-unknown-linux-musl' if target_architecture in ['x86_64', 'amd64']
        else 'aarch64-unknown-linux-musl' if target_architecture in ['aarch64', 'arm64']
        else 'armv7-unknown-linux-musleabihf' if target_architecture in ['armv7l', 'armv7', 'armhf']
        else ''
      }}

- name: Skip trippy on unsupported architecture
  when:
    - ansible_facts["os_family"] in ["Debian", "RedHat"]
    - trippy_check.rc != 0
    - trippy_gnu_target | trim == '' or trippy_musl_target | trim == ''
  ansible.builtin.debug:
    msg: "trippy skipped: no release for {{ target_architecture }}"

- name: Install trippy from GitHub releases (Debian/Ubuntu, system-level)
  # Use the musl target for parity with the user fallback below: gnu binaries
  # compiled on modern CI can require a glibc newer than Jammy's 2.35 and fail
  # to start. Upstream ships musl builds for x86_64, aarch64, and armv7.
  when:
    - ansible_facts["os_family"] in ["Debian", "RedHat"]
    - trippy_check.rc != 0
  become: true
  tags: [sudo]
  block:
    - name: Get latest trippy release
      ansible.builtin.uri:
        url: https://api.github.com/repos/fujiapple852/trippy/releases/latest
        return_content: true
      register: trippy_release

    - name: Download trippy tarball (musl)
      ansible.builtin.get_url:
        url: "https://github.com/fujiapple852/trippy/releases/download/{{ trippy_release.json.tag_name }}/trippy-{{ trippy_release.json.tag_name }}-{{ trippy_musl_target }}.tar.gz"
        dest: /tmp/trippy.tar.gz
        mode: '0644'
        timeout: 120
      retries: 3
      delay: 5

    - name: Create trippy temp directory
      ansible.builtin.file:
        path: /tmp/trippy
        state: directory
        mode: '0755'

    - name: Extract trippy
      ansible.builtin.unarchive:
        src: /tmp/trippy.tar.gz
        dest: /tmp/trippy
        remote_src: true

    - name: Find trip binary
      ansible.builtin.find:
        paths: /tmp/trippy
        patterns: trip
        file_type: file
        recurse: true
      register: trippy_find

    - name: Fail if trip binary was not found in extracted archive
      when: trippy_find.matched | default(0) | int == 0
      ansible.builtin.fail:
        msg: "trip binary was not found under /tmp/trippy after extraction"

    - name: Install trip to /usr/local/bin
      ansible.builtin.copy:
        src: "{{ trippy_find.files[0].path }}"
        dest: /usr/local/bin/trip
        mode: '0755'
        remote_src: true

    - name: Clean up trippy temp files
      ansible.builtin.file:
        path: "{{ item }}"
        state: absent
      loop:
        - /tmp/trippy.tar.gz
        - /tmp/trippy
  rescue:
    - name: Warn trippy installation failed (system-level)
      ansible.builtin.debug:
        msg: "trippy system-level installation failed — skipping. Error: {{ ansible_failed_result.msg | default('unknown') }}"

    - name: Clean up trippy temp files (rescue)
      ansible.builtin.file:
        path: "{{ item }}"
        state: absent
      loop:
        - /tmp/trippy.tar.gz
        - /tmp/trippy

# --- trippy (user-level fallback) ---
- name: Re-check if trippy is installed
  when: ansible_facts["os_family"] in ["Debian", "RedHat"]
  ansible.builtin.shell: command -v trip
  register: trippy_recheck
  changed_when: false
  failed_when: false

- name: Install trippy from GitHub releases (user-level, no sudo)
  when:
    - ansible_facts["os_family"] in ["Debian", "RedHat"]
    - trippy_recheck.rc != 0
  block:
    - name: Ensure ~/.local/bin exists (trippy)
      ansible.builtin.file:
        path: "{{ ansible_facts['env']['HOME'] }}/.local/bin"
        state: directory
        mode: '0755'

    - name: Get latest trippy release (user)
      ansible.builtin.uri:
        url: https://api.github.com/repos/fujiapple852/trippy/releases/latest
        return_content: true
      register: trippy_release_user

    - name: Download trippy tarball (user)
      ansible.builtin.get_url:
        url: "https://github.com/fujiapple852/trippy/releases/download/{{ trippy_release_user.json.tag_name }}/trippy-{{ trippy_release_user.json.tag_name }}-{{ trippy_musl_target }}.tar.gz"
        dest: /tmp/trippy.tar.gz
        mode: '0644'
        timeout: 120
      retries: 3
      delay: 5

    - name: Create trippy temp directory (user)
      ansible.builtin.file:
        path: /tmp/trippy-user
        state: directory
        mode: '0755'

    - name: Extract trippy (user)
      ansible.builtin.unarchive:
        src: /tmp/trippy.tar.gz
        dest: /tmp/trippy-user
        remote_src: true

    - name: Find trip binary (user)
      ansible.builtin.find:
        paths: /tmp/trippy-user
        patterns: trip
        file_type: file
        recurse: true
      register: trippy_find_user

    - name: Fail if trip binary was not found in extracted archive (user)
      when: trippy_find_user.matched | default(0) | int == 0
      ansible.builtin.fail:
        msg: "trip binary was not found under /tmp/trippy-user after extraction"

    - name: Install trip to ~/.local/bin
      ansible.builtin.copy:
        src: "{{ trippy_find_user.files[0].path }}"
        dest: "{{ ansible_facts['env']['HOME'] }}/.local/bin/trip"
        mode: '0755'
        remote_src: true

    - name: Clean up trippy temp files (user)
      ansible.builtin.file:
        path: "{{ item }}"
        state: absent
      loop:
        - /tmp/trippy.tar.gz
        - /tmp/trippy-user
  rescue:
    - name: Warn trippy installation failed (user-level)
      ansible.builtin.debug:
        msg: "trippy user-level installation failed — skipping. Error: {{ ansible_failed_result.msg | default('unknown') }}"

    - name: Clean up trippy temp files (user rescue)
      ansible.builtin.file:
        path: "{{ item }}"
        state: absent
      loop:
        - /tmp/trippy.tar.gz
        - /tmp/trippy-user

# --- bandwhich (bandwidth monitor) ---
- name: Check if bandwhich is installed
  when: ansible_facts["os_family"] in ["Debian", "RedHat"]
  ansible.builtin.command: bandwhich --version
  register: bandwhich_check
  changed_when: false
  failed_when: false

- name: Install bandwhich from GitHub releases (Debian/Ubuntu)
  when:
    - ansible_facts["os_family"] in ["Debian", "RedHat"]
    - bandwhich_check.rc != 0
    - target_architecture in ['x86_64', 'amd64', 'aarch64', 'arm64']
  block:
    - name: Ensure ~/.local/bin exists (bandwhich)
      ansible.builtin.file:
        path: "{{ ansible_facts['env']['HOME'] }}/.local/bin"
        state: directory
        mode: '0755'

    - name: Get latest bandwhich release
      ansible.builtin.uri:
        url: https://api.github.com/repos/imsnif/bandwhich/releases/latest
        return_content: true
      register: bandwhich_release

    - name: Download bandwhich tarball
      ansible.builtin.get_url:
        url: "https://github.com/imsnif/bandwhich/releases/download/{{ bandwhich_release.json.tag_name }}/bandwhich-{{ bandwhich_release.json.tag_name }}-{{ target_architecture }}-unknown-linux-musl.tar.gz"
        dest: /tmp/bandwhich.tar.gz
        mode: '0644'
        timeout: 120
      retries: 3
      delay: 5

    - name: Extract bandwhich to ~/.local/bin
      ansible.builtin.unarchive:
        src: /tmp/bandwhich.tar.gz
        dest: "{{ ansible_facts['env']['HOME'] }}/.local/bin"
        remote_src: true
        extra_opts:
          - --wildcards
          - bandwhich

    - name: Clean up bandwhich tarball
      ansible.builtin.file:
        path: /tmp/bandwhich.tar.gz
        state: absent
  rescue:
    - name: Warn bandwhich installation failed
      ansible.builtin.debug:
        msg: "bandwhich installation failed — skipping. Error: {{ ansible_failed_result.msg | default('unknown') }}"

    - name: Clean up bandwhich temp files (rescue)
      ansible.builtin.file:
        path: /tmp/bandwhich.tar.gz
        state: absent

# --- Ookla Speedtest CLI ---
- name: Check if speedtest is installed
  when: ansible_facts["os_family"] in ["Debian", "RedHat"]
  ansible.builtin.command: speedtest --version
  register: speedtest_check
  changed_when: false
  failed_when: false

- name: Install Ookla Speedtest CLI (Debian/Ubuntu)
  when:
    - ansible_facts["os_family"] in ["Debian", "RedHat"]
    - speedtest_check.rc != 0
  block:
    - name: Ensure ~/.local/bin exists (speedtest)
      ansible.builtin.file:
        path: "{{ ansible_facts['env']['HOME'] }}/.local/bin"
        state: directory
        mode: '0755'

    - name: Set speedtest arch mapping
      ansible.builtin.set_fact:
        speedtest_arch: >-
          {{
            'x86_64' if target_architecture in ['x86_64', 'amd64']
            else 'aarch64' if target_architecture in ['aarch64', 'arm64']
            else 'armhf' if target_architecture in ['armv7l', 'armhf']
            else ''
          }}

    - name: Download Ookla Speedtest CLI
      ansible.builtin.get_url:
        url: "https://install.speedtest.net/app/cli/ookla-speedtest-1.2.0-linux-{{ speedtest_arch }}.tgz"
        dest: /tmp/speedtest.tgz
        mode: '0644'
        timeout: 120
      retries: 3
      delay: 5

    - name: Extract speedtest
      ansible.builtin.unarchive:
        src: /tmp/speedtest.tgz
        dest: /tmp
        remote_src: true

    - name: Install speedtest to ~/.local/bin
      ansible.builtin.copy:
        src: /tmp/speedtest
        dest: "{{ ansible_facts['env']['HOME'] }}/.local/bin/speedtest"
        mode: '0755'
        remote_src: true

    - name: Clean up speedtest temp files
      ansible.builtin.file:
        path: "{{ item }}"
        state: absent
      loop:
        - /tmp/speedtest.tgz
        - /tmp/speedtest
        - /tmp/speedtest.md
        - /tmp/speedtest.5

# --- rustscan (fast port scanner) ---
- name: Check if rustscan is installed
  when: ansible_facts["os_family"] in ["Debian", "RedHat"]
  ansible.builtin.command: rustscan --version
  register: rustscan_check
  changed_when: false
  failed_when: false

- name: Install rustscan from GitHub releases (Debian/Ubuntu)
  when:
    - ansible_facts["os_family"] in ["Debian", "RedHat"]
    - rustscan_check.rc != 0
  block:
    - name: Ensure ~/.local/bin exists (rustscan)
      ansible.builtin.file:
        path: "{{ ansible_facts['env']['HOME'] }}/.local/bin"
        state: directory
        mode: '0755'

    - name: Set rustscan arch mapping
      ansible.builtin.set_fact:
        rustscan_arch: >-
          {{
            'x86_64' if target_architecture in ['x86_64', 'amd64']
            else 'aarch64' if target_architecture in ['aarch64', 'arm64']
            else target_architecture
          }}

    - name: Download rustscan archive (x86_64)
      when: rustscan_arch == 'x86_64'
      ansible.builtin.get_url:
        url: "https://github.com/RustScan/RustScan/releases/latest/download/x86_64-linux-rustscan.tar.gz.zip"
        dest: /tmp/rustscan.zip
        mode: '0644'
        timeout: 120
      retries: 3
      delay: 5

    - name: Download rustscan archive (aarch64)
      when: rustscan_arch == 'aarch64'
      ansible.builtin.get_url:
        url: "https://github.com/RustScan/RustScan/releases/latest/download/aarch64-linux-rustscan.zip"
        dest: /tmp/rustscan.zip
        mode: '0644'
        timeout: 120
      retries: 3
      delay: 5

    - name: Extract and install rustscan (x86_64 - double wrapped)
      when: rustscan_arch == 'x86_64'
      ansible.builtin.shell: |
        set -e
        cd /tmp
        python3 -m zipfile -e rustscan.zip .
        tar xzf x86_64-linux-rustscan.tar.gz
        cp rustscan "{{ ansible_facts['env']['HOME'] }}/.local/bin/rustscan"
        chmod 0755 "{{ ansible_facts['env']['HOME'] }}/.local/bin/rustscan"
      args:
        executable: /bin/bash
      changed_when: true

    - name: Extract and install rustscan (aarch64)
      when: rustscan_arch == 'aarch64'
      ansible.builtin.shell: |
        set -e
        cd /tmp
        python3 -m zipfile -e rustscan.zip .
        cp rustscan "{{ ansible_facts['env']['HOME'] }}/.local/bin/rustscan"
        chmod 0755 "{{ ansible_facts['env']['HOME'] }}/.local/bin/rustscan"
      args:
        executable: /bin/bash
      changed_when: true

    - name: Clean up rustscan temp files
      ansible.builtin.shell: |
        rm -f /tmp/rustscan.zip /tmp/rustscan /tmp/x86_64-linux-rustscan.tar.gz /tmp/aarch64-linux-rustscan
      args:
        executable: /bin/bash
      changed_when: false
  rescue:
    - name: Warn rustscan installation failed
      ansible.builtin.debug:
        msg: "rustscan installation failed — skipping. Error: {{ ansible_failed_result.msg | default('unknown') }}"

    - name: Clean up rustscan temp files (rescue)
      ansible.builtin.shell: |
        rm -f /tmp/rustscan.zip /tmp/rustscan /tmp/x86_64-linux-rustscan.tar.gz /tmp/aarch64-linux-rustscan
      args:
        executable: /bin/bash
      changed_when: false

# =============================================================================
# Tunnel tools — ngrok + cloudflared
# Tags: tunnel_tools (gated by installTunnelTools chezmoi flag)
# =============================================================================

# --- ngrok ---

- name: Install ngrok (macOS)
  tags: [tunnel_tools]
  when: ansible_facts["os_family"] == "Darwin"
  block:
    # Skip the install when the binary is already on PATH — ngrok cask downloads
    # from bin.ngrok.com which periodically returns HTTP 52 "empty reply from
    # server" (Heroku-fronted, transient). Pre-checking lets re-applies short-
    # circuit instead of paying a 1-2 min download attempt that may fail.
    - name: Check if ngrok is already installed (macOS)
      ansible.builtin.command: which ngrok
      register: ngrok_present_macos
      changed_when: false
      failed_when: false

    - name: Install ngrok via Homebrew (with retries for flaky bin.ngrok.com CDN)
      when: ngrok_present_macos.rc != 0
      community.general.homebrew:
        name: ngrok
        state: present
      register: ngrok_brew_install
      until: ngrok_brew_install is succeeded
      retries: 3
      delay: 15

  rescue:
    # Most common failure: `Download failed: ... curl: (52) Empty reply from
    # server` from bin.ngrok.com (Heroku CDN). Transient — retrying minutes
    # later usually works. We don't fail the whole apply over it; user can
    # re-run `chezmoi apply --tag tunnel_tools` or `brew install --cask ngrok`
    # later. See pitfalls/ngrok-cask-download-flaky.md for the full story.
    - name: Warn ngrok installation failed (macOS)
      ansible.builtin.debug:
        msg: |-
          ngrok macOS installation failed after 3 retries — skipping. Re-run
          `brew install --cask ngrok` later. Error: {{ ansible_failed_result.msg | default('unknown') }}

- name: Install ngrok (Debian/Ubuntu)
  tags: [tunnel_tools]
  when: ansible_facts["os_family"] == "Debian"
  block:
    - name: Check if ngrok is installed
      ansible.builtin.command: ngrok version
      register: ngrok_check
      changed_when: false
      failed_when: false

    - name: Install ngrok via official apt repo (Debian/Ubuntu)
      when: ngrok_check.rc != 0
      become: true
      tags: [sudo]
      block:
        - name: Download ngrok apt GPG key
          ansible.builtin.get_url:
            url: https://ngrok-agent.s3.amazonaws.com/ngrok.asc
            dest: /tmp/ngrok.asc
            timeout: 60

        - name: Install ngrok GPG key
          ansible.builtin.shell: |
            gpg --dearmor < /tmp/ngrok.asc > /etc/apt/keyrings/ngrok.gpg
            chmod 0644 /etc/apt/keyrings/ngrok.gpg
          args:
            creates: /etc/apt/keyrings/ngrok.gpg

        - name: Add ngrok apt source
          ansible.builtin.copy:
            content: "deb [signed-by=/etc/apt/keyrings/ngrok.gpg] https://ngrok-agent.s3.amazonaws.com buster main\n"
            dest: /etc/apt/sources.list.d/ngrok.list
            mode: '0644'

        - name: Install ngrok from apt
          ansible.builtin.apt:
            name: ngrok
            update_cache: true
            state: present

    - name: Re-check ngrok after apt install
      ansible.builtin.command: ngrok version
      register: ngrok_recheck
      changed_when: false
      failed_when: false

    - name: Install ngrok from tgz (user-level fallback)
      when: ngrok_recheck.rc != 0
      block:
        - name: Set ngrok tgz architecture
          ansible.builtin.set_fact:
            ngrok_tgz_arch: >-
              {{ 'amd64' if target_architecture in ['x86_64', 'amd64']
                 else 'arm64' if target_architecture in ['aarch64', 'arm64']
                 else '' }}

        - name: Ensure ~/.local/bin exists
          ansible.builtin.file:
            path: "{{ ansible_facts['env']['HOME'] }}/.local/bin"
            state: directory
            mode: '0755'

        - name: Download ngrok tgz (user-level)
          ansible.builtin.get_url:
            url: "https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-linux-{{ ngrok_tgz_arch }}.tgz"
            dest: /tmp/ngrok.tgz
            timeout: 120
          when: ngrok_tgz_arch != ''

        - name: Extract and install ngrok (user-level)
          ansible.builtin.shell: |
            set -e
            tar -xzf /tmp/ngrok.tgz -C /tmp/
            install -m 0755 /tmp/ngrok "{{ ansible_facts['env']['HOME'] }}/.local/bin/ngrok"
          args:
            executable: /bin/bash
            creates: "{{ ansible_facts['env']['HOME'] }}/.local/bin/ngrok"
          changed_when: true

        - name: Clean up ngrok tgz
          ansible.builtin.file:
            path: /tmp/ngrok.tgz
            state: absent

  rescue:
    - name: Warn ngrok installation failed
      ansible.builtin.debug:
        msg: "ngrok installation failed — skipping. Error: {{ ansible_failed_result.msg | default('unknown') }}"

    - name: Clean up ngrok temp files (rescue)
      ansible.builtin.file:
        path: "{{ item }}"
        state: absent
      loop:
        - /tmp/ngrok.tgz
        - /tmp/ngrok
        - /tmp/ngrok.asc

- name: Install ngrok (RedHat/CentOS — tgz user-level)
  tags: [tunnel_tools]
  when: ansible_facts["os_family"] == "RedHat"
  block:
    - name: Check if ngrok is installed (RedHat)
      ansible.builtin.command: ngrok version
      register: ngrok_rh_check
      changed_when: false
      failed_when: false

    - name: Set ngrok tgz architecture (RedHat)
      when: ngrok_rh_check.rc != 0
      ansible.builtin.set_fact:
        ngrok_rh_arch: >-
          {{ 'amd64' if target_architecture in ['x86_64', 'amd64']
             else 'arm64' if target_architecture in ['aarch64', 'arm64']
             else '' }}

    - name: Ensure ~/.local/bin exists (RedHat)
      when: ngrok_rh_check.rc != 0 and ngrok_rh_arch | default('') != ''
      ansible.builtin.file:
        path: "{{ ansible_facts['env']['HOME'] }}/.local/bin"
        state: directory
        mode: '0755'

    - name: Download and install ngrok tgz (RedHat)
      when: ngrok_rh_check.rc != 0 and ngrok_rh_arch | default('') != ''
      ansible.builtin.shell: |
        set -e
        curl -fsSL "https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-linux-{{ ngrok_rh_arch }}.tgz" -o /tmp/ngrok.tgz
        tar -xzf /tmp/ngrok.tgz -C /tmp/
        install -m 0755 /tmp/ngrok "{{ ansible_facts['env']['HOME'] }}/.local/bin/ngrok"
        rm -f /tmp/ngrok.tgz /tmp/ngrok
      args:
        executable: /bin/bash
        creates: "{{ ansible_facts['env']['HOME'] }}/.local/bin/ngrok"
      changed_when: true

  rescue:
    - name: Warn ngrok installation failed (RedHat)
      ansible.builtin.debug:
        msg: "ngrok installation failed on RedHat — skipping."

    - name: Clean up ngrok temp files (RedHat rescue)
      ansible.builtin.shell: rm -f /tmp/ngrok.tgz /tmp/ngrok
      args:
        executable: /bin/bash
      changed_when: false

# --- cloudflared ---

- name: Install cloudflared (macOS)
  tags: [tunnel_tools]
  when: ansible_facts["os_family"] == "Darwin"
  community.general.homebrew:
    name: cloudflared
    state: present

- name: Install cloudflared (Debian/Ubuntu)
  tags: [tunnel_tools]
  when: ansible_facts["os_family"] == "Debian"
  block:
    - name: Check if cloudflared is installed
      ansible.builtin.command: cloudflared version
      register: cf_check
      changed_when: false
      failed_when: false

    - name: Set cloudflared deb architecture
      when: cf_check.rc != 0
      ansible.builtin.set_fact:
        cf_deb_arch: >-
          {{ 'amd64' if target_architecture in ['x86_64', 'amd64']
             else 'arm64' if target_architecture in ['aarch64', 'arm64']
             else 'arm' if target_architecture in ['armv7l', 'armhf']
             else '' }}

    - name: Install cloudflared .deb (system-level)
      when: cf_check.rc != 0 and cf_deb_arch | default('') != ''
      become: true
      tags: [sudo]
      block:
        - name: Get latest cloudflared release info
          ansible.builtin.uri:
            url: https://api.github.com/repos/cloudflare/cloudflared/releases/latest
            return_content: true
            timeout: 30
          register: cf_release
          retries: 3
          delay: 5
          until: cf_release is succeeded

        - name: Download cloudflared .deb
          ansible.builtin.get_url:
            url: "https://github.com/cloudflare/cloudflared/releases/download/{{ cf_release.json.tag_name }}/cloudflared-linux-{{ cf_deb_arch }}.deb"
            dest: /tmp/cloudflared.deb
            timeout: 120

        - name: Install cloudflared .deb
          ansible.builtin.apt:
            deb: /tmp/cloudflared.deb
            state: present

        - name: Clean up cloudflared .deb
          ansible.builtin.file:
            path: /tmp/cloudflared.deb
            state: absent

    - name: Re-check cloudflared after deb install
      ansible.builtin.command: cloudflared version
      register: cf_recheck
      changed_when: false
      failed_when: false

    - name: Install cloudflared binary (user-level fallback)
      when: cf_recheck.rc != 0 and cf_deb_arch | default('') != ''
      block:
        - name: Ensure ~/.local/bin exists
          ansible.builtin.file:
            path: "{{ ansible_facts['env']['HOME'] }}/.local/bin"
            state: directory
            mode: '0755'

        - name: Get latest cloudflared release info (fallback)
          ansible.builtin.uri:
            url: https://api.github.com/repos/cloudflare/cloudflared/releases/latest
            return_content: true
            timeout: 30
          register: cf_release_fb
          retries: 3
          delay: 5
          until: cf_release_fb is succeeded

        - name: Download cloudflared binary (user-level)
          ansible.builtin.get_url:
            url: "https://github.com/cloudflare/cloudflared/releases/download/{{ cf_release_fb.json.tag_name }}/cloudflared-linux-{{ cf_deb_arch }}"
            dest: "{{ ansible_facts['env']['HOME'] }}/.local/bin/cloudflared"
            mode: '0755'
            timeout: 120

  rescue:
    - name: Warn cloudflared installation failed
      ansible.builtin.debug:
        msg: "cloudflared installation failed — skipping. Error: {{ ansible_failed_result.msg | default('unknown') }}"

    - name: Clean up cloudflared temp files (rescue)
      ansible.builtin.file:
        path: /tmp/cloudflared.deb
        state: absent

- name: Install cloudflared (RedHat/CentOS — rpm)
  tags: [tunnel_tools]
  when: ansible_facts["os_family"] == "RedHat"
  block:
    - name: Check if cloudflared is installed (RedHat)
      ansible.builtin.command: cloudflared version
      register: cf_rh_check
      changed_when: false
      failed_when: false

    - name: Set cloudflared rpm architecture (RedHat)
      when: cf_rh_check.rc != 0
      ansible.builtin.set_fact:
        cf_rpm_arch: >-
          {{ 'x86_64' if target_architecture in ['x86_64', 'amd64']
             else 'aarch64' if target_architecture in ['aarch64', 'arm64']
             else '' }}

    - name: Get latest cloudflared release info (RedHat)
      when: cf_rh_check.rc != 0 and cf_rpm_arch | default('') != ''
      ansible.builtin.uri:
        url: https://api.github.com/repos/cloudflare/cloudflared/releases/latest
        return_content: true
        timeout: 30
      register: cf_rh_release
      retries: 3
      delay: 5
      until: cf_rh_release is succeeded

    - name: Install cloudflared .rpm (system-level)
      when: cf_rh_check.rc != 0 and cf_rpm_arch | default('') != ''
      become: true
      tags: [sudo]
      block:
        - name: Download cloudflared .rpm
          ansible.builtin.get_url:
            url: "https://github.com/cloudflare/cloudflared/releases/download/{{ cf_rh_release.json.tag_name }}/cloudflared-linux-{{ cf_rpm_arch }}.rpm"
            dest: /tmp/cloudflared.rpm
            timeout: 120

        - name: Install cloudflared .rpm
          ansible.builtin.shell: |
            rpm -i --force /tmp/cloudflared.rpm
          args:
            executable: /bin/bash
          changed_when: true

        - name: Clean up cloudflared .rpm
          ansible.builtin.file:
            path: /tmp/cloudflared.rpm
            state: absent

  rescue:
    - name: Warn cloudflared installation failed (RedHat)
      ansible.builtin.debug:
        msg: "cloudflared installation failed on RedHat — skipping."

    - name: Clean up cloudflared temp files (RedHat rescue)
      ansible.builtin.file:
        path: /tmp/cloudflared.rpm
        state: absent
