# TODO

Long-term backlog for the dotfiles repository. See [`AGENTS.md`](AGENTS.md) for
maintenance contract and [`README.md`](README.md) for current architecture.

> **For agents**: when the user surfaces an idea explicitly **not** being implemented
> this session (signals: "maybe later", "nice to have", "if I'm interested",
> "工程量太大需要再評估", "先記下來"), add it here with priority + effort tags.
> Do not create new `ROADMAP.md` / `IDEAS.md` / `BACKLOG.md` files — `TODO.md` is
> the single backlog index. Long-form research goes in [`backlog/<slug>.md`](backlog/).

## How to read this

Each item carries two tags:

- **Priority**: `P1` (next likely batch) · `P2` (worth doing) · `P3` (someday) · `P?` (needs evaluation first — spike before committing)
- **Effort**: `S` (< 1h) · `M` (half day) · `L` (multi-day) · `XL` (architectural, design doc first)

A trailing `→ [research](backlog/<slug>.md)` link means the item has accompanying
investigation, design notes, or paused troubleshooting — read that first before
re-investigating. Items without a link are simple enough to act on directly.

When implementing an item, move it to the `## Done` section in the same commit
with a one-line summary of what shipped.

---

## P1 — Likely next batch

- [ ] **[S] Starship status-aware modules** — enable `status` / `cmd_duration` / `shlvl` / `container` in `dot_config/starship.toml`. Pure additive, no risk. → [research](backlog/starship-context-modules.md)
- [ ] **[S] tmux2k bandwidth bug** — drop `bandwidth` from `@tmux2k-right-plugins` in `dot_config/tmux/theme.tmux2k.conf` (uint64 underflow already documented in the file's own comment). → [research](backlog/tmux2k-tuning.md)
- [ ] **[S] tmux2k theme alignment** — switch `@tmux2k-theme` from `onedark` to `catppuccin` so the tmux2k layout matches Ghostty/Neovim's colour story. → [research](backlog/tmux2k-tuning.md)
- [ ] **[M] Fix Claude Code hook on Ubuntu (headless)** — `Stop hook error: ... org.freedesktop.Notifications was not provided by any .service files`. Headless servers have no notification daemon; need either fallback in the hook or `libnotify` + lightweight daemon.

## P2 — Worth doing, no rush

- [ ] **[S] Migrate B-class zsh tools to shared shell layer** — A-class (8 pure-POSIX files: `06_cargo` `07_bunx_cli` `25_bat` `26_eza` `29_marimo` `34_openclaw` `37_lazygit` `40_codexbar`) already moved to `dot_config/shell/`, so bash users now get `lg` / `ls`/`la`/`ll`/`lt`/`llt` / `ccusage` / `cbu`/`cbc`/`cbca` / `BAT_THEME` / cargo PATH and per-shell `marimo` + `openclaw` completions. The five "B-class" files still need small rewrites before they can move:
  - `35_yazi.zsh` — change `function y() { ... }` to POSIX `y() { ... }`; rest is portable (`read -r -d ''` works in both shells).
  - `28_tldr.zsh` — replace `typeset -ga TLDR_LANGUAGES=(...)` with a space-separated string + `for lang in $TLDR_LANGUAGES`, or shell-detect dispatch.
  - `32_try.zsh` — `${TRY_PATH:A}` and `${TRY_PATH:h}` are zsh-only string modifiers; replace with `cd "$TRY_PATH" && pwd` / `dirname "$TRY_PATH"`. **Verified 2026-07-13** that `ruby try.rb init` output is pure POSIX (try-cli 1.7.1 only branches fish vs bash/zsh, no `bindkey`) — no `[ -n "$ZSH_VERSION" ]` gate needed, one cache serves both shells. `try-update-completion` (`10_aliases.zsh`) must move too. `try-sesh` / `tsesh` aliases already portable. → [research](backlog/try-bash-port.md)
  - `38_lazyvim.zsh` — fully portable already (`local args=()` + `args+=()` work in bash too); just rename and ship.
  After moving, update `docs/shells/aliases.md` rows + scope (`zsh` → `both`), and the matching rows in `docs/shells/aliases.zh-TW.md`. Verify with `bash -i -c 'type y tldrf lazy-update'`. (`36_pueue.zsh` was deleted on 2026-05-13 — pqsum migrated to `dot_dotfiles/bin/executable_pqsum` Python uv-script; both shells now resolve `pqsum` via PATH.)
- [ ] **[M] bash conda/mamba lazy-init** — `dot_config/zsh/tools/04_conda_mamba.zsh` has zsh-specific `unfunction` + `shell.zsh hook` and is hard to share verbatim. Plan: extract the detection logic (which conda root / micromamba binary / `MAMBA_ROOT_PREFIX`) into `dot_config/shell/04_conda_mamba_detect.sh` so both shells reuse the search; then write `dot_config/bash/04_conda_mamba.bash` using `conda shell.bash hook` / `micromamba shell hook --shell bash`. Without this, bash-primary machines have to run `conda init bash` (which writes a duplicate-init block to `~/.bashrc` that chezmoi would fight). Out of scope for the A-class migration — defer until a bash-primary host actually needs conda. See also the existing P2 entry on the conda ansible role.
- [ ] **[M] miniforge/conda ansible role** — `dot_config/zsh/tools/04_conda_mamba.zsh` already lazy-loads if conda is present, but no role installs it. Decide: ansible role vs delegate to `mise`/`brew install --cask miniforge`.
- [ ] **[M] nvm ansible role** — `02_legacy_tools.zsh` wires PATH + provides `load-nvm` alias, but no role installs nvm itself. May be obviated by mise consolidation (see P? below).
- [ ] **[S] bun / pnpm / go install via ansible** — PATH wiring already in `02_legacy_tools.zsh`; just need install steps (likely additions to `js_cli_tools` and a new `go_tools` role, or fold into `devtools`).
- [ ] **[M] Pueue config via chezmoi** — manage `~/.config/pueue/pueue.yml` and decide macOS strategy (`PUEUE_CONFIG_PATH` env var vs path sync to `~/Library/Application Support/pueue/pueue.yml`). (Partial 2026-05-13: pqsum migrated to Python binary + `fleet pueue` SSH read-only view + AI cleanability/recovery analysis landed. Still pending: config templating + macOS daemon autostart.)
- [ ] **[?/M] Pueue TLS remote-connect profiles** — `dot_config/pueue/pueue.yml.tmpl` per-host client profiles, daemon bind override (`0.0.0.0:port`), cert distribution, firewall opening via ansible. Enables `pueue -c HOST add/kill/status` natively → unlocks `fleet pueue --clean` and write-ops across the fleet. See [docs/tools/pueue.md](docs/tools/pueue.md) § Deferred. Upstream wiki: <https://github.com/Nukesor/pueue/wiki/Connect-to-remote>.
- [ ] **[?/L] Agent quota auto-continue loop** — current shipped scope is explicit (`tv agent-wakeup` / `agent-continue-at`) one-off scheduling. A full supervisor would detect quota waits, schedule `continue`, repeat after the next quota hit, and stop only when the session exits or reaches a non-quota terminal state. Needs robust pane-state parsing before automating writes into live agent panes. → [research](backlog/agent-quota-auto-loop.md)
- [ ] **[?/M] Remote image paste into coding agents (SSH)** — no way today to get a local screenshot into a Claude Code / Neovim agent on a remote box (OSC 52 is text-only, wrong direction). Likely two complementary picks: `claude-ssh-image-skill` (ccimgd/ccimg + `/paste-image`, :9998) for terminal/tmux Claude, and VS Code Remote-SSH or `sshimg.nvim` (:9999) for the editor side. Reverse-tunnel + user-daemon cost to weigh; survey done, not chosen. → [research](backlog/remote-image-paste-into-agents.md) · [survey](docs/tools/remote-image-paste.md)
- [x] **[?/S] Agent quota warmup at scheduled time** — SHIPPED 2026-06-20 as `agent-warmup`. Key finding: the 2026-06-15 billing split means headless `claude -p` no longer moves the 5-hour subscription window (only interactive does), so the tool drives a real interactive `claude` TUI in a detached tmux session (isolated cwd, subscription auth) via launchd/systemd (`install`) or pueue (`at`). → [docs](docs/tools/agent-warmup.md) · [research](backlog/agent-quota-warmup-at-time.md) · [pitfall](pitfalls/headless-claude-p-does-not-move-5h-window.md)
- [ ] **[?/M] Agent delayed-run scheduler for non-quota work** — native support exists in some surfaces (Claude `/loop` / Desktop scheduled tasks / Routines; Codex App Automations), but local cross-agent tmux workflows still need a small wrapper for "resume this exact agent/session at 09:31 and run the market-open smoke test". Evaluate `agent-run-at` / `agent-resume-at` backed by pueue, with per-agent adapters for Claude/Codex/OpenCode and dashboard visibility. → [research](backlog/agent-delayed-run-scheduler.md)
- [ ] **[?/XL] Native agent-session dashboard TUI** — evaluate a first-class `agent-session` / `tv agent-session` replacement that is not constrained by TV channels: unified live pane dashboard, quota/reset timers, scheduled/auto-continue controls, transcript links, per-agent status, and tmux actions in one native TUI. → [research](backlog/agent-session-dashboard-tui.md)
- [ ] **[P2/M] Extract AICAP Python dispatch into shared module** — **four** Python consumers now read the SSOT at `dot_config/shell/04_ai_agents.sh` and reimplement `AGENT_CONFIG` + `detect_agent` + `invoke_agent` + SHA cache: `dot_config/tmux/executable_tmux-session-summary.py`, `scripts/aiblock.py`, `dot_dotfiles/bin/executable_pqsum`, `scripts/fleet/exec.py`. **Priority bumped from `?` to `P2` on 2026-05-13** when `fleet exec` landed as the 4th consumer — adding a new agent now means updating four AGENT_CONFIG dicts. Promote to a shared `scripts/aisum/__init__.py` module (`invoke_agent`, `detect_agent`, `cache_lookup`, `cache_save`, `load_ssot`, `parse_strict_json`) and migrate all four. Next AI-tooling change should land this refactor first. **Pain re-confirmed 2026-05-20** when adding the Antigravity CLI (`agyc`) agent meant editing the same `AGENT_CONFIG` + model-label dict in all four files (plus the two shell SSOT consumers).
- [ ] **[?/S] Auto-archive `pqsum ai --report` to `~/notes/pueue-reports/`** — today the `--report` flag prints markdown to stdout or `--out PATH`. Optional rolling archive under `~/notes/pueue-reports/YYYY-MM-DD-HHMMSS.md` with retention policy (keep last N, prune older than M days). Requires user opt-in (don't write to `~/notes/` by default).
- [ ] **[?/S] `pqsum ai --restart-failed`** — companion to `--clean`: execute the LLM's suggested `recovery_command` for Failed tasks with same y/N safeguards. Higher risk than `--clean` (restarts are not idempotent), so separate flag + always-prompt by default.
- [x] **[?/M] `fleet exec` — cross-host argv-list command runner with AI summary** — **Done 2026-05-13**: shipped as `scripts/fleet/exec.py`. Argv-list default + `--shell bash|zsh` for pipes/globs + `--login` for rc-loaded env + `--no-augment-path` escape hatch + `--ai` succeeded/differed/failed classifier + `--report --out PATH` markdown + cache (TTL `FLEETEXEC_MIN_REFRESH_INTERVAL`, default 120s, excludes elapsed_ms from hash). Reused fleet's asyncssh+semaphore-8 + the PATH-augmentation lesson from `fleet pueue`. Copy-pasted AICAP from `executable_pqsum` (now 4th Python consumer of the SSOT). → [research](backlog/fleet-exec.md).
- [ ] **[M] Unify the three SSH-config parsers** — `dot_config/shell/96_ssh_setup.sh:_ssh_cfg_py` (surgical block editor), `dot_config/television/cable/ssh-config.toml`'s awk (display lister), and `dot_dotfiles/bin/executable_tsnet` (managed-block owner + include-reachability + parse-order shadow detection). Blocked on a deploy-path decision: the shared module belongs in `scripts/`, which is chezmoi-ignored, and `96_ssh_setup.sh` must keep working on a fresh box with no source tree. A cross-implementation agreement test in `tests/unit/tsnet_ssh_block.bats` holds the line meanwhile. → [research](backlog/ssh-config-parser-unification.md)
- [ ] **[?/M] `tsnet sync-fleet`** — reconcile tailnet devices into `~/.config/fleet/machines.toml`. Needs a spike on the population mismatch first (the tailnet has iOS handsets and other people's Macs; fleet's inventory is a curated subset), plus a `Host` dataclass field — `load_hosts()` silently drops unknown TOML keys. → [research](backlog/ssh-config-parser-unification.md)
- [ ] **[?/M] `tsnet serve` for non-HTTP targets** — v1 is HTTP-proxy only. `tailscale serve` also does TCP, TLS-terminated TCP, unix sockets and `--service`; add them when something actually needs one.
- [ ] **[S] CLAUDE.md headroom trim** — 32,161 chars vs its own ~30k rule (was already 1.6k over before the tsnet change added ~510). Fattest candidate is the yazi-preview-coverage row (~1,450 chars, nearly all duplicated in `docs/tools/yazi-previews.md`, which it already links). Deliberately kept as its own commit rather than smuggled into a feature change.
- [ ] **[L] secrets.zsh encryption with age** — currently plaintext + chezmoi-ignored. Age-encrypted version could live in the source tree. Needs key distribution plan across machines.
- [ ] **[M] aicapture: non-tmux output capture (Tier 2)** — Tier 1 (`aifix-stdin` / `aifix-run` / `aifix-rerun`) shipped; transparent preexec/precmd tee redirect would remove the "remember to prefix" friction for non-tmux daily users. Tier 3 (script(1) or PTY proxy) explicitly rejected. → [research](backlog/ai-capture-non-tmux-output.md)
- [ ] **[M] `llms.txt` drops every translated page** — `mkdocs-static-i18n` (`docs_structure: suffix`) replaces the default-locale `File` for any page with a `.zh-TW.md` twin, so `mkdocs-llmstxt` can't resolve its `sections:` URIs and skips them. `llms.txt` currently ships 1 of 12 curated pages. Was invisible until the `social` plugin stopped emitting 347 warnings. → [research](backlog/mkdocs-llmstxt-i18n-page-uri-mismatch.md)
- [ ] **[S] mkdocs anchor drift cleanup** — first deploy uses `validation.links.anchors: info` so ~20 stale section-anchor links in existing docs don't fail CI strict. Batch-fix them, then raise back to `warn` in `mkdocs.yml`. → [research](backlog/mkdocs-anchor-drift.md)
- [ ] **[S] devtools: gate `Install tmux plugins via TPM` on tmux being on PATH** — currently fatal on `centos_server` + `noRoot=true` boxes (and any RedHat-family box where the tmux yum branch hasn't run): `tmux source-file /home/devuser/.tmux.conf` exits with `tmux: command not found`. Surfaced by `just docker-test-rocky9`. The `dot_ansible/roles/devtools/tasks/main.yml:3371` task already registers `tmp_bin` from `~/.tmux/plugins/tpm/bin/install_plugins`; add a `when:` condition checking `command -v tmux` (mirror the `gh-dash` pattern already in this role). Once landed, flip `CHEZMOI_ALLOW_PARTIAL_FAILURE` back to `false` for the centos7-noroot / rocky9-noroot compose services.
- [ ] **[S] devtools role: broaden remaining user-level fallbacks to RedHat (residual)** — bulk of the gate-broadening landed 2026-05-13 (bat, yazi, btop, zellij, lnav, gh, glab, diffnav, superfile, dasel, yq, witr, git-graph, tldr, jnv, plus git-delta x86_64 switch from gnu→musl, plus iac_tools Terraform/OpenTofu/Azure CLI). **Residual tools NOT yet broadened** because they were missed by the audit's `# --- toolname ---` marker scan: `gum`, `vhs`, `freeze` (Charm tools — likely Go-static, simple sweep), `taplo` (Rust musl), `television` (Rust, has `pitfalls/tv-binary-glibc-2-39-not-found.md` so verify upstream musl asset first), `sesh` (Go-static, also has an aarch64 path to check), `eza` aarch64 (no upstream musl, Linuxbrew fallback only), `fastfetch` (Debian apt + GitHub `.deb` fallback — no Rust/Go static). Each is a separate read-and-edit pass; mechanically identical to the 2026-05-13 batch. → see git log for `centos7-noroot` / `centos7-neovim-apt-register-defined` commit for the pattern; new tasks should follow the same `# Gate broadened to RedHat per pitfalls/centos7-noroot.md — <tool> is <musl/Go-static>` comment template.
- [ ] **[S] ansible env-var lookup: handle `HTTPS_PROXY` (uppercase) hosts** — every `get_url:` / `uri:` block that wraps `lookup('env', 'https_proxy')` is **case-sensitive** and skips the proxy on hosts that only export uppercase. Bites silently on corporate IDC boxes (e.g. `idc-server104`) where IT scripts set `HTTPS_PROXY` but not `https_proxy`. Surfaced 2026-05-13 retrying neovim-releases download under proxy. Fix candidates: (A) `lookup('env', 'https_proxy', 'HTTPS_PROXY')` chain in every block, (B) controller-side normalize in `run_onchange_after_20_ansible_roles.sh.tmpl`, or (C) drop the explicit `environment:` block and let inherited proxy vars flow. → [research](backlog/ansible-env-proxy-var-case-sensitivity.md)
- [ ] **[S] `just ansible-tags` / `ansible-base` / etc.: auto-detect uv-managed Python on CentOS 7** — `chezmoi apply` already pins `ansible_python_interpreter` to a uv Py3.13 when `/usr/bin/python3 < 3.7` (via `.chezmoiscripts/global/run_onchange_after_20_ansible_roles.sh.tmpl`), but the just recipes call `ansible-playbook` bare and crash with `SyntaxError: future feature annotations is not defined` on EL7. Extract the existing `detect_target_python` + `find_uv_python` helpers into `scripts/lib/ansible_interpreter.sh` (matches the `scripts/lib/sudo_shared.sh` precedent), then source from each `ansible-*` recipe in the justfile. → [research](backlog/just-ansible-tags-python-interpreter.md). Sibling of (but distinct from) the existing P? L item below on `ANSIBLE_PYTHON_INTERPRETER` env-var pinning — that one targets the chezmoi run-script path and is mostly done.
- [ ] **[L] CentOS 7: pin ANSIBLE_PYTHON_INTERPRETER to uv-managed Python 3.13** — CentOS 7 ships Python 3.6.8 as `/usr/bin/python3`; ansible 2.18+ modules use `from __future__ import annotations` (Py3.7+) so every `gather_facts` call dies with `SyntaxError: future feature annotations is not defined`. The bootstrap already installs ansible-core under uv-managed Python 3.13; need to discover that path (e.g. `uv python find 3.13`) in `.chezmoiscripts/global/run_onchange_after_20_ansible_roles.sh.tmpl` and `export ANSIBLE_PYTHON_INTERPRETER=...` when system python3 is < 3.7. Until landed, `just docker-build-centos7-noroot` produces an image with all 9 ansible tags failed → only chezmoi/uv/mise/ansible/bootstrap-installed binaries present; smoke tests cannot run meaningfully. Tracked alongside the Linuxbrew/git<2.7 issue as the two CentOS-7-specific blockers. **2026-05-13 partial**: chezmoi run-script path lands the auto-detect + `--extra-vars` injection (verified live on `idc-server104`); `just ansible-tags` recipe still needs the same — see new `[S]` entry above.
- [ ] **[S] base: ripgrep aarch64 fallback to gnu when musl 404s** — ripgrep 15.1.0+ stopped publishing `aarch64-unknown-linux-musl.tar.gz`; the `aarch64-unknown-linux-gnu.tar.gz` is still there. Surfaced by `just docker-test-rocky9` on Apple Silicon. Currently handled by `ignore_errors: true` so non-fatal but `command -v rg` returns nothing on arm64 RedHat. Either pin to a version that still has musl, or detect the 404 and retry with the gnu tarball.
- [ ] **[M] bootstrap: skip Linuxbrew when git < 2.7.0 (CentOS 7 sudo path)** — `run_once_before_00_bootstrap.sh.tmpl:175` runs the Homebrew installer which hard-fails with `The version of Git that was found does not satisfy requirements for Homebrew. Please install Git 2.7.0 or newer.` on CentOS 7 (ships git 1.8.x). Surfaced by `just docker-build-centos7` (sudo flavor, currently broken). Either parse `git --version` and skip Linuxbrew with a warn, or `yum install -y rh-git227` from SCL before calling `brew`. Once landed, the `centos7` (sudo) compose service is testable; today only `centos7-noroot` works.
- [ ] **[S] Dockerfile + dotfiles_init.py doctor: verify --prompt* flag text matches template** — current doctor only checks ARG names, missing the prompt-text-vs-key-name confusion that cost a debugging round when adding the centos7/rocky9 images. Add a regex extract of `--prompt(String|Bool|Choice)\s+"([^=]+)=` from the Dockerfile's `chezmoi init` block and assert it matches the corresponding template `promptXOnce` text. → [research](pitfalls/chezmoi-init-prompt-flag-mismatch.md)
- [ ] **[S] docker test: revisit chezmoi source-path strategy** — fixed today by symlinking `~/.local/share/chezmoi` → `/tmp/dotfiles-source` after `chezmoi init` in all three Dockerfiles. The symlink is the minimum-invasive fix (matches what `chezmoi init https://...` would create when cloning a real repo). Open question for later: should `tests/smoke/docker_install.bats` use `chezmoi apply --source="$SOURCE_DIR"` explicitly instead, so the test works against any container layout including ones without the symlink? Decide once we add a second smoke test target (e.g. devbox vs prod-like). The current test was always going to fail without `~/.local/share/chezmoi` populated; the earlier "5/8 passing" reading on `dotfiles:test-rocky9` was a buildkit cache fluke.
- [x] **[M] Gate mise runtimes (rust/dotnet/bun/ruby) behind prompts + fix .NET-SDK contract bug** — **Done 2026-06-10**: new `installExtraRuntimes` prompt (default true) gates rust/bun/ruby in `dot_config/mise/config.toml.tmpl` AND drops `rust_cargo_tools`/`ruby_gem_tools` from the ansible TAGS when false; `dotnet` now honours `installDotnetTools` (the contract bug — SDK no longer lands on a No answer). Docker ARG default flipped to false (leaner CI images). Option "A + C" from the research note. → [research](backlog/mise-runtime-gating.md)

## P3 — Someday / nice to have

- [ ] **[S] yazi Markdown preview: 256-colour glow** — the `*.md` previewer forces colour with `CLICOLOR_FORCE=1`, which termenv caps at ANSI-16 because piper hands glow a pipe rather than a pty. Native 256-colour needs a pty allocator (`script(1)` syntax diverges BSD vs util-linux) or a new in-house launcher. Cosmetic only; 16-colour tracks the terminal theme. → [research](backlog/yazi-markdown-preview-truecolor.md)
- [ ] **[S] `wake --wait` + fleet MAC integration** — `wake` currently fires and forgets; add `--wait` to poll ping/ssh until the woken host answers (so `wake box && ssh box` works), and consider `fleet wake <host>` reading a MAC field. Fleet coupling touches its sync invariants — evaluate first. → [research](backlog/wake-on-lan.md)
- [ ] **[S] neovim role: refresh `nvim_current_version` after sudo apt install** — current behaviour: `nvim_current_version` fact is set ONCE in `dot_ansible/roles/neovim/tasks/main.yml:11` BEFORE the sudo apt branch runs. If apt successfully installs an old nvim (e.g. Debian 12 ships nvim 0.7.x, < the `0.11.2` minimum), the user-level fallback's gate `nvim_current_version is version(neovim_min_version, '<')` still evaluates against the original `0.0.0` reading and fires correctly — but the SAME tarball gets extracted on top, wasting bandwidth. Snap branch handles this via a separate `nvim_apt_version` re-check; user-level branch doesn't. Low impact (idempotent overwrite); fix by mirroring the snap-branch's `Check Neovim version after apt install` pattern into the user-level gate. Surfaced 2026-05-13 while fixing the `apt_neovim is not defined` bug.
- [ ] **[S] TA-Lib path management** — currently in `secrets.zsh` as machine-specific. Could move to a `99_local_*.zsh` create-only stub like the proxy pattern in `dot_config/zsh/99_local_proxy.zsh`.
- [ ] **[S] Try / Toolkami custom tool aliases** — currently in `secrets.zsh`. Same `99_local_*.zsh` pattern as TA-Lib above; group together.
- [ ] **[M] Super Productivity Linux install** — currently macOS-only via `dot_config/homebrew/Brewfile.darwin.tmpl:83` cask. For fleet parity (`fleet-apply` against Ubuntu desktops) need either AppImage in a new ansible task or snap. Defer until first Linux desktop host actually wants it. The `tv super-productivity` channel + REST API integration ([docs/tools/super-productivity.md](docs/tools/super-productivity.md)) are cross-platform; only the desktop install path is missing.
- [ ] **[?/L] `spctl` + `super-productivity-mcp`** — wrap SP's local REST API ([docs/tools/super-productivity.md](docs/tools/super-productivity.md)) as a CLI for shell automation and an MCP server for coding agents. The TV channel (v0.1) intentionally exercises every read route first to validate behaviour. Phase 2 (mutations: start/stop/complete/archive) lands as Alt-key bindings on the same channel before extracting into a separate package. Open question: TypeScript monorepo vs. plain Python `uv` script — the latter matches the rest of this repo's `scripts/` style.
- [ ] **[M] `js_cli_tools` / `coding_agents` ansible: gate on minimum Node version** — both roles call `npm install --global` against system npm without checking the version. On Ubuntu 22.04 jammy hosts (e.g. `ts_nas`) the apt-installed `/usr/bin/npm` is v8.5.1 / Node v12.22.9, which fails `EBADENGINE` on modern packages (`@openai/codex` requires Node ≥18) and `EACCES` on `/usr/local/lib/node_modules` for non-root users. Fix path A in [pitfalls/ansible-js-cli-tools-old-system-node.md](pitfalls/ansible-js-cli-tools-old-system-node.md) covers the role-side patch (`is version('v18.0.0', '<')` check that forces mise fallback).
- [ ] **[M] fleet-apply long-run sudo session expiry** — `jingle207` ran for 7m50s under `just fleet-apply` and failed at task `Ensure xz is available for worktrunk extraction` with `sudo: a password is required` even though the run started with `CHEZMOI_SUDO_PASSWORD_FILE` populated. Either (a) the sudo password file gets cleaned up by the wrapper's `_cleanup` trap before late tasks need it, or (b) Linux sudo `timestamp_timeout` (default 5m for many distros, 15m for some) expires mid-run and `sudo -S` doesn't re-prompt automatically. Investigation needed: instrument `scripts/lib/sudo_shared.sh` to refresh the cache periodically, or have `sudo_session_init` re-validate via `sudo -S -v -p ''` before each privileged task. See AGENTS.md → "Sudo session is shared across all run-scripts" for the existing contract.
- [ ] **[S] fleet-apply `--serial` mode misses drift downgrade** — `_classify_drift()` in `scripts/fleet/apply.py` correctly downgrades `chezmoi: <path>: could not open a new TTY: open /dev/tty:` to drift in parallel mode, but `just fleet-apply-one HOST` (which uses `--serial`) reports `rc=1` for the same condition instead of `drift`. Verified on `hanru_mac` 2026-04-24: only diff was `~/.config/worktrunk` directory mode (40700 → 40755), and serial-mode reported failed; identical condition under parallel was correctly classified. Likely a code path that bypasses `_classify_drift` when `--serial` is passed.
- [ ] **[M] CentOS / RedHat: broaden user-level fallback gates in remaining roles** — the `centos_server` profile (added 2026-05-06) only ports the foundational subset of roles to `os_family in ["Debian", "RedHat"]`: `base`, `zsh`, `starship`, `ruby_gem_tools`, `security_tools`, `rust_cargo_tools`. Still gated `Debian`-only on user-level paths (so they no-op on CentOS even without sudo): `devtools` (~20 hits), `neovim` (8 hits), `lazyvim_deps` (~19 hits), `docker` (~10 hits). The broadening pattern is mechanical (`pitfalls/centos7-noroot.md` documents it); the cost is the audit + re-test. Pick up when a second CentOS user appears or when `idc-server104` reports concrete missing-tool symptoms.
- [ ] **[M] CentOS / RedHat: full `yum:` / EPEL bootstrap for sudo-able RedHat hosts** — with the `centos_server` profile, parallel `RedHat` blocks were added only for trivial cases (zsh, ruby_gem_tools libffi-devel/libyaml-devel, base packages). For users with sudo on a RedHat-family box, a comprehensive port would need: `epel-release` enable as a pre-task, parallel yum blocks for `devtools` (~30 packages), `neovim` (EPEL has 0.9 — old; consider AppImage path), `lazygit` (no standard RedHat repo — use GitHub release path), and possibly `vault.centos.org` mirror configuration if running on EOL CentOS 7. Keep deferred until a sudo-capable RedHat user actually requests it. Design: piggyback on `useChineseMirror` for `mirrors.tuna.tsinghua.edu.cn` if any user reports Great-Firewall-side EPEL / vault access issues.
- [ ] **[S] Auto-detect `centos_server` default in `.chezmoi.toml.tmpl`** — currently `$defaultProfile` defaults to `ubuntu_server` on every non-darwin OS, including CentOS. Could use `.chezmoi.osRelease.id` (or `.idLike`) to default to `centos_server` when chezmoi sees `id=centos|rhel|rocky|alma`. Mostly cosmetic — `dotfiles_init` prompts anyway — but worth a one-liner if/when adjacent prompt logic is touched.
- [ ] **[S] Wire `upgrade-skills` into `scripts/upgrade_tools.sh` as a proper category** — recipe shipped 2026-04 as a standalone `just upgrade-skills` that calls `npx skills@latest update --global -y` directly. Promote to a real category so it gets picked up by `upgrade-all` (and `upgrade-dry-run`) and appears in the per-category matrix at [`docs/this_repo/upgrades.md`](docs/this_repo/upgrades.md). Mechanical: add `skills` to the script's case statement, append to the `upgrade-all` comment in `justfile`, add a row to upgrades.md. No semantic change — `chezmoi apply` is still install-only for skills (the restore loop in `.chezmoiscripts/global/run_onchange_after_40_install_global_skills.sh.tmpl` only fires when the merger's managed-set changes, which is the right behaviour).
- [ ] **[L] Split `devtools` mega-role into focused per-tool roles** — [`dot_ansible/roles/devtools/tasks/main.yml`](dot_ansible/roles/devtools/tasks/main.yml) is a 166-task grab-bag installing ~50 unrelated tools (gh, glab, bat, btop, fastfetch, jnv, lnav, pandoc, rclone, sesh, tailspin, taplo, television, tldr, tmux, vhs, yazi, yq, zellij, zoxide, …). Sibling tools that get standalone roles (atuin, starship, neovim, docker, bitwarden) prove the standalone-role pattern is fine. Cost of the current shape: long apply times even when one tool changes, hard to skip individual tools per-host (e.g. headless server doesn't need yazi), and an oversized blast radius for any single edit. Proposed split into ~6 cohesive roles with a meta-role keeping playbook entries unchanged. → [research](backlog/devtools-role-split.md)
- [ ] **[S] macOS: add Stats.app cask to `Brewfile.darwin.tmpl`** — [exelban/stats](https://github.com/exelban/stats), MIT-licensed native Swift menubar widget for memory pressure / swap / CPU / network. Documented in [docs/tools/macos-swap.md → Prevention & monitoring](docs/tools/macos-swap.md#prevention--monitoring) as the recommended free alternative to iStat Menus. Currently NOT installed by default — user evaluation needed for whether it crowds the menubar (Codexbar / Codex history / wuhrai / etc. already there). Add as `cask "stats"` once user opts in. Decision criteria: at least one menubar slot freed up, or user explicitly says "I want a swap meter visible at all times". One-line Brewfile change.
- [ ] **[?/M] `tv mac-storage` channel** — `tv mac-procs` (shipped 2026-05) covers the per-process angle. A complementary `tv mac-storage` would visualise *what's eating my disk* in general: parsed `du -shx /*` + `system_profiler SPStorageDataType` + `tmutil listlocalsnapshots /` + `diskutil apfs listSnapshots /` rows in one fuzzy picker. Preview: `du -h --max-depth=1 <selected>` for dirs, `ls -lh <file>` for files. Actions: open in Finder, `rm -i`, copy path. Useful when "System Data" balloons but the cause isn't swap (e.g. Xcode DerivedData, Docker volumes, brew cleanup needed, `/Library/Caches`). Defer until a second "where did my disk go" investigation actually happens — the existing `tv files-ls`/`du-here` aliases may be enough.
- [ ] **[?/L] Scheduled `audit-monitor` with apprise routing** — turn the ad-hoc `health-check` / `audit-failed-logins` / `audit-summary` helpers into a real monitoring story: scheduled (pueue or systemd timer), diff-aware (only alert on state change), routed via [apprise](https://github.com/caronc/apprise) (one CLI, ~80 notification backends incl. Slack/Discord/Telegram/ntfy/email). Hard part is secret management (apprise URLs contain auth tokens — must NOT sync via dotfiles); three patterns documented (Bitwarden CLI fetch / per-host 0600 env / systemd `LoadCredentialEncrypted=`). → [research](backlog/audit-monitor-with-apprise.md). Promote when at least 2 of these become true: (a) user runs a check 3+ days in a row manually, (b) user has an incident they wish they'd caught, (c) user already has a target apprise channel provisioned.
- [ ] **[S] shellcheck: tighten `dot_config/{shell,bash}/` from severity=error → warning** — added a second shellcheck pre-commit hook (alias `shellcheck-shared`) 2026-05 covering the shared shell modules, but kept it at `--severity=error` to avoid blocking on ~41 preexisting warnings. Three buckets: ~30 mechanical false positives (mostly SC2207 in bash completion files where `COMPREPLY=( $(compgen ...) )` is the canonical idiom — add `# shellcheck disable=SC2207` directive at top of each), ~8 real subtle bugs (SC2164 `cd-without-fallback` in 3 files, SC2155 `declare-and-assign` in 2 files, SC2088 tilde-in-quotes in `99_chezmoi_reload.sh:56`), and ~3 stylistic. Once `just lint-shell` is clean, flip the hook's `--severity` arg in `.pre-commit-config.yaml` and remove the "errors only" alias caveat from [`docs/this_repo/testing.md`](docs/this_repo/testing.md). → [research](backlog/shellcheck-broaden-shared-scope.md).
- [ ] **[M] TUI E2E testing harness — Textual Pilot for `mlf` (+ pexpect/pyte for binaries)** — 2026-07 evaluated `shell-use` (Microsoft's "Playwright for the terminal") and **declined it for managed config** (pre-1.0 `v0.0.1-beta.3`, self-declared WIP with unstable CLI/install, background daemon, brew-tap/asset-pinning trap) — and it's mismatched anyway: the repo has exactly **one** full-screen TUI, `mlf` (Python Textual, `scripts/mlf/tui.py`), which Textual's own harness tests better. Direction decided, execution deferred. Slice 1 (M): wire greenfield pytest + `textual[dev]`/`pytest-textual-snapshot` dev-deps and add `Pilot` tests for `mlf` (key bindings, modals, `snap_compare()` baselines; import `scripts.mlf.*` from chezmoi source dir). Slice 2 (→L): thin `pexpect+pyte` driver for arbitrary 3rd-party TUI binaries (model the tmux-driver ergonomics already in `agent-warmup`). Watch-list: `tuiwright` / `pytest-tuitest` (both v0.1). Revisit shell-use only after a stable 1.0. → [research](backlog/tui-testing-harness.md)
- [ ] **[?/M] `linux-mem-status` / `linux-mem-reclaim` helpers** — mirror the macOS `mac-mem-*` ergonomics on the Linux side. Linux actually has *more* targeted reclaim primitives than macOS (`swapoff -a && swapon -a`, `journalctl --vacuum-*`, `systemd-tmpfiles --clean`, `docker system prune`, `echo 2 > /proc/sys/vm/drop_caches`), so `linux-mem-reclaim --include LIST` would have richer non-reboot options than its macOS sibling. `linux-mem-status` would surface `free -h` + `vmstat` + `journalctl --disk-usage` + `df -h` + top RSS in one screen. Source file: `dot_config/shell/56_linux_mem.sh.tmpl` gated on `eq .chezmoi.os "linux"`. Trigger: when the first IDC server in the fleet (e.g. `idc-server104`) shows memory bloat that this would have helped diagnose. Cross-link from [docs/tools/macos-swap.md → Practical implication for this repo's fleet](docs/tools/macos-swap.md). Doc page should explain the fundamentally different reclaim model (Linux: per-resource targeted tools; macOS: reboot or wait).
## P? — Needs evaluation before committing

- [ ] **[?/M] Fold `summarize` into the AICAP dispatch chain** — `summarize` (shipped 2026-08-31, `installSummarize`; see [docs/tools/summarize.md](docs/tools/summarize.md)) does its own provider fallback and can drive an already-authenticated coding CLI via `--cli claude|codex|gemini|pi`. That overlaps with the hand-rolled dispatch in `dot_config/shell/04_ai_agents.sh` that `tsum` / `aiblock` / `pqsum` / `fleet exec --ai` each re-parse. Question is whether `tsum` should shell out to `summarize --cli` instead of owning a second fallback ladder — which would also give it JSON output for free. Pairs with the queued `scripts/aisum/` extraction. Spike on `tsum` first; do not touch the other three until it proves out.
- [ ] **[?/L] copilot-proxy global apply mode for Claude Code and Codex** — Design an explicit reversible user-wide switch only after the proxy has a real supervisor; require atomic snapshots, ownership markers, drift detection, exact restore, and Unix/Windows parity. → [research](backlog/copilot-proxy-global-apply-mode.md)
- [ ] **[?/L] yth semantic subtitle search** — `yth` (shipped 2026-07; `dot_dotfiles/bin/executable_yth` + `scripts/yth/`, picker twin `tv yth`) does keyword FTS5 over title/channel/description + fetched captions. Semantic/embedding search over transcripts was deferred at plan time: provider undecided (Voyage / OpenAI / local `fastembed`), vector store (`sqlite-vec` vs `lancedb`), chunking + hybrid bm25 merge. Also parked in the same note: Takeout `.html` parser, materialized watch-stats, a Textual `yth tui`. → [research](backlog/yth-semantic-search.md)
- [ ] **[?/M] copilot-proxy: replace `nohup` start with a real supervisor** — `copilot-proxy start` (`dot_config/shell/43_copilot_proxy.sh:146`) launches the fork via `nohup bunx … &` + a `$TMPDIR` pidfile/logfile. Gaps: no crash auto-restart, no start-at-login, log lives in ephemeral `$TMPDIR` (wiped on reboot, differs under launchd), no rotation. Reuse `agent-warmup`'s runtime `install` pattern (`dot_dotfiles/bin/executable_agent-warmup` `_install_launchd`/`_install_systemd` + `_PATH_PRELUDE` for the mise/`bunx` PATH), but adapt from a one-shot **timer** → a persistent **daemon** (`KeepAlive`+`RunAtLoad` on macOS launchd; `Restart=always`+`WantedBy=default.target` on systemd --user). `pueued` is already launchd-supervised here (`homebrew.mxcl.pueue.plist`) so a `pueue add` variant is near-free but isn't a real supervisor. **⚠ Spike the ROI first**: the API errors that triggered this are an upstream 60 s timeout, NOT proxy crashes — a supervisor buys login-start + log hygiene, not timeout resilience. → [research](backlog/copilot-proxy-supervisor.md)
- [ ] **[?/S] Antigravity CLI `agy` name-collision — revisit `agyc` workaround** — Google ships TWO tools named `agy`: the Antigravity **IDE** symlinks `agy`/`antigravity` → a VS Code-fork editor launcher (no agent), and the standalone **Antigravity CLI** (the agentic `agy -p`). On this machine `~/.zshrc.adhoc:20` prepends the IDE bin to PATH (pos 1), shadowing the CLI at `~/.local/bin` (pos 15). Shipped 2026-05-20: AICAP keys on a collision-free `agyc` symlink (`~/.local/bin/agyc → agy`, created by the `coding_agents` ansible role) + `antigravity-cli`/`agy-ide` convenience aliases. This is a deliberately temporary handle — keep tracking whether (a) Google disambiguates the binary names upstream, (b) the CLI gets a `--model` flag (today model is account/config-side, so there's no `AICAP_AGYC_MODEL`), or (c) auth can be shared with the IDE keyring to avoid a second interactive Google login. Revisit the naming + drop the symlink if a cleaner upstream story emerges.
- [ ] **[?/M] Linux Desktop app control equivalent of `54_macos_apps.sh`** — mirror the macOS `appquit` / `applaunch` / `appactivate` / `apprestart` / `applist` / `appresponsive` helpers + `tv mac-apps` channel on `ubuntu_desktop` hosts. No universal Linux analog exists: macOS osascript/Apple Events fragment across (display server: X11/Wayland) × (compositor: GNOME Shell / KWin / Sway / Hyprland) × (per-app D-Bus opt-in). The pragmatic first cut is **option A — `gapplication`-only subset** (works on both X11 + Wayland, covers GNOME-native apps; misses third-party that don't set `DBusActivatable=true`). Compositor-specific fallbacks (sway/hyprctl/KDE qdbus, GNOME `window-calls` extension) only if a real second-compositor host joins the fleet. Skipped today — needs hands-on Ubuntu Desktop experimentation to measure coverage on a real app set before committing. → [research](backlog/linux-desktop-app-control.md)
- [ ] **[P?/S] Consolidate Linux tiling-WM choice — niri vs i3 vs GNOME** — `installNiri` (2026-06; [niri-wm/niri](https://github.com/niri-wm/niri) built from source via the `niri` ansible role, see [docs/playbooks/niri.md](docs/playbooks/niri.md)) added niri as a GDM session alongside the pre-existing **i3 (X11)** + **GNOME**. They **coexist fine today** — each is a separate display-manager session with no runtime conflict; choosing one is purely a login-time pick (GDM gear menu). Open question is whether to eventually daily-drive *one* and retire the others. If niri wins: port i3 keybinds into `dot_config/niri/config.kdl` for muscle-memory parity, and pick a Wayland bar/launcher (waybar + fuzzel) to replace i3bar/rofi. No urgency — decide after living in niri for a while. Trade-off: niri is Wayland-native (better NVIDIA-on-580 + HiDPI) but scrollable-tiling is a different mental model from i3's manual tiling, and X11-only tools may still want the i3/Xorg fallback. Keep all three until the call is made.
- [ ] **[?/M] tmux window status indicators — option C (hook-based per-pane state)** — option A (built-in `monitor-activity`/`bell` flags) shipped 2026-04 in `dot_config/tmux/common.conf`. Option C (semantic running/idle/error glyphs driven by Claude hooks + OpenCode plugin writing per-pane state files) is fully scoped but deferred until option A's `#` flag proves insufficient or another feature also needs the per-pane state file. Claude side is additive to `dot_claude/hooks/executable_notify.sh.tmpl` (low risk); OpenCode side is a new JS plugin (new surface, more risk). → [research](backlog/tmux-window-status-indicators.md)
- [ ] **[?/S] specstory: enable opencode auto-wrap when upstream lands** — `_sesh_wrap_agent` in `dot_config/zsh/tools/22_sesh.zsh` currently passes `opencode` through raw because specstory doesn't support it as a provider. Add to the case statement (one-line change) once `specstoryai/getspecstory#156` merges + ships. → [research](backlog/specstory-opencode-support.md)
- [ ] **[?/S] Audit `~/.config/zsh/tools/` for other pre-chezmoi orphans** — `02_legacy.zsh` was caught and removed manually on `Da-Weis-Mac-mini` 2026-04-27 (see [pitfall](pitfalls/zsh-nvm-lazy-loader-orphan.md)). Likely siblings exist on other hosts (`27_thefuck.zsh`, etc. — `chezmoi managed --include=files | grep tools/` does NOT account for every `tools/*.zsh` file we observe on disk). A content-aware audit script (diff `ls` against managed list, verify each unmanaged file matches a known stale signature, then prompt-and-rm) would handle this without `.chezmoiremove`'s blast radius. Skip until a second host shows the symptom — one-off `rm` per host is fine for now.
- [ ] **[?/S] Switch macOS tmux from 3.6a bottle to `--HEAD`, then revert when 3.7 ships** — homebrew `tmux 3.6a` ships a copy-mode double-free that fires `[server exited unexpectedly]` after long-running sessions (slice UUID `1b2d8e7b-bb77-3465-8bab-73f5bbaffe49`). Fixed upstream in [`035a2f3`](https://github.com/tmux/tmux/commit/035a2f35d40628dcfe235179220fc0ede848a195) closing [tmux/tmux#4777](https://github.com/tmux/tmux/issues/4777) (2026-01-06) but not yet in any tagged release. Action **A** = `tmux kill-server && brew install --HEAD tmux` (interrupts every running session — defer until quiescent). Action **B** = once 3.7 (or 3.6b) hits homebrew-core, `tmux kill-server && brew uninstall tmux && brew install tmux` to return to the bottle. tmux is **not** in `Brewfile.darwin.tmpl` so this is host-local; no fleet broadcast. Linux side (Linuxbrew + tmux-appimage in `dot_ansible/roles/devtools/tasks/main.yml`) is unaffected — those install paths target the popup-clamp invariant (tmux ≥ 3.3), not this bug. → [pitfall](pitfalls/tmux-copy-mode-double-free-3.6a.md)
- [ ] **[?/S] Replace global-skills restore loop with `npx skills experimental_install -g -y`** — current `.chezmoiscripts/global/run_onchange_after_40_install_global_skills.sh.tmpl` hand-rolls restore by looping `npx skills add <source> -s <name> -g -y` for each entry in `~/.agents/.skill-lock.json` whose `~/.agents/skills/<name>/SKILL.md` is missing. Reason: upstream `experimental_install` only reads project lock (`src/install.ts:19` — no `-g`/`--global` branch), and `update --global` is refresh-in-place (skips entries where the on-disk skill is gone — `src/cli.ts:619`). [PR #743 "feat: add global restore/relink from .skill-lock.json"](https://github.com/vercel-labs/skills/pull/743) proposes exactly this; status as of 2026-04-27: open, `mergeable_state=dirty`, no progress since 2026-04-10. When it merges, collapse the restore script's loop body to a single `npx skills experimental_install -g -y` call (the merger script in `dot_agents/modify_dot_skill-lock.json.tmpl` stays — still need it to enforce the managed-set into the lock). Watch [#283](https://github.com/vercel-labs/skills/issues/283) and [#549](https://github.com/vercel-labs/skills/issues/549) for movement.
- [ ] **[?/M] tv agent-sessions: Cursor Agent (`[cu]`) + Cursor IDE (`[ci]`) full transcript rendering** — the first round of richer preview (2026-04; see Done) shipped Claude / Codex / OpenCode full transcripts only. Cursor-side is deferred: `[cu]` needs blob decode + role guessing (fragile), `[ci]` needs a two-table `composerData` + `bubbleId:*` join (straightforward but ~50 LOC). Both keep the legacy "first 5 user messages" preview for now. Promote when the user actually browses Cursor sessions often. → [research](backlog/tv-agent-sessions-richer-preview.md)
- [ ] **[?/L] Use mise to manage most runtime versions** — already used for Node (Neovim) and Rust (Cargo) per `README.md`. Question: extend to python/go/ruby and retire ad-hoc PATH wiring + nvm? Trade-off: another layer vs unified version pinning. Spike: pick one runtime currently outside mise (go?), try alongside current setup for two weeks.
- [ ] **[?/L] Optimize zsh & tmux startup time** — needs profiling first (`zprof`, `time zsh -i -c exit`). Current state: conda/nvm are lazy-loaded already, so low-hanging fruit may be gone. Don't optimize blind.
- [ ] **[?/M] User-level `zsh` install for noRoot RedHat-family boxes** — `pitfalls/centos7-noroot.md` currently hard-requires `command -v zsh` as a precondition (admin must `yum install zsh` first); without it the dotfiles deploy a `~/.zshrc` pointing at a binary the user can't run, and `just docker-test-rocky9` smoke tests #2/#3/#6/#8 all fail on this single missing binary. Three install paths surveyed (Miniforge+conda-forge primary on glibc 2.17, mise asdf-zsh fallback when ncurses-devel exists, manual ncurses+zsh source build last resort) plus the `chsh`-blocked-on-noRoot bash-profile auto-exec workaround with four-guard structure. Decision deferred until we know whether the target box is a daily-driver (worth the ~400 MB Miniforge cost) vs occasional-job-runner (bash + starship is fine and cheaper). → [research](backlog/zsh-noroot-fallback.md)
- [ ] **[?/XL] CUDA / ML toolchain ansible role** — driver/cudnn/nccl version matrix is large; only relevant on Linux ML hosts. Defer until there's a concrete host that needs it. Likely belongs behind a new `ml_linux` profile. **2026-06-10 partial decision**: for Azure dev VMs the GPU story is handled WITHOUT a role — `just az-dev-vm --gpu` (scripts/azure/dev_vm.py) provisions an NC-series SKU + Microsoft's `NvidiaGpuDriverLinux` VM extension for the driver, and CUDA userspace stays project-level (conda / uv pytorch-cuda wheels). Only revisit a full role if a long-lived bare-metal ML host joins the fleet.
- [ ] **[?/XL] SLURM client config** — same shape as CUDA above: host-specific, likely a `dot_slurm/` template gated by profile. No machine currently needs it.
- [ ] **[?/XL] Raycast config sync for non-Pro users** — git-tracked, redacted cross-machine sync for hotkeys / quicklinks / snippets / aliases / extensions. Full scaffold was built (key-name redactor, extensions-manifest walker, secrets.env.example emitter, chezmoi `syncRaycast` prompt with 3-file parity, `just raycast-*` recipes) but paused: Raycast's `.rayconfig` export is AES-encrypted with a non-public KDF that mixes in keychain-bound material — we brute-forced 150+ scheme combinations (PBKDF2/scrypt/SHA*/EVP_BytesToKey) against password `12345678` with zero plausible decodes, and Raycast forces a non-empty password field so even the empty-password bypass is closed. Cheapest revival spike: inspect the separate `Export Quicklinks` / `Export Snippets` commands for a plaintext format. Adjacent pitfall (`raycast-library-files-not-portable-across-machines.md`) stays live. → [research](backlog/raycast-sync-non-pro.md)
- [ ] **[?/M] Evaluate Vorssaint on Apple Silicon Macs** — keep Raycast as the only launcher and Scroll Reverser on Intel; pilot complementary features on an official arm64 build only after upstream exposes a stable declarative/XDG config or headless import path, because the current UserDefaults + GUI onboarding flow cannot provide zero-touch setup. → [research](backlog/vorssaint-evaluation.md)
- [ ] **[P3/L] Port `aisuggest` ZLE widget to ble.sh `ble-bind -x` form** — bash users currently get the CLI form (`aisuggest "<text>"`) only. The shell-agnostic backend (_aiagent_invoke / _aisuggest_query / _aisuggest_sanitize) lives in `dot_config/zsh/tools/05_aisuggest.zsh`; widget porting needs to extract the backend into `dot_config/shell/04_ai_agent.sh` and rewrite the `_aisuggest_widget()` ZLE-driven UI as ble.sh's `ble-bind -m vi_imap -x` + `_ble_edit_str` manipulation. Pre-req: ble.sh widget API study, sanitize-pipeline regression test.
- [ ] **[P3/M] Port tools_picker / television / sesh ZLE widgets to ble.sh `ble-bind`** — each ~100-200 LOC of `LBUFFER` manipulation. ble.sh's `_ble_edit_str` + `ble/widget/insert-string` provide the equivalent surface. Files: `dot_config/zsh/tools/{11_tools_picker,12_television,22_sesh}.zsh`. Bash users currently get plain CLI commands (`tv <channel>`, `sesh-connect`).
- [ ] **[P3/S] Port `02_shell_integration.zsh` OSC 133 markers to bash** — uses `add-zsh-hook` for prompt/preexec markers. Bash needs `PROMPT_COMMAND` for prompt and ble.sh's `preexec_functions` (or bash-preexec) for command. Currently bash users miss tmux output navigation by prompt boundary.
- [ ] **[P3/L] Add `fish` as a third primaryShell choice** — would need `dot_config/fish/conf.d/` (translated from `dot_config/shell/`; fish syntax differs — `set -gx` vs `export`, `function name; ...; end` vs `name() { ...; }`, `alias v 'nvim'` vs `alias v=nvim`), a fish ansible role (chsh logic mirrors bash role), oh-my-fish or fundle plugin manager. Tools that already have fish support: starship/zoxide/mise/direnv/atuin (all `init fish`). vi-mode is built-in (`fish_vi_key_bindings`); autosuggest + syntax-highlight are built-in (no ble.sh-equivalent needed). Estimated ~400 LOC translation, one focused afternoon. **2026-05 explicitly evaluated and declined** — feature gap fish would close (~5%, mostly `abbr` + cleaner script syntax) is smaller than feature regression (loses ZLE widgets for sesh/tools_picker/television/aisuggest, breaks shared POSIX tier). Revisit only if shell-script friction crosses ~30% of writing time, or pick `zsh-abbr` plugin first if `abbr` is the only draw. → [research](backlog/fish-shell-evaluation.md). See also [docs/shells/architecture.md → fish 3.x](docs/shells/architecture.md#fish-3x) for the original scoping.
- [ ] **[P?/L] Add `powershell` as a fourth primaryShell choice** — pwsh 7+ runs cross-platform (mac/linux/windows). Profile lives at `~/.config/powershell/Microsoft.PowerShell_profile.ps1` on linux/mac; env is `$env:VAR = "value"`; aliases are `Set-Alias` (no shell-function-style; multi-step needs functions). starship / zoxide / mise / atuin all have pwsh support; PSReadLine provides autosuggest + syntax highlight + vi-mode (the equivalent of ble.sh). Shared POSIX layer cannot be reused at all — pipelines are object-streams. Estimated ~1-2 days for parity. Defer until a concrete user need surfaces. See [docs/shells/architecture.md → PowerShell Core 7+](docs/shells/architecture.md#powershell-core-7-pwsh) for the scoping.
- [ ] **[P3/S] Prune `.chezmoiremove` shell-migration entries after ~6-12 months on the fleet** — the 8 entries added by commit 9bb56c2 (zsh files moved to `dot_config/shell/`) are dead weight on hosts that have already applied. Cost is microseconds per apply (a `stat()` per entry). Don't prune until the maintainer is confident every host on the fleet has applied at least once since 2026-05; no rush.
- [ ] **[P?/S] Pre-commit hook to enforce shell-tier placement** — fail CI when a new `dot_config/zsh/` file lands without a matching shared/bash counterpart (or vice versa), enforcing the "both shells stay viable" invariant programmatically. Mostly relevant if the codebase grows more shell helpers; currently the manual review catches it.
- [ ] **[P?/S] chezmoi diff pager: TTY-aware delta fallback** — current `[diff].pager` in `.chezmoi.toml.tmpl:102-107` runs delta unconditionally with `--side-by-side`, which mangles output for agents / `\| head` / redirects (column wrap + ANSI residue). Wrapper script + `[ -t 1 ]` check + `cat` fallback designed but not shipped this round. Ship when a concrete agent misread happens or when adjacent tools (`git diff` / lazygit / yazi) want the same TTY-detection pattern. → [research](backlog/chezmoi-diff-pager-agent.md)
- [ ] **[P3/S] Migrate VisiData rc to XDG path** — Move `dot_visidatarc.tmpl` → `dot_config/visidata/config.py.tmpl` so the config lives under `~/.config/visidata/config.py` instead of `~/.visidatarc`. VisiData v2.9+ supports the XDG location natively on Linux (`visidata/settings.py:_get_config_file`), but `appdirs.user_config_dir('visidata')` on Darwin returns `~/Library/Preferences/visidata` (NOT `~/.config/visidata`), so the migration needs `export VD_DIR="${XDG_CONFIG_HOME:-$HOME/.config}/visidata"` + `export VD_CONFIG="$VD_DIR/config.py"` in `dot_config/shell/00_exports.sh.tmpl` to make macOS pick up the same path. `VD_CONFIG` / `VD_DIR` are the upstream-supported overrides (`visidata/settings.py:440-441`). Caveat: `VD_DIR` also relocates plugins / macros / urlcache, so if mutable state should stay in a "data" location (e.g. `~/.local/share/visidata`) rather than "config", only export `VD_CONFIG` and leave `VD_DIR` at the platform default. Full appdirs-on-Darwin analysis captured in [`pitfalls/visidata-feather-stringdtype-numpy-dtype.md → XDG path note`](pitfalls/visidata-feather-stringdtype-numpy-dtype.md). Update cross-refs: pitfall doc, `docs/this_repo/vim-mode.md` (App-layer VisiData subsection + catalog table), file header in the relocated template.
- [ ] **[P?/M] rtk-ai/rtk evaluation** — LLM token compressor (Rust CLI proxy, hooks into agent Bash tool calls, claims 60-90% token reduction). Evaluated alongside the chezmoi-diff trigger and **declined for now**: doesn't actually solve the diff-readability question (no chezmoi filter), and Risk #1 (pitfalls/ verbatim error grep gets broken by RTK's dedup/truncate) structurally conflicts with this repo's knowledge harness. Re-evaluate at v1.0 or when token-cost becomes the dominant agent pain. → [research](backlog/rtk-evaluation.md)
- [ ] **[?/L] svibe: multi-window spillover when panes would fall below min-width** — width-aware single-window layout shipped 2026-04; deferred follow-up is to overflow explicit `svibe N` into `agents-1` / `agents-2` / ... windows (each a wide `even-horizontal` layout at `min_width`) instead of cramming everything into one tiled window. Needs design on window naming (shifts `git` / `edit` indices), `--on-exit restart` routing across windows, focus-on-attach rules, and a `--spill` / `SVIBE_SPILL` gate. Wait for first "svibe > 6" signal before building. → [research](backlog/svibe-multi-window-spillover.md)
- [ ] **[?/M] `swt` — per-feature worktree + editor/agent split on a hotkey** — Sidecar-style flow that stitches `wt add <branch>` + new tmux window + `nvim` ‖ `specstory run <agent>` split into one motion. Reuses worktrunk's worktree machinery and `_sesh_wrap_agent` / `_sesh_on_exit_wrap` helpers from `22_sesh.zsh`. Open design calls: key binding (menu row vs `prefix + W`), branch-name source (fzf over existing branches + auto-named `wip/YYYY-MM-DD-hhmm` fallback), whether to recommend `scode`-style 75/25 layout or something else. Rejected alternative: N standby worktrees ("agent pool") — pre-commits to N idle agents, degenerates back to regular `svibe` when branches aren't actually parallel. → [research](backlog/svibe-worktree-per-feature.md)
- [ ] **[?/L] Lean "no-brainer" bundles + init-CLI UX + decision-tree SSOT** — **2026-06-10 partial**: the `cloud-vm` lean bundle shipped (BUNDLES in `dotfiles_init.py`, used by `just az-dev-vm`), and the prompt-block codegen (`just gen-prompts`) already killed the template/Dockerfile hand-sync. Remaining open here: the `--explain` decision-tree renderer + marking decide-once-upfront vs toggle-anytime prompts. → [research](backlog/lean-bundle-init-ux.md)
- [x] **[?/L] Cloud-VM provision + dotfiles bootstrap combo command** — **Done 2026-06-10** as standalone option B: `scripts/azure/dev_vm.py` + `just az-dev-vm` / `az-dev-vm-down` / `az-dev-vm-status` / `az-dev-vm-ssh`. Idempotent RG+name key, default `az vm auto-shutdown` guardrail, `--spot`, `--gpu` seam (NC SKU + NvidiaGpuDriverLinux extension, no CUDA role), SSH-wait, remote non-interactive `chezmoi init --apply --bundle cloud-vm` (flag set computed from the PROMPTS/BUNDLES SSOT), fleet machines.toml register/de-register, symmetric teardown. Docs: [docs/this_repo/az-dev-vm.md](docs/this_repo/az-dev-vm.md). Promote to a `fleet up` verb only if a second provider shows up. → [research](backlog/cloud-vm-provision-combo.md)
- [ ] **[?/M] `opencodebox` wrapper for the containerized opencode image** — opencode's startup-tip suggests `docker run -it --rm ghcr.io/anomalyco/opencode` for sandboxed runs, but the raw invocation is awkward (no cwd mount, no auth share, hits the Alpine/glibc OpenTUI loader bug on first run — see [pitfall](pitfalls/opencode-docker-opentui-glibc-loader-missing.md)). A thin wrapper (`dot_dotfiles/bin/executable_opencodebox` Python uv-script, or a shell function in `dot_config/shell/`) would auto-mount `$PWD`:/workspace + `~/.local/share/opencode` (auth/sessions), forward TTY/COLOR/term env, apply the `apk add gcompat` workaround (or pin a `:debian` tag if upstream publishes one), and expose `--no-net` / `--ro` toggles. Useful for (a) sandboxed agent runs where the agent might `rm -rf` something, (b) CI / one-shot tasks, (c) trying opencode on a host without the native CLI installed. Native install (the just-shipped official-installer path in `coding_agents`) stays primary because the image has none of our mise/uv/cargo/brew tooling — only worth building if the container use case actually shows up. Two design splits: lightweight (shell function, dispatches `gcompat`-installing variant tag when present) vs full (Python script alongside `fleet` / `mlf` / `pqsum`, with completion + AGENTS.md row). → [research](backlog/opencodebox-wrapper.md)
- [ ] **[?/M] herdr usage-status driver — CodexBar-analog AI quota in the herdr sidebar** — herdr has no native usage/quota display, but exposes a per-pane status hook (`herdr pane report-metadata <pane> --source ID --custom-status "…" --ttl-ms N`). Two paths: (A) install the Codex-only plugin [jerryfane/herdr-codex-usage-kit](https://github.com/jerryfane/herdr-codex-usage-kit) via the devtools ansible role (reads the same `~/.codex/sessions` rate_limits CodexBar does); (B) DIY driver loop that pushes `"Claude X% • Codex Y%"` per agent pane, ideally reusing CodexBar's cached data (no herdr plugin covers Claude/ChatGPT quota). Decision: keep CodexBar (`40_codexbar`, menu bar) as the primary view; build only when the user lives in herdr full-time. Ships alongside `hvibe`/`hcode` (`dot_config/shell/24_herdr.sh`). → [research](backlog/herdr-usage-status-driver.md)
- [ ] **[?/S] Surface `hvibe`/`hcode` inside herdr's UI** — the launchers exist as shell functions (`dot_config/shell/24_herdr.sh`) but only run by typing. Follow-ups to trigger the whole-pack layout from inside herdr: (A) `[[keys.command]]` in `create_config.toml` (e.g. `prefix+alt+v` → `hvibe -p "$HERDR_ACTIVE_PANE_CWD"`); (B) a `Ctrl+V` action in the `herdr-sesh` tv channel that runs `hvibe` on the selected zoxide dir instead of opening an empty workspace; (C) static herdr-plus Projects TOML for fixed per-repo layouts. Ship A first. → [research](backlog/hvibe-herdr-surfacing.md)
- [ ] **[?/L] `fleet`: Windows / WSL worker support** — `fleet exec` cannot reach a Windows devbox today: `_build_remote_cmd()` emits a POSIX string (`export PATH="<_PATH_PRELUDE>"; exec <argv>`) while `dotfiles-windows/scripts/enable-sshd.ps1` sets the sshd `DefaultShell` to pwsh, so our own Windows dotfiles produce a host `fleet` breaks on. Fix is a `platform` / `exec_via` pair on `Host` plus a `pwsh -NoProfile -EncodedCommand <base64 UTF-16LE>` wrapper — pure-ASCII on the wire, so it survives either `DefaultShell` and `fleet` never needs to know which one a box has (neutralises the ansible community.general#11307 quoting-mangle class). Same wrapper carries the optional WSL hop (`wsl.exe -d … -- bash -lc`), which is the *less* reliable target (microsoft/WSL#8072/#9373/#8889). ⚠ `load_hosts()` silently drops unknown TOML keys — new fields must land on the `Host` dataclass in the same commit. Queue stays off native Windows (pueue runs tasks through PowerShell there and `pueued` has no Windows service-event handling, Nukesor/pueue#344). **Blocked on a reachability spike, not effort**: a corporate Dev Box may have neither Tailscale (`managedMachine`) nor inbound 22, in which case the design flips to an outbound-dialing worker. Scheduler / data affinity / Dev Box lifecycle deliberately out of scope. → [research](backlog/fleet-windows-workers.md), cross-repo note `dotfiles-all/docs/multi-machine-compute-plane.md`

---

## Done (recent, kept for context — older items pruned)

- ✅ **Wake-on-LAN (active + passive)** (2026-07) — passive `wake_on_lan` ansible role (Linux-only, prompt `installWakeOnLan`; installs ethtool, deploys a device-bound `wol@<iface>.service`, auto-detects magic-packet-capable wired NICs so arming survives reboots) + active `wake` CLI (`dot_dotfiles/bin/executable_wake`, pure stdlib — works before pip/uv) & `wakeonlan` pkg (`networking_tools`). Host→MAC map in private `~/.config/wake/hosts.toml` (repo ships only a `create_private_` template — real MACs stay out of the public repo). Bilingual guide [docs/sysadmin/wake-on-lan.md](docs/sysadmin/wake-on-lan.md); ErP/S5 silent-fail trap in [pitfalls/wol-wakes-from-suspend-but-not-full-shutdown.md](pitfalls/wol-wakes-from-suspend-but-not-full-shutdown.md). Decision log: [backlog/wake-on-lan.md](backlog/wake-on-lan.md).

- ✅ **atuin zsh integration + cross-shell Alt+R** (2026-05) — new `dot_ansible/roles/atuin/` (brew on macOS, official `https://setup.atuin.sh` installer with `--non-interactive` + `ATUIN_NO_MODIFY_PATH=1` on Linux). Shared init in `dot_config/shell/15_atuin.sh` runs `atuin init zsh --disable-up-arrow --disable-ctrl-r` (zsh keeps `Ctrl+R` on `fzf-history-widget` for muscle memory + OMZ integration), binds `Alt+R` to `_atuin_search_widget` on zsh (re-applied from `zvm_after_init` to survive zsh-vi-mode wipe), and deferred-binds `Alt+R` → `__atuin_history` on bash via `PROMPT_COMMAND` (because `__atuin_history` is defined by atuin init at dot_bashrc.tmpl step 8, AFTER `15_atuin.sh` is sourced at step 7). Bash `Ctrl+R` stays on atuin (long-standing). New `atuin` upgrade category in `scripts/upgrade_tools.sh` + `just upgrade-atuin` (Linux only; macOS uses `upgrade-brew`). Sync left local-only by default; opt-in flow documented in [docs/tools/atuin.md](docs/tools/atuin.md). Cross-shell asymmetry (zsh `Ctrl+R`=fzf, bash `Ctrl+R`=atuin, both `Alt+R`=atuin) is intentional — see the keybindings table in atuin.md.

- ✅ **fleet-status: `init-in-progress` 13th state** (2026-05) — probe now `pgrep`s for `chezmoi (update|apply|init)` in SSH probe + `_probe_readiness_local`; promotes `no-source` to `init-in-progress` when a `chezmoi update --init` is mid-clone (often paused at an interactive prompt in another SSH session). Outranks `no-source` in priority because transient. Self-pgrep hazard avoided by excluding probe shell ($$ + $PPID; `os.getpid()` + `os.getppid()` locally). Detection via argv subcommand match — no age check, trust pgrep. → [research](backlog/fleet-status-init-in-progress-state.md)

- ✅ **fleet-apply: `self` (local) host stuck-running** (2026-05) — `run_one_local` was using `await proc.wait()` which on asyncio waits for stream EOF, not OS-level process exit. Orphan grandchildren of chezmoi (ansible/brew/mas children that survived the `run_onchange_*` scripts) held our stdout/stderr fds open, so the wait never returned and the live table tickered the elapsed clock indefinitely. Fixed via `os.kill(pid, 0)` polling on the actual chezmoi PID, plus 5s grace cancel of the `_drain` tasks. Tested with synthetic orphan-child scenario (60s → 2s). → [pitfall](pitfalls/fleet-apply-self-stuck-running.md) (option-3 walkthrough including the false-summit option-1 attempt)
- ✅ **tv agent-sessions richer preview + SpecStory linkage** (2026-04) — three gaps fixed in one pass. (1) `all` mode now sorts globally by mtime DESC across agents (helper previously printed five per-agent blocks each sorted internally). (2) New TSV column 5 carries the matched `.specstory/history/*.md` path (grep-based reverse lookup via the `<!-- <Provider> Session <uuid> -->` comment SpecStory embeds on line 5 of every file); `Alt+S` copies it to clipboard. (3) Preview 1 (default) now shows metadata + full back-and-forth transcript rendered via `glow` (fallback `bat -l md`), preferring SpecStory's pre-rendered markdown when present and falling back to raw-source walk for live sessions — Claude Code / Codex / OpenCode covered; Cursor side deferred (P? above). Docs split: deep-dive moved to [docs/tools/specstory-internals.md](docs/tools/specstory-internals.md); install/config stays in [docs/tools/specstory.md](docs/tools/specstory.md). Decision log: [backlog/tv-agent-sessions-richer-preview.md](backlog/tv-agent-sessions-richer-preview.md).
- ✅ **`.chezmoiscripts/{global,repo}/` layout** — six `run_onchange_after_*.sh.tmpl` scripts moved out of repo root into a 2-bucket nested layout (Option A). `repo_` infix dropped from `45_bootstrap_skills` (scope now encoded in directory). Cross-refs updated in AGENTS/CLAUDE/docs/ignore files. Layout doc: [docs/this_repo/chezmoiscripts-layout.md](docs/this_repo/chezmoiscripts-layout.md). Decision log: [backlog/chezmoiscripts-namespace-refactor.md](backlog/chezmoiscripts-namespace-refactor.md). One-time cost: chezmoi re-runs each script once on every machine after the path change.

- ✅ **tmux built-in window activity/bell flags** — `monitor-activity`/`monitor-bell` enabled in `dot_config/tmux/common.conf` so Catppuccin's `#{window_flags}` shows `#`/`!` glyphs for non-current windows that produced output or rang the bell. `monitor-silence` left disabled (agent panes have a perpetual spinner, would never fire). Option A of `backlog/tmux-window-status-indicators.md`; option C (hook-based semantic state) scoped + deferred.
- ✅ **conda/mamba lazy init** — `dot_config/zsh/tools/04_conda_mamba.zsh` finds miniforge3/miniconda3/anaconda3 and lazy-loads on first `conda`/`mamba` call.
- ✅ **NVM lazy setup** — `02_legacy_tools.zsh` wires `NVM_DIR`; opt-in via `LOAD_NVM=1` env or `load-nvm` alias to keep startup fast.
- ✅ **yazi `y()` function + ansible install** — `35_yazi.zsh` provides cwd-tracking wrapper; `devtools` role installs yazi (apt fallback to GitHub release musl binary).
- ✅ **bun / pnpm / cargo / go PATH** — wired in `02_legacy_tools.zsh` + `06_cargo.zsh`. Install side still pending (P2 above).
- ✅ **ccusage alias** — `07_bunx_cli.zsh` runs via `bunx`.
- ✅ **rust/cargo ansible role** — `rust_cargo_tools` role with `cargo-update` for upgrade flow.
- ✅ **Brewfile-based macOS package management** — XDG Brewfiles at `~/.config/homebrew/` with chezmoi `run_onchange` script (opt-in via `installBrewApps` prompt).
