{{- /*
.chezmoiremove — paths to delete from $HOME on each `chezmoi apply`.
Templated; re-evaluated every apply (idempotent — chezmoi only acts
when the path actually exists). One pattern per line.

Currently used to clean up the zsh-side copies of files that were
moved into the shared dot_config/shell/ POSIX layer (see commit
9bb56c2 "shell: extract POSIX layer..." 2026-05). Without this, the
orphan ~/.config/zsh/ files would still get sourced by
load_modular_dir and double-load PATH/env on every shell start.

Pruning policy: leave entries forever unless the file grows large
(>50 lines). `stat()` on each non-existent path is a few µs per
apply — negligible cost vs the safety of cleaning up any host that
hasn't applied since the migration commit. After ~6-12 months on
the fleet, individual entries can be pruned in a follow-up commit
if the maintainer is confident every host has applied at least
once. Tracked as a [P3/S] follow-up in TODO.md; not urgent.
*/ -}}
.config/zsh/00_exports.zsh
.config/zsh/02_legacy_tools.zsh
.config/zsh/tools/01_starship.zsh
.config/zsh/tools/05_mise.zsh
.config/zsh/tools/20_zoxide.zsh
.config/zsh/tools/27_thefuck.zsh
.config/zsh/tools/30_direnv.zsh
.config/zsh/tools/10_fzf.zsh
.config/zsh/tools/06_cargo.zsh
.config/zsh/tools/07_bunx_cli.zsh
.config/zsh/tools/25_bat.zsh
.config/zsh/tools/26_eza.zsh
.config/zsh/tools/29_marimo.zsh
.config/zsh/tools/34_openclaw.zsh
.config/zsh/tools/37_lazygit.zsh
.config/zsh/tools/40_codexbar.zsh
.config/zsh/tools/41_github.zsh
.config/zsh/tools/42_gitlab.zsh
.config/zsh/tools/50_networking.zsh
.config/zsh/tools/28_tldr.zsh
.config/zsh/tools/29_log_tools.zsh
.config/zsh/tools/35_yazi.zsh
.config/zsh/tools/38_lazyvim.zsh
.config/zsh/tools/38_workmux.zsh
.config/zsh/tools/55_web_reader.zsh
.config/zsh/tools/94_ssh_agent.zsh
.config/zsh/tools/04_ai_capture.zsh
.config/zsh/tools/29_media.zsh
.config/zsh/tools/96_ssh_setup.zsh
{{- /*
Legacy AeroSpace config at $HOME root. We manage the XDG copy
(~/.config/aerospace/aerospace.toml); AeroSpace errors if BOTH it and
~/.aerospace.toml exist, so remove the legacy one. Gated to darwin +
installBrewApps (where the cask + XDG config actually deploy). smart backup
captures it before removal.
*/ -}}
{{- $installBrewApps := false }}{{ if hasKey . "installBrewApps" }}{{ $installBrewApps = .installBrewApps }}{{ end }}
{{- if and (eq .chezmoi.os "darwin") $installBrewApps }}
.aerospace.toml
{{- end }}
