# Pitfalls

Past traps we've stepped on. Symptoms-first knowledge base — the goal is that
when a problem recurs (on a new machine, after an upgrade, with a new tool
combo), grepping the symptom here lands you on the root cause and workaround
in seconds, instead of re-debugging from scratch.

This folder is `chezmoi`-ignored (see `.chezmoiignore.tmpl` → `pitfalls/**`); it
is repo metadata for maintainers, not user-facing config to deploy.

## Pitfalls vs the rest

This repo has four sibling surfaces with overlapping shapes; pick the right one:

| Surface | Time direction | Question it answers | Access pattern |
|---|---|---|---|
| `docs/tools/<tool>.md` | Present | "How does this tool work / how do I configure it?" | Read top to bottom |
| `docs/this_repo/<topic>.md` | Present | "Why did we architect it this way?" | Read top to bottom |
| `pitfalls/<slug>.md` | **Past** | **"I see error X — has this happened before?"** | **Grep symptoms** |
| `backlog/<slug>.md` | Future | "We thought about doing Y — what was the analysis?" | Index in `TODO.md` |
| `AGENTS.md` Hard invariants | Present | "What rules MUST agents follow?" | Read top to bottom |

A pitfall **graduates** to a Hard invariant in `AGENTS.md` when the trap is
serious enough that you can't rely on memory or grep — typically when (a) it
recurs across machines, (b) it silently corrupts state, or (c) the workaround
is non-obvious and easy to undo by accident. When graduating, leave a
`pitfalls/<slug>.md` as historical record and link to it from the invariant.

## When to add a pitfall doc

Add a `pitfalls/<slug>.md` when you've spent more than ~15 minutes on something
where the answer wasn't googleable, AND any of:

- The symptom is non-obvious from the root cause (`SSH_CONNECTION` cleared
  silently → hostname module disappears, no error)
- The fix is "do nothing different but in a specific order" (sentinel writes
  must come after `wait $pid` for fleet-apply, etc.)
- The same trap could be hit by a new agent / new machine / new contributor
- An upstream bug exists with no ETA — workaround needs to outlive memory
- A specific tool version is required (or forbidden) and the failure mode at
  the wrong version is silent / confusing (e.g. tmux 3.2a popup off-screen)

## When NOT to add a pitfall doc

- Trivially googleable error (the next person will solve in 30 seconds anyway)
- Already covered in `docs/tools/<tool>.md` as part of the tool's normal
  configuration — cross-link from this README's "Cross-referenced pitfalls"
  table below instead of duplicating
- Already a `AGENTS.md` Hard invariant — those have higher priority enforcement
  (cross-link only)
- One-off transient (network glitch, machine-specific config rot) — fix and
  move on

## File template

See [`assets/pitfall-doc.md.template`](#) in the
[`project-knowledge-harness` skill](https://github.com/daviddwlee84/agent-skills/tree/main/skills/local/project-knowledge-harness),
or copy the structure of an existing entry below.

Key sections (different from `backlog/` template — symptom-first, not
context-first):

```markdown
# <Title describing the SYMPTOM, not the root cause>

**Symptoms** (grep this section): <verbatim error messages, observable behaviour>
**First seen**: YYYY-MM
**Affects**: <tool/version/OS combo>
**Status**: workaround documented / fixed upstream in vX.Y / WONTFIX

## Symptom

Full error messages (verbatim, not paraphrased — preserves grep-ability).
Reproduction steps if applicable.

## Root cause

Why this happens. Reference source, docs, or upstream issue.

## Workaround

Immediate workaround (commands, config diff). Should be copy-pasteable.

## Prevention

How to avoid stepping on this again. If serious enough, link to the
corresponding `AGENTS.md` Hard invariant.

## Related

Links to `docs/`, sibling pitfalls, `TODO.md` entries (if a real fix is
queued), upstream issues/PRs.
```

## Index

Pitfalls owned by this folder. Keep alphabetical.

| Slug | Symptom keywords | Status |
|---|---|---|
| [`agent-warmup-scheduled-run-hangs-or-blank-under-daemon`](agent-warmup-scheduled-run-hangs-or-blank-under-daemon.md) | launchd/pueue `agent-warmup run` wedges for hours (`state = running`, no log) or finishes with an EMPTY `/usage` panel / blank `capture-pane`; `sample` shows `dyld mapFileReadOnly`; stuck PID is `uv` or `python3 .../agent-warmup`; identical `agent-warmup verify` works by hand | fixed (main-volume stdlib bootstrap · isolated uv-first runtime with startup sentinel/fallback · existing Herdr or GUI tmux backend) |
| [`ansible-folded-scalar-regex-empty-url-silent-skip`](ansible-folded-scalar-regex-empty-url-silent-skip.md) | `~/.local/share/applications/zen-browser.desktop` has a **recent** mtime but `stat ~/Applications/zen.AppImage` → `No such file or directory`; Zen absent from the GNOME launcher (`TryExec=` dangles, so the entry hides rather than erroring); `applaunch Zen` does nothing while `appquit`/`appactivate` still work; the Zen actually running is an unmanaged `zen-x86_64_<hash>.AppImage` frozen at `Version=1.11.5b` / `BuildID=20250419054652`, so Twitch says `Your browser is not currently supported`; ansible reports `failed=0`, `--syntax-check` passes, `rescue:` never fires | fixed — three compounding bugs: `\\.` in a YAML **folded** scalar (`>-`) stays two literal backslashes so the `match` regex never matched and the URL resolved to `''` (double-quoted scalars *do* unescape — the mirror image of [`ansible-when-regex-replace-backslash-strip`](ansible-when-regex-replace-backslash-strip.md)); a **skipped** task passes `is succeeded`, so the icon/`.desktop`/`update-desktop-database` tasks ran for an AppImage never fetched; and `retries:`/`delay:` without `until:` are ignored outright. Now `equalto` (no regex), `is not skipped` guards, an explicit no-match warning, and real `until:` |
| [`ansible-homebrew-expecting-value-line-1-column-1`](ansible-homebrew-expecting-value-line-1-column-1.md) | `Module failed: Expecting value: line 1 column 1 (char 0)` at a `community.general.homebrew` task (`roles/devtools` resvg), `failed=1`, `20_ansible_roles.sh: exit status 2` — so every later `run_after_` script (bat theme, brew bundle, yazi plugins, completions) is silently skipped; also `brew bundle` "succeeding" while installing nothing | fixed (a fake `brew` stub — `#!/bin/sh`+`exit 0` — satisfied `command -v brew` and returned 0 with empty output for every subcommand; bootstrap now has a glibc >= 2.28 gate so the stub isn't needed, and all probes test `brew --prefix` **output** instead of exit status) |
| [`ansible-js-cli-tools-old-system-node`](ansible-js-cli-tools-old-system-node.md) | `Tag 'js_cli_tools' had failures` / `Tag 'coding_agents' had failures`; `npm WARN EBADENGINE` + `EACCES /usr/local/lib/node_modules`; system node v12.22.9 on Ubuntu jammy | fix paths documented (no in-repo fix yet) |
| [`ansible-missing-sudo-tag`](ansible-missing-sudo-tag.md) | `chezmoi apply` in noRoot mode aborts at one ansible task with `sudo: a password is required` / `Premature end of stream waiting for become success`; works fine on rooted hosts | workaround documented (add `tags: [sudo]`); convention = every `become: true` in `dot_ansible/` must pair with `tags: [sudo]` |
| [`ansible-when-regex-replace-backslash-strip`](ansible-when-regex-replace-backslash-strip.md) | an ansible `when:` / `failed_when:` / `changed_when:` clause combining `regex_replace(...)` with `is version(...)` dies with `The test plugin 'ansible.builtin.version' failed: Version comparison failed: '<' not supported between instances of 'str' and 'int'` — even though **both** operands look purely numeric (`0.7.4` vs `0.8.5`, no `rc`/`dev` component); the byte-identical expression inside `debug.msg: "{{ … }}"` prints the right substring (`0.7.4`); double-escaping to `\\S` / `\\1` works while the single-escaped `when: >-` form keeps failing; error origin points at the task header / `when:` column, never at the regex; ansible-core >= 2.18 + Python 3.13; `ansible-playbook --syntax-check` is clean | fixed (`dot_ansible/roles/python_uv_tools` extracts the version with `awk '{print $2}'` at the registering step + `set_fact`, so there is no `regex_replace` in `when:` at all) — the conditional lexer strips one extra backslash layer (`\S`→`S`, `\1`→`1`), the pattern stops matching, and `regex_replace` returns the **input unchanged** so `"uv 0.7.4 (…)"` reaches the `version` test; see [ansible#41555](https://github.com/ansible/ansible/issues/41555) |
| [`appimagelauncher-renames-managed-appimage`](appimagelauncher-renames-managed-appimage.md) | Zen has both `zen-browser.desktop` and `appimagekit_*Zen*.desktop`; deleting the latter lasts only until login; older installs vanish after first launch because AIL md5-renamed `zen.AppImage` and left `TryExec` dangling | fixed — AppImageLauncher integrates through both its watched-directory daemon and a `binfmt_misc` execution hook. Zen now lives at the unwatched fixed path `~/.local/opt/zen/zen.AppImage`, launches with the upstream-supported `APPIMAGELAUNCHER_DISABLE=1`, removes stale AIL entries/icons, and uses the single stable ID `zen-browser.desktop` |
| [`apprise-dbus-disabled-in-uv-tool-venv`](apprise-dbus-disabled-in-uv-tool-venv.md) | Claude Code `Stop`/`Notification` hooks produce **no banner and no error**; `apprise --tag desktop --title t --body b` prints nothing and exits `1`; `apprise -vvv` reveals `WARNING - DBus Notification is currently disabled on this system.` plus `WARNING - Failed to load Apprise configuration from file://…/custom.yaml`; `notify-send` works fine by hand and the *system* python imports `dbus` happily | fixed (`uv tool install --force apprise --with dbus-python` — a uv-tool venv can't see distro `python3-dbus`, so the `dbus://` plugin disables itself; the "Failed to load" line means the included config has **zero services**, not bad YAML). `notify.sh` is now apprise → `notify-send`/`terminal-notifier` → log file so it can never fail silently again. Ansible role still unpatched (needs a per-tool OS gate) |
| [`appsrc-tui-subprocess-thread-hang`](appsrc-tui-subprocess-thread-hang.md) | `appsrc tui` opens, paints the `Name  Source  Kind  Size  Path` header, and the table stays **empty forever** (no rows after 10-20s, no crash, no traceback); `tv appsrc` shows `0 / 0` / `Select an entry to preview` while `appsrc scan` in a plain shell happily prints 2266 items; `APPSRC_TUI_DEBUG=/tmp/x appsrc tui` log stops dead at `ctx built -> inventory` or at a random `cli 1500/2193 :: …` with no `inventory=N`; intermittent and stalls at a **different** subprocess each run; a headless `App.run_test()` pilot populates 2276 rows every single time, so the pilot "passes" while the real terminal is dead; `stdin=subprocess.DEVNULL` does not help | fixed (async `@work` worker + `asyncio.create_subprocess_exec` shelling out **once** to `appsrc scan/size --json` instead of thousands of in-thread forks) — blocking `subprocess.run` inside a Textual `@work(thread=True)` races the loop's asyncio child-watcher over reaping, so it deadlocks probabilistically per call; validate in a real tmux PTY, `run_test()` is necessary but not sufficient |
| [`apt-update-fails-base-role-empty-error`](apt-update-fails-base-role-empty-error.md) | `Failed to update apt cache after 5 retries: ''` (empty reason) at base role; `Failed to update cache after 5 retries due to , retrying`; `chezmoi init` exit 1 on Debian/Ubuntu; GFW host with unreachable third-party repo / dead lazygit PPA leftover | fixed (best-effort cache update + CLI diagnostics; lazygit PPA removed + purged; deb822 migration with policy-probe rollback) |
| [`bat-theme-cache-cleared-never-rebuilt`](bat-theme-cache-cleared-never-rebuilt.md) | `[bat warning]: Unknown theme 'tokyonight_night', using default.` on every `bat` call and every `git diff` (delta embeds bat); `bat --list-themes \| grep tokyonight` empty; `ls "$(bat --cache-dir)"` → `No such file or directory`; the managed `.tmTheme` IS present and `chezmoi apply` never fixes it | fixed (run-script `run_onchange_after_` → self-healing `run_after_` with a cache-freshness check; shell only exports `BAT_THEME` when the compiled cache exists) |
| [`bitwarden-ssh-agent-confirm-blocks-remote-git-push`](bitwarden-ssh-agent-confirm-blocks-remote-git-push.md) | `git push` / ssh fails `Connection closed by … port 443` + `Could not read from remote repository` when `$SSH_AUTH_SOCK` is Bitwarden's agent and you're on a remote/headless session; intermittent (first push OK, later fail); `ssh-add -l` still lists the key | by design — disable per-use confirm, approve at the physical screen, `ssh -A` forward, or use HTTPS+PAT |
| [`blesh-set-v-leaks-gexec-wrapper`](blesh-set-v-leaks-gexec-wrapper.md) | `{ _ble_edit_exec_gexec__save_lastarg "$@"; } 4>&1 5>&2 &>/dev/null` printed after every prompt under bash + ble.sh; typed command echoed twice; goes away in fresh terminal; no `+` prefix (so not `set -x`) | workaround = `set +v` in-session; `set -v` is architecturally incompatible with ble.sh's eval-based gexec |
| [`bootstrap-curl-bash-hangs-after-reattaching-tty`](bootstrap-curl-bash-hangs-after-reattaching-tty.md) | `curl … \| bash` of `bootstrap.sh` hangs forever right after `[bootstrap …] stdin is a pipe — reattaching to /dev/tty for interactive prompts`; no `fetching + running …` log; no `uv`/`chezmoi` child process; only happens on certain fresh users / hosts | fixed (`exec </dev/tty` removed mid-script; `/dev/tty` redirected only on the final `exec uv run`) |
| [`bootstrap-no-tty-sudo-prompt-skipped`](bootstrap-no-tty-sudo-prompt-skipped.md) | `[WARN] Could not establish a sudo session (no TTY and sudo needs a password)` then Linuxbrew installer aborts with `Insufficient permissions to install Homebrew to "/home/linuxbrew/.linuxbrew"`; `mkdir: cannot create directory '/run/user/<UID>': Permission denied`; user verifiably has interactive sudo; CentOS 7 + high-UID AD/LDAP user | workaround documented (`CHEZMOI_SUDO_PASSWORD_FILE` / `just apply-with-sudo` / `NOPASSWD: ALL` sudoers drop-in) |
| [`brew-bundle-redownloads-manually-installed-cask`](brew-bundle-redownloads-manually-installed-cask.md) | `brew bundle` re-downloads cask for `.app` already in `/Applications/`; chains into `process has already locked .incomplete` parallel-fetch errors; `[WARN] Some packages in macOS packages from Brewfile.darwin still failed after 2 attempts` | partial: stale-lock cleanup permanent; `--adopt` pre-flight opt-in via `CHEZMOI_BREW_ADOPT_PREFLIGHT=1` (version-strict + password-storm trade-offs documented) |
| [`brew-cask-slow-github-release-assets`](brew-cask-slow-github-release-assets.md) | `brew upgrade --cask <x>` download creeps at ~25 KB/s from `release-assets.githubusercontent.com`; TUNA/ghproxy/Clash don't help | no workaround (network-path issue) |
| [`btop-themes-permission-denied-core-dumped`](btop-themes-permission-denied-core-dumped.md) | `btop` aborts on launch: `IOT instruction (core dumped)` + `terminate called ... std::filesystem::filesystem_error` / `directory iterator cannot open directory: Permission denied [~/.config/btop/themes]`; dir perms are fine (775 user-owned); `command -v btop` = `/snap/bin/btop` (publisher `kz6fittycent`) | not a repo bug; snap `home` interface can't read hidden dirs; workaround = `sudo snap remove btop` (or `brew install btop`) so apt/GitHub/brew btop wins on PATH |
| [`centos7-ansible-yum-dnf-backend`](centos7-ansible-yum-dnf-backend.md) | every `ansible.builtin.yum:` task on CentOS 7 fails with `Could not detect which major revision of dnf is in use, which is required to determine module backend` / `You should manually specify use_backend to tell the module whether to use the dnf4 or dnf5 backend` + `ansible_facts: pkg_mgr: unknown`, then `...ignoring` — so `chezmoi apply` "succeeds" while `git`, `git-lfs`, `jq`, `gcc`, `gawk`, `zsh` are silently never installed and downstream blows up as `modify_*.json: jq not found` / `ble.sh Makefile: gawk not found`; porting the task to `shell:` then hits `Error loading tasks: failed at splitting arguments, either an unbalanced jinja2 block or quotes` pointing at the **task header line** (not the shell line) — caused by `${#missing[@]}` (Jinja2 reads `{#missing[@]}` as a comment open) or by an apostrophe in a shell comment (`# We don't …`) | fixed (all four roles — `base`/`zsh`/`bash`/`ruby_gem_tools` — replaced `ansible.builtin.yum:` with `ansible.builtin.shell:` running the yum CLI + `rpm -q` idempotency and an `ALL_INSTALLED` sentinel; works unchanged on Rocky/Alma where `yum` shims to dnf). ansible-core 2.18 collapsed `yum` into the dnf backend and removed the `use_backend: yum` escape, and CentOS 7 has no `dnf` binary at all; avoid `${#arr[@]}` and contractions inside `shell:` blocks. No Docker profile caught it — `docker-test-centos7` ran `noRoot=true`, `rocky9` has dnf |
| [`centos7-idc-slow-broken-installs`](centos7-idc-slow-broken-installs.md) | `chezmoi apply` on a no-sudo CentOS 7 IDC box takes **4h41m**, of which 4h04m (87%) is the single `Install Claude Code (Linux)` task (`curl https://claude.ai/install.sh \| bash`); PLAY RECAP shows only `failed=1` while six tasks failed silently under `ignore_errors: true`; tree-sitter-cli cargo fallback dies `Unable to find libclang … libclangAST.so.7: No such file`; four `mise exec -- npm install -g` tasks (Copilot / Codex / Gemini / OpenChamber) die `EACCES /usr/lib/node_modules/@githubnext` plus `EBADENGINE` — `mise exec` only injects env vars, so with no active mise node `npm` falls through to system `/usr/bin/node` v16 with prefix `/usr/lib`; `mise install rust@latest` 404s on the TUNA rustup mirror (`1.95.0-x86_64-unknown-linux-gnu`); three `community.general.homebrew` tasks return `Expecting value: line 1 column 1 (char 0)` (`brew info --json=v2` empty) costing ~1 min + a pointless tap each | partially fixed — only the CodexBar leg landed: its Linux install now probes glibc and pulls the truly-static `linux-musl-<arch>` tarball below `GLIBC_2.38` **and skips the Linuxbrew path too** (`steipete/tap/codexbar` ships the same glibc tarball, so `brew install` "succeeds" and leaves a binary that cannot run, after which the `which codexbar` guard suppresses the working fallback forever). The earlier "profile-gate CodexBar/td/sidecar off headless Linux" proposal was **retracted 2026-07-27** (all three are CLI/TUI, useful over SSH). Rest still queued: `[P1][S]` pin `rust@stable`, `[P1][M]` fix the mise npm prefix for the 4 CLI tasks, `[P2][M]` resumable Claude install cache, `[P3][S]` skip the cargo tree-sitter fallback on CentOS 7 |
| [`centos7-neovim-apt-register-defined`](centos7-neovim-apt-register-defined.md) | `just ansible-tags neovim` / `chezmoi apply` on **sudo-having** CentOS 7 box completes `ok=8 changed=0 skipped=23 failed=0` but `command -v nvim` is empty; user-level fallback gated `apt_neovim is not defined` but `register:` on a `when:`-skipped task IS defined (as a skip dict); plus mainline nvim ≥0.10 binaries require glibc 2.28+ which EL7 lacks | fixed (gate adds `or apt_neovim is skipped`; oldEL branch downloads from upstream's `neovim/neovim-releases` older-glibc repo) |
| [`centos7-noroot`](centos7-noroot.md) | `chezmoi apply` on a sudo-less CentOS Linux 7.9 corporate box finishes cleanly with ansible `0 failed` and configs deployed, yet `command -v rg fd jq nvim starship` returns nothing and every new shell prints `command not found` for half the prompt; `sudo -v` → `sudo: PAM account management error: Permission denied` / `sudo: a password is required`; both install paths are gated out at once — the system path by `--skip-tags sudo`, the user-level GitHub-release fallback by a stale `when: ansible_facts["os_family"] == "Debian"`; glibc 2.17 also yields `./tool: /lib64/libc.so.6: version \`GLIBC_2.29' not found (required by ./tool)`; bootstrap silently resolves `ansible-core==2.11.12` against system Python 3.6.8 and then `[WARNING]: Skipping Galaxy server https://galaxy.ansible.com/api/` / `ERROR! Unexpected Exception, this is probably a bug: '/api/v3/plugin/ansible/content/published/collections/index/community/general/versions/'` / `chezmoi: 00_bootstrap.sh: exit status 1` (reads like a network/proxy fault — the script's proxy hint is a red herring); plus `mise exec -- npm install -g` hitting system node v16 `EACCES` and `mise install rust@latest` 404ing on TUNA | fixed (user-level predicates broadened to `os_family in ["Debian", "RedHat"]` across `base`/`starship`/`security_tools`/`rust_cargo_tools`; bootstrap pins `uv tool install --force --python 3.13 ansible-core` and force-reinstalls any ansible on Python <3.10, so old boxes self-heal; 2026-05 an `oldEL` fact in `playbooks/linux.yml` gates the mise-node/rust tasks, with a direct `sh.rustup.rs` branch). Deliberately NOT broadened: the sudo-tagged `apt:` blocks. Prefer musl release assets; `zsh` + `git` must already exist on the box. Repro: `just docker-run-centos7-noroot` / `docker-run-rocky9-noroot` |
| [`centos7-numpy-pandas-source-build`](centos7-numpy-pandas-source-build.md) | `python_uv_tools` role loops `FAILED - RETRYING: Install Python CLI tools via uv (3 retries left)` then `failed: [localhost] (item=sqlit-tui[ssh])` / `(item=visidata)` with `Failed to download and build \`pandas==2.3.3\`` → `Failed to build \`numpy==2.4.4\`` → `Call to \`mesonpy.build_wheel\` failed (exit status: 1)` / `../meson.build:25:4: ERROR: Problem encountered: NumPy requires GCC >= 9.3` / `C compiler for the host machine: cc (gcc 4.8.5 ...)`; pure-Python items (`thefuck`, `apprise`, `tmuxp`, `copyparty`, `trafilatura`) install fine in the same loop, so it looks tool-specific rather than host-wide; same family hits `mlflow` (greenlet C++11) and `jupyterlab` (pyzmq 27 Cython-3 C99 vs gcc's `-std=gnu89`) | fixed (`needs_modern_gcc: true` per-tool flag in `python_uv_tools/defaults/main.yml` + a `gcc -dumpversion` probe setting `gcc_too_old` when major < 9 → CentOS 7 skips `sqlit-tui[ssh]`/`visidata`/`mlflow`/`marimo[recommended,mcp]`/`jupyterlab`, Rocky 9 / Ubuntu 22.04+ unaffected); numpy 2.x dropping the manylinux_2_17 baseline is upstream and uv can't downgrade to numpy 1.x without pinning pandas, so conda/mamba or `uv tool install --python 3.11 --with 'pandas<2'` stay manual escape hatches |
| [`centos7-systemd-user-instance-missing`](centos7-systemd-user-instance-missing.md) | `rust_cargo_tools : Enable and start pueued service` fails with `Failed to get D-Bus connection: No such file or directory`, ansible `...ignoring`; PLAY RECAP `failed=0` but daemon never starts; `sudo loginctl enable-linger $USER` does NOT help; `sudo systemctl start user@$(id -u).service` returns `Unit not found`; `find /usr/lib/systemd -name 'user@.service'` is empty | WONTFIX upstream (RHEL 7 EOL); workaround = run pueued under tmux/nohup or as system service; ansible role's `ignore_errors: true` is correct |
| [`centos7-zsh-too-old`](centos7-zsh-too-old.md) | after a "successful" `chezmoi apply` on CentOS 7, launching `zsh` dumps a wall of plugin errors: `zsh-vi-mode.zsh:487: parse error near \`]]'`, `(eval):73: unknown condition: -v`, `~/.config/zsh/tools/05_aisuggest.zsh:429: add-zle-hook-widget: function definition file not found`, `28_tldr.zsh:4: unknown file attribute`, `_maybe_add_keys:6: unknown file attribute`; `zsh --version` = `zsh 5.0.2` (5.0.2-34.el7, the newest base+EPEL has); the co-printed `command not found: conda` is an unrelated `/etc/profile.d/` red herring; when source-building from ansible's `shell:` the build itself dies `configure: error: no controlling tty` / `Try running configure with --with-tcsetpgrp or --without-tcsetpgrp` | fixed (zsh role detects `zsh < 5.3` on RedHat-family and source-builds zsh 5.9 into `~/.local/bin/zsh` — `ncurses-devel` via yum CLI, `--with-tcsetpgrp` passed explicitly, `creates:`-idempotent, appended to `/etc/shells` so `chsh` accepts it — and "Locate zsh binary" then prefers it over `/usr/bin/zsh`); a pre-existing zsh ≥ 5.3 on PATH auto-skips the build, and noRoot hosts stay on 5.0.2 with vi-mode + aisuggest broken (`backlog/zsh-noroot-fallback.md`) |
| [`chezmoi-init-prompt-flag-mismatch`](chezmoi-init-prompt-flag-mismatch.md) | `docker build` (or `chezmoi init --apply` over SSH / any TTY-less run) fails at the very last step with `chezmoi: template: chezmoi.toml:23:16: executing "chezmoi.toml" at <promptChoiceOnce . "profile" "Which profile" ...>: error calling promptChoiceOnce: could not open a new TTY: open /dev/tty: no such device or address` — even though the `RUN` line passes `--promptString "profile (ubuntu_server\|ubuntu_desktop\|macos\|centos_server)=centos_server"`, which chezmoi silently ignores with no warning; latent for years because it only fires when the value differs from the template default (the Ubuntu image passed `ubuntu_server`, which is the Linux default anyway), so `Dockerfile.centos7` / `Dockerfile.rocky9` / the `ubuntu_desktop` compose service broke while plain `just docker` looked healthy; `dotfiles_init.py doctor` reports clean throughout | fixed (`--prompt*` flags match on the prompt **text** — 3rd arg to `promptXOnce`, here `Which profile` — not the key name, and are type-strict so `--promptString` can never answer a `promptChoiceOnce`; all three Dockerfiles now pass `--promptChoice "Which profile=${CHEZMOI_PROFILE}"`. `scripts/init/dotfiles_init.py` was already correct via `Prompt.prompt_text`; extending `doctor_scan` to regex the Dockerfile's flag text is queued as `[P2][S]` in `TODO.md`) |
| [`chezmoi-reinit-promptonce-keeps-stale-value`](chezmoi-reinit-promptonce-keeps-stale-value.md) | re-running `chezmoi init` / `dotfiles_init.py` doesn't change a setting; `--promptBool "...=false"` ignored, `installX` flag stays `true`; `chezmoi data` shows stale value after re-apply; no error | fixed (`init` re-init branch + `reconfigure` both pass `--prompt`) |
| [`chezmoi-update-blesh-local-changes-overwritten`](chezmoi-update-blesh-local-changes-overwritten.md) | `chezmoi update` / `--refresh-externals` aborts on `.local/share/blesh`: `Your local changes to the following files would be overwritten by merge` (`lib/core-syntax.sh` …); diff = added license header + stripped comments; nobody edited ble.sh source | fixed (source clone moved to `~/.local/src/blesh`, decoupled from `make install` target) |
| [`chezmoiignore-negation-noop-under-recursive-glob`](chezmoiignore-negation-noop-under-recursive-glob.md) | a new managed file under a `**`-ignored dir shows `chezmoi: …: not managed`; `chezmoi managed \| grep` empty; `!path` re-include in `.chezmoiignore` silently does nothing; `chezmoi apply` exits 0 with no error | workaround documented (use single-`*` globs `dir/*` + `dir/*/**` so the parent stays un-excluded, then `!dir/keep`) |
| [`claude-code-permission-mode-resets-after-interactive-prompt`](claude-code-permission-mode-resets-after-interactive-prompt.md) | Claude Code drops from `bypassPermissions` / `plan` back to `acceptEdits` after answering any `AskUserQuestion` / CodeIsland popup / remote-control inject; `Bash(...)` then re-prompts for permission | workaround documented (`permissions.defaultMode = "auto"` in overlay); WONTFIX upstream |
| [`claude-code-keybindings-empty-bindings-array`](claude-code-keybindings-empty-bindings-array.md) | `/doctor` → `✘ Keybinding configuration issues` / `keybindings.json must have a "bindings" array` / `Use format: { "bindings": [ ... ] }`; the file is valid JSON with `$schema` + `$docs`; only fresh-bootstrapped machines; later `chezmoi apply`s don't fix it | fixed (`modify_keybindings.json` bootstrap stub emits `"bindings": []` + steady-state heal leg) |
| [`claude-hud-shows-raw-model-id`](claude-hud-shows-raw-model-id.md) | claude-hud statusline prints the literal pinned model string `claude-opus-5[1m]` (with the `[1m]` suffix visible) instead of `Opus 5 (1M context)`; **same dotfiles config renders differently on two machines**; no error, model works fine; possibly alongside HUD context % climbing ~5× fast or exceeding 100% | not a claude-hud or pin bug — Claude Code too old to match the model id, so it sends no `display_name` and claude-hud falls back to `model.id`; fixed by `claude update` / `just upgrade-agents` on the lagging host |
| [`claude-hud-usage-statusline-stale`](claude-hud-usage-statusline-stale.md) | `agent-wakeup status` says `WAIT_QUOTA` while Claude is active; tmux capture contains stale `Context ... \| Usage ... Limit reached`; HUD usage line misread as quota-blocked pane | fixed in `agent-wakeup` (ignore Claude HUD usage statusLine; use explicit quota/menu markers only) |
| [`claude-plans-here-misses-global-plans-jsonl-schema`](claude-plans-here-misses-global-plans-jsonl-schema.md) | `claude-plans-here` prints only `Wrote .../.claude/settings.json`; project `.claude/plans/` stays empty; matching plan still under `~/.claude/plans/*.md`; JSONL uses `toolUseResult.filePath` / `ExitPlanMode.planFilePath` | fixed (find + jq parser; Bats fixture coverage) |
| [`codex-config-toml-emit-unsupported-scalar-dict`](codex-config-toml-emit-unsupported-scalar-dict.md) | `chezmoi apply` prints a Python traceback with filename-less `File "<stdin>", line NN` frames ending in `TypeError: unsupported scalar type for TOML emit: dict`, then `chezmoi: .codex/config.toml: exit status 1`; every other file applies fine | fixed (the `modify_` script's hand-rolled TOML writer now emits arrays-of-tables, so peon-ping's `[[hooks.*]]` blocks round-trip; regression test added) |
| [`codex-model-metadata-not-found-after-copilot-refresh`](codex-model-metadata-not-found-after-copilot-refresh.md) | `Model metadata for gpt-5.6-sol not found` / `Defaulting to fallback metadata; this can degrade performance and cause issues.` even though Copilot `/responses` returns HTTP 200; bundled debug catalog contains Sol but normal debug catalog does not; `~/.codex/models_cache.json` contains only three Gemini adapter records | fixed locally (`codex-copilot` pins a versioned bundled catalog through `model_catalog_json` while retaining live gateway context/compaction limits; deleting the global cache alone is only temporary) |
| [`codeisland-auto-approves-permissionrequest`](codeisland-auto-approves-permissionrequest.md) | Claude Code shows `⎿ Allowed by PermissionRequest hook` / `⏺ Plan approved.` instantly with no notch popup, no native confirm dialog; `~/.claude/settings.json` contains a `PermissionRequest` hook pointing at `~/.codeisland/codeisland-hook.sh` with `timeout: 86400` | workaround documented (toggle off auto-approve in the CodeIsland HUD app); repo-enforced subtractive removal designed but deliberately NOT wired up |
| [`copilot-api-caches-degraded-model-list-at-startup`](copilot-api-caches-degraded-model-list-at-startup.md) | every Claude Code request via the Copilot proxy dies with `400 model_not_supported`; `copilot-proxy logs` startup banner says `Models refresh: 13 new` and lists no `claude-*` ids; nothing changed in settings; a plain restart fixes it | workaround documented (`copilot-proxy doctor` diffs cached vs live upstream list → `copilot-proxy restart`); fetch-once cache is upstream behaviour |
| [`copilot-proxy-ide-token-expired-after-refresh-backoff`](copilot-proxy-ide-token-expired-after-refresh-backoff.md) | proxy and shim still listen, `/v1/models` works, but every inference returns `401` / `IDE token expired: unauthorized: token expired`; earlier logs show `Failed to refresh Copilot token` from `ENOTFOUND api.github.com`, connect timeout, or GitHub HTML error pages, with `Retrying ... in 600s`; restart immediately restores HTTP 200 | upstream refresh loop exists but retains the expired token during backoff; workaround = `copilot-proxy restart`; reactive shim self-heal designed but not implemented |
| [`copilot-proxy-openai-model-silent-stall`](copilot-proxy-openai-model-silent-stall.md) | a `gpt-*`/`gemini-*` agent through the proxy shows `0 tok` for minutes and sometimes never recovers (Esc + `continue` is the only way out); backend log shows only `200`s, or a `<-- POST` with no matching `--> POST`; occasionally surfaces as `API Error: Connection lost mid-response` | mitigated (shim commits the SSE response early and sends `: keepalive` comment frames through the silent think/queue window; `COPILOT_SHIM_STALL_MS` bounds each attempt). `0 tok` alone is a FleetView display artifact, not this bug |
| [`copilot-proxy-shim-eaddrinuse-stale-build`](copilot-proxy-shim-eaddrinuse-stale-build.md) | `copilot-proxy: shim did not come up — check .../copilot-shim-4142.log` and every managed launcher refusing with `managed client refused to bypass the enabled metrics shim`; `status` insists `shim: ON but DOWN` and `copilot-proxy shim on` changes nothing; the **proxy's** log (:4141) is flooded with `GET /_shim/health 404`, and only the shim's own log names `EADDRINUSE` | fixed (`_copilot_shim_start` reads the port with `lsof` instead of inferring it from the health probe: reclaims a stale/old-build `copilot-throttle-shim.js`, names any other squatter and refuses; failure now tails the shim log inline) |
| [`copilot-proxy-stale-package-lock-integrity`](copilot-proxy-stale-package-lock-integrity.md) | every `copilot-proxy start`/`restart` dies at `copilot-proxy: installed package integrity does not match the trusted pin.` right after the install banner, so the proxy never launches (`logs` says `no log file`) and `codex-copilot`/`claude-copilot` all fail closed — yet `node_modules/@jeffreycao/copilot-api/package.json` holds the correct pinned version | fixed (a `package-lock.json` left by the npm CA-stack fallback survives every later `bun add`, which writes only `bun.lock`; the guard was comparing the OLD version's genuine hash against the new pin. Lock reads are now version-gated, else the check falls back to registry metadata for the **installed** version). Manual recovery: `rm ~/.local/share/copilot-api/pkg/package-lock.json` |
| [`copilot-proxy-start-hangs-at-resolving-dependencies`](copilot-proxy-start-hangs-at-resolving-dependencies.md) | `copilot-proxy: did not come up in time — check 'copilot-proxy logs'.`; `copilot-proxy logs` shows only `nohup: ignoring input` + `Resolving dependencies`; wedged `bun add @jeffreycao/copilot-api@… --no-summary` in `ps`; every retry stacks another zombie pair and none binds port 4141; `curl` to the npm registry works fine so it looks like a non-network fault | fixed (runner installs the pinned pkg once into `~/.local/share/copilot-api/pkg` and execs the binary — no per-launch `bunx` resolve; install is timeout-killed so it can't hold bun's cache lock; `start` reaps its own server on timeout). `doctor` still flags a stale installer as a safety net |
| [`docker-proxy-set-but-docker-info-shows-empty`](docker-proxy-set-but-docker-info-shows-empty.md) | `/etc/systemd/system/docker.service.d/http-proxy.conf` exists and looks correct, but `docker info` reports `HttpProxy: ""` / `HttpsProxy: ""`; `docker pull` of ghcr/gcr images behaves as if no proxy were set; `systemctl is-enabled docker.service` → `disabled` while `$DOCKER_HOST` points at `/run/user/$UID/docker.sock`; `/etc/docker/daemon.json` equally ignored | detected by `docker-net doctor` (rootful config orphaned by the rootless pivot); cleanup stays advisory so a switch back to rootful is not sabotaged |
| [`docker-pull-fails-dead-registry-mirrors`](docker-pull-fails-dead-registry-mirrors.md) | `Error response from daemon: unknown: failed to resolve reference "docker.io/library/…": unexpected status from HEAD request to https://<mirror>/v2/…: 403 Forbidden` — reads like the image does not exist; pulls that take 30–60 s or fail intermittently; `curl: (6) Could not resolve host: docker.mirrors.ustc.edu.cn`; `curl: (35) OpenSSL SSL_connect: SSL_ERROR_SYSCALL … mirror.baidubce.com:443` | fixed — 4 of 5 managed mirrors measured dead (2026-07) and removed; `docker-net mirrors` re-measures, `docker-net pull` adds a client-side rung for images no mirror carries |
| [`editor-overlay-flattens-empty-settings`](editor-overlay-flattens-empty-settings.md) | Cursor/VSCode/Antigravity loses `workbench.colorTheme` + `workbench.iconTheme` after `chezmoi apply`; `settings.json` collapses to ~286 bytes = only the 6 overlay keys; reverts to `Dark Modern` + no file icons; user blames the Brewfile cask reinstall (it's not); sibling editors untouched | by design (`jq '. * $overlay'` on an *empty* base yields overlay-only); recovery + optional guard documented |
| [`escape-escape-chord-never-completes`](escape-escape-chord-never-completes.md) | wanted "press Esc twice to interrupt" in Claude Code, so bound `"escape escape": "chat:cancel"` in `~/.claude/keybindings.json` — now **Esc does nothing at all**: no interrupt, no `Esc again to clear`, no empty-prompt rewind; **no error**, no validation warning, `/doctor` clean, action + context names both valid; no settings key exists for the behaviour either | by design — `escape` is the hardcoded chord-**cancel** key (`if(e.name==="escape"&&n!==null)return{type:"chord_cancelled"}`), so it can never be a chord's 2nd key, and as a 1st key it `preventDefault`s Esc away from the components that implement it; workaround = `{"escape": null}` (unbound keys still propagate) + interrupt with Ctrl+C, or vim editor mode |
| [`fleet-apply-password-source-not-for-ssh-login`](fleet-apply-password-source-not-for-ssh-login.md) | `just fleet-status` marks a host ✗ `unreachable` with `PermissionDenied: Permission denied for user <user> on host <ip>`, and `just fleet-apply --hosts <host>` dies the same way at the readiness preflight — while `ssh <host>` interactively works fine (it just **prompts for a password**); every other host in `~/.config/fleet/machines.toml` succeeds on key/agent auth; the failing entry already has `password_source = { type = "plain", value = "..." }` (or `bitwarden`/`prompt`) which you reasonably assumed covers the SSH login, but that field only ever becomes `CHEZMOI_SUDO_PASSWORD_FILE` for remote sudo; secondary trap — `ssh-copy-id` reports `Number of key(s) added: 1` and the `authorized_keys` entry IS on the remote, yet the next login still prompts because `ssh -v` shows `Authentications that can continue: gssapi-keyex,gssapi-with-mic,keyboard-interactive` with `publickey` absent (`PubkeyAuthentication no`), and `ssh -o BatchMode=yes <host> true` errors `Permission denied (publickey,password)` | conditionally fixed 2026-07-02 (opt-in per-host `ssh_login_password_source`, tried only after key/agent auth is rejected, implemented with asyncssh's native `password=` + `preferred_auth=("keyboard-interactive", "password")` so PAM-only sshds work with no `sshpass`/`expect` dependency); `password_source` staying sudo-only and the password fallback staying opt-in are by design (security posture + parallel-apply has no TTY), so `ssh-copy-id` / asking the admin for `PubkeyAuthentication yes` remains the preferred fix |
| [`fleet-apply-self-stuck-running`](fleet-apply-self-stuck-running.md) | `just fleet-apply` finishes every remote host (`rc=0`, `state=done`) but the `self` (local) row ticks `elapsed` forever at `state=running`, `rc=-` (2902s, 3359s while remotes took seconds); the `last` column is frozen on an early chezmoi line such as `Already up to date.`, so chezmoi visibly finished long ago; Ctrl+C gives `File ".../fleet_apply.py", line 699, in run_one_local / await asyncio.gather(` → `line 688, in _drain / line = await stream.readline()` → `asyncio.exceptions.CancelledError`; `pgrep -af 'chezmoi\|ansible\|brew'` shows orphaned `brew bundle` / `community.general.homebrew` / `mas install` / ble.sh-build children that outlived chezmoi and still hold the inherited stdout/stderr write end, so the pipe never EOFs; only `local = true` hosts are affected | fixed (option 3 — `os.kill(pid, 0)` existence poll at 100ms for chezmoi's real exit, then a 5s grace-cancel of the `_drain` tasks, with `os.waitpid(WNOHANG)` as last resort; 60s synthetic repro now returns in 2.1s with output intact). The obvious `asyncio.wait(..., FIRST_COMPLETED)` on `proc.wait()` is a **false summit** — asyncio only fires `_process_exited` once returncode is set AND all pipes are closed, so `proc.wait()` blocks exactly as hard as `_drain`. Remote hosts were always immune thanks to the `wait $_cz_pid` + sentinel bash wrapper |
| [`git-delta-empty-stdin-huge-allocation`](git-delta-empty-stdin-huge-allocation.md) | `delta` / `printf '' \| delta` crashes with `memory allocation of 1970324836974592 bytes failed`; `delta --version` still works; `something is very wrong if the default theme is missing` | fixed (clear incompatible bat cache + delta stdin health check) |
| [`git-lfs-global-hook-breaks-brew-tap`](git-lfs-global-hook-breaks-brew-tap.md) | `brew tap` / `brew bundle` fails: `This repository is configured for Git LFS but 'git-lfs' was not found on your path`, `git clone ... exited with 2`, `Tapping <tap> has failed!`, then `Cask '<name>' is unavailable: No Cask with this name exists`; git-lfs IS installed; stock git-lfs hook in the global `core.hooksPath` `exit 2`s under Homebrew's stripped superenv PATH | fixed (chezmoi-managed git-lfs hooks made PATH-robust + non-fatal) |
| [`go-install-gobin-resets-to-mise-toolchain-dir`](go-install-gobin-resets-to-mise-toolchain-dir.md) | `go env GOBIN` = `~/.local/share/mise/installs/go/<ver>/bin` not `~/.local/bin`; `go install`'d CLIs vanish after a `go` version bump; shell `export GOBIN=~/.local/bin` reverts after every `cd`; `mise settings ls --all \| grep go` shows `go_set_gopath`/`go_set_goroot` but no `go_set_gobin` | fixed (override `GOBIN` in mise's own `[env]`; mise force-sets it via hook-env with no toggle) |
| [`headless-claude-p-does-not-move-5h-window`](headless-claude-p-does-not-move-5h-window.md) | scheduled `claude -p 'Reply: hi'` warmup runs clean (exit 0) but the Claude Pro/Max 5-hour window never starts; `/usage` shows no active window at warmup time; metered Agent SDK credit consumed instead of subscription; `--bare` forces `ANTHROPIC_API_KEY` | by design (2026-06-15 billing split) — `agent-warmup` drives an interactive tmux session instead |
| [`herdr-0.7.4-drops-custom-status`](herdr-0.7.4-drops-custom-status.md) | after a herdr upgrade `prefix+m` / `hmark` die with `error: unexpected argument '--custom-status' found`; `tv herdr-review` / `prefix+i` opens EMPTY with no error; pre-existing ⭐ vanish from the sidebar; restarting the server does NOT help | fixed (migrated to the 0.7.4 `--token NAME=VALUE` / `--clear-token` map across review-mark.sh + herdr-review.toml + a pinned `[ui.sidebar.agents] rows` with `$review` — tokens render ONLY where a row layout names them). Silent removal: not listed as a breaking change upstream |
| [`herdr-brew-upgrade-strands-running-server`](herdr-brew-upgrade-strands-running-server.md) | after `brew upgrade herdr` every `herdr` CLI call returns `{"error":{"code":"protocol_mismatch"}}` (`client protocol 17 is newer than server protocol 16`) while the TUI works fine; **exit code still 0**; `tv herdr-sesh` / `herdr-agent-panes` / `herdr-review` open empty; `hvibe`/`hcode` and the `prefix+m/p/u/P/D/V/S` helpers do nothing; `herdr status` shows `compatible: no` + `restart_needed: yes`; `herdr --version` misleadingly reports the NEW version | by design (fail-closed protocol gate) — and `herdr update --handoff` is **disabled on Homebrew/mise/Nix installs**, so macOS has no pane-preserving path; wait for a restart point, `herdr server stop` + reattach (topology + agent sessions restore, processes die), or `brew pin herdr` |
| [`herdr-update-handoff-refuses-inside-pane`](herdr-update-handoff-refuses-inside-pane.md) | `herdr update --handoff` exits immediately with `update failed: run herdr update outside herdr after detaching from the session`, before any `checking stable channel` line; the same command works from another terminal seconds later; a coding agent shelling out can never fix it, because the guard reads `HERDR_ENV` and not the invocation | by design (the handoff replaces the server that owns your pane) — detach `prefix+q` → update from a plain terminal → reattach with bare `herdr`; the handoff does preserve running panes. `just upgrade-herdr` now skips-with-instructions rather than failing |
| [`hhere-p-relative-path-opens-workspace-at-home`](hhere-p-relative-path-opens-workspace-at-home.md) | `hhere -p ../sibling` exits 0 with no error but the new workspace's shell sits in `$HOME`; **absolute** `-p ~/foo` works fine so the flag looks functional; the space is still *labelled* by the basename, so nothing renames it to `~`; `hcode -p ../foo` / `hvibe -p ../foo` instead misreport it as `not inside a git repo`; `herdr pane list` shows `cwd=/Users/<you>` | fixed (`_herdr_abs_dir` absolutizes `-p` client-side + hard-errors on a non-directory) — herdr resolves `--cwd` **server-side** against the server's launch dir and silently falls back to `$HOME` on a miss; tmux's `new-session -c` is client-resolved, which is why the `shere`/`scode`/`svibe` originals never needed it |
| [`homebrew-6-refuses-untrusted-tap-formula`](homebrew-6-refuses-untrusted-tap-formula.md) | `chezmoi apply` dies at `devtools : Install developer CLI tools (macOS)` with `Error: Refusing to load formula raine/workmux/workmux from untrusted tap`; `Run \`brew trust raine/workmux\``; `brew tap-info <tap>` shows `Untrusted`; both `raine/workmux` and `dlvhdr/formulae` affected; worked days earlier | fixed (devtools role auto-`brew trust`s third-party taps; Homebrew 6.0 formula-trust gate) |
| [`homebrew-aliyun-brew-git-hang-core-clone-bloat`](homebrew-aliyun-brew-git-hang-core-clone-bloat.md) | any auto-updating `brew` cmd hangs 60s+ after `HOMEBREW_BREW_GIT_REMOTE set: using https://mirrors.aliyun.com/homebrew/brew.git`; `unset HOMEBREW_BREW_GIT_REMOTE` "fixes" it; `git ls-remote` fast but `git fetch` stalls; SEPARATELY `du -sh "$(brew --repo homebrew/core)/.git"` = ~1 GB after being ~8 KB | fixed (baseline Aliyun→BFSU across 3 layers + `brew-mirror`; `HOMEBREW_CORE_GIT_REMOTE` dropped — brew 4.x API mode) |
| [`ish-illegal-instruction-despite-sse2`](ish-illegal-instruction-despite-sse2.md) | `Illegal instruction` running `node`/`npm`/`rg`/`cargo` in iSH on iOS; `illegal instruction at 0x571b64ee: 66 0f 6a d0 66 0f 7e d0`; older builds say `Bad system call`; `nodejs and npm versions are not supported` when installing an agent CLI; `apk add` succeeded but the binary dies instantly | no workaround — iSH's CPUID DOES advertise sse2; the traps come from NULL gadget slots in `asbestos/gen.c` + 31 `UNDEFINED` points in `emu/decode.h`, so you cannot predict runnability from the instruction set. Upgrading Alpine to edge makes it strictly worse (x86 baseline raised to SSE2) |
| [`ish-tmux-ctrl-digit-dead-wrong-binding`](ish-tmux-ctrl-digit-dead-wrong-binding.md) | tmux `Ctrl+1`..`Ctrl+9` window switching does nothing inside iSH on iOS, while `Ctrl+2` / `Ctrl+6` fire the WRONG binding (arrive as legacy `C-Space` 0x00 / `C-^` 0x1E); `Shift+Enter` dead in Claude Code; mouse/touch scroll never reaches copy-mode despite `mouse on`; no error anywhere and `extended-keys` reports `on` | workaround documented (hand-trim `~/.tmux.conf` on the device) — iSH hardcodes `TERM=xterm-256color` so `terminal-features 'xterm*:extkeys'` matches, but hterm marks `CSI > Pm m` "Won't support" and iSH has no CSI-u path at all |
| [`linux-app-control-appimage-runtime-path`](linux-app-control-appimage-runtime-path.md) | `pkill -f /home/$USER/Applications/foo.AppImage` exits 0 (signal delivered) but the window stays open and `pgrep -f …AppImage` still lists 1–2 PIDs; `appquit Zen` / any `app-*` helper from `56_linux_apps.sh` reports success and the app keeps running; only the `appimagelauncher/binfmt-bypass` shim, the wrapper and `squashfuse` carry the `.AppImage` path — the real process is `/tmp/.mount_<hash>/<inner-binary>` (plus `-contentproc` children), and the mount hash (`.mount_remp6447…` vs `.mount_Frpc-D4MNzod`) is regenerated on every launch so it can't be hard-coded | fixed (register an explicit `--pkill='\.mount_.+/<inner>($\| )'` runtime-path pattern in `~/.config/shell/linux-apps.conf`; auto-derivation **deliberately** fails for AppImages so users are pushed to the override) |
| [`linux-app-control-gapplication-zero-coverage`](linux-app-control-gapplication-zero-coverage.md) | `gapplication list-apps` returns only ~10–20 entries and every one is GNOME-core (`org.gnome.Calendar`, `org.gnome.Nautilus`, `io.snapcraft.SessionAgent`, …); no browser / Electron / AppImage / Snap / Flatpak app appears even though they're installed, have `.desktop` files and launch fine via `gtk-launch`; `gapplication action <id> quit` works for `org.gnome.Calendar` but returns "no such app" for every third-party id; blog/SO answers presenting `gapplication` as the Linux analog of macOS Apple Events look right in isolation and collapse on a real desktop — measured coverage on Ubuntu 24.04 GNOME 46 Wayland was **0/5** (Zen / Cursor / Spotify / Discord / Frpc-Desktop), not "patchy" | by design — `list-apps` enumerates only `.desktop` entries with `DBusActivatable=true` and adoption is ~zero outside GNOME core; hybrid stack documented instead (`gtk-launch`/`gio launch` to start, `pkill -TERM` to quit, GNOME `window-calls` `gdbus …Activate(<WM_CLASS>)` to raise, `playerctl`/MPRIS for media) |
| [`lazygit-ctrl-o-no-clipboard-utilities-nvim-yank-silent`](lazygit-ctrl-o-no-clipboard-utilities-nvim-yank-silent.md) | lazygit `Ctrl+O` → `No clipboard utilities available. Please install xsel, xclip, wl-clipboard or Termux:API add-on for termux-clipboard-get/set.`; **paired** symptom — Neovim `yy` / `"+y` puts nothing on the system clipboard, `:checkhealth provider.clipboard` → `No clipboard tool found`; `x copy` silently uses only its OSC 52 fallback and `x copy-file` errors with `no file clipboard backend found`; local Ubuntu desktop (Wayland/`niri` or X11), not SSH; CopyQ is installed and running yet none of the three apps see a clipboard | fixed (no ansible role installed `wl-clipboard`/`xclip`/`xsel` — CopyQ's `copyq` CLI is a history manager, not what lazygit's `atotto/clipboard` / nvim's provider probe / `x` shell out to; `gui_apps_linux` now installs all three on `ubuntu_desktop`, and lazygit's `Ctrl+O` is additionally routed through `x copy` for SSH). Manual: `sudo apt install wl-clipboard xclip xsel` |
| [`macos-swap-files-never-shrink`](macos-swap-files-never-shrink.md) | About > General > Storage shows "System Data" / "Other Volumes" growing 1–3 GB/day with no new user files (`du -sh ~` stable); Activity Monitor Memory Pressure is green/yellow (OS "coping") while `Swap Used: 17.59 GB`; a reboot instantly reclaims 15–20+ GB and the pattern repeats every few days; `ls -lh /System/Volumes/VM/swapfile*` (Apple Silicon/modern Intel) or `/private/var/vm/swapfile*` (older Intel) lists many 1 GB files; `sysctl vm.swapusage` `total` only ever grows across days while `used` fluctuates; `sudo rm /System/Volumes/VM/swapfile*` "succeeds" but `df -h /` is unchanged — zero space reclaimed | by design (Apple never deletes swapfiles on a running system and exposes no API to shrink them — reboot is the only fix); repo workaround `mac-mem-status` / `mac-mem-reclaim` reclaims *adjacent* storage (purge, Spotlight, TM snapshots, sleepimage) and deliberately has **no** `--include swapfiles`; `nvram vm_compressor=2` and `dynamic_pager -L 0` hard-hang, `rm` orphans the inode |
| [`macos-tcc-tmux-volumes-permission-denied`](macos-tcc-tmux-volumes-permission-denied.md) | `ls /Volumes/Data/...` in Alacritty or an attached old tmux session prints `Permission denied: . - code: 13` / `Skipped 1 directories due to permission denied: .`, while Cursor can read the same path and Unix perms are normal | workaround documented (Full Disk Access belongs to the app that started the tmux server; kill old server and restart tmux from the authorized app) |
| [`macfuse-too-old-unsupported-macos-version-rclone-mount`](macfuse-too-old-unsupported-macos-version-rclone-mount.md) | macFUSE dialog `Unsupported macOS Version — The installed version of macFUSE is too old for the operating system` on boot / `rclone mount`; `rclone mount --help` works fine (binary not the cause); `brew info --cask macfuse` reports 5.x but Caskroom has 4.x; `brew upgrade` doesn't fix (cask is `auto_updates`) | by design (kext version-locked to macOS major); manual `brew reinstall --cask macfuse` + reboot + GUI approval; not repo-managed |
| [`mise-rust-cargo-shim-dead`](mise-rust-cargo-shim-dead.md) | `chezmoi apply` dies in `rust_cargo_tools` with `mise ERROR cargo is not a valid shim. This likely means you uninstalled a tool and the shim does not point to anything.` (or `mise ERROR cargo is a mise bin however it is not currently active`); `which cargo` → `cargo not found` and `~/.cargo/bin/cargo` is gone; `mise ls rust` prints `1.95.0 (missing)` while `~/.local/share/mise/installs/rust/<ver> -> ~/.cargo/bin` is a dead symlink; the `Install Rust via mise` task reports `ok` in ~0.2s (skipped by its `creates:` guard, never actually run); mirror variants `error: the server unexpectedly provided an obsolete version of the distribution manifest` and `error: could not download nonexistent rust version 1.96.0-x86_64-unknown-linux-gnu` / TUNA `.tar.gz.sha256` 404; cascading form takes down *unrelated* `mise exec -- npm install -g …` in `coding_agents` / `js_cli_tools` because `mise exec` auto-installs every missing config tool first | fixed (guard the mise rust install on `creates: ~/.cargo/bin/cargo` — the real backing binary, not the `installs/rust` dir — plus try the configured mirror then fall back to `RUSTUP_DIST_SERVER=https://static.rust-lang.org`; `lazyvim_deps` retries `mise install --yes` against the official server before the npm roles run) |
| [`mkdocs-copy-to-llm-custom-css-no-trailing-newline`](mkdocs-copy-to-llm-custom-css-no-trailing-newline.md) | `git status` always shows `docs/assets/copy-to-llm/copy-to-llm-custom.css` modified after `mkdocs build` / `mkdocs serve`; diff is just `-}` / `+}` `\ No newline at end of file`; `end-of-file-fixer` adds `\n` back, next build strips it; ping-pong forever | workaround documented (gitignore + `git rm --cached`); upstream PR not yet sent |
| [`modify-script-jq-bootstrap-cycle`](modify-script-jq-bootstrap-cycle.md) | `jq: command not found` / `chezmoi: .agents/.skill-lock.json: exit status 127` immediately after `[SUCCESS] Bootstrap complete!` on a fresh box; subsequent applies stuck in same loop because ansible never reached | fixed (`command -v` guards added to all 7 `modify_*` scripts that needed them) |
| [`ngrok-cask-download-flaky`](ngrok-cask-download-flaky.md) | `networking_tools : Install ngrok via Homebrew` fails after ~1m39s with `Download failed on Cask 'ngrok' with message: Download failed: https://bin.ngrok.com/a/<token>/ngrok-v3-<ver>-darwin-amd64.zip` / `curl: (52) Empty reply from server`; networking is otherwise healthy — Homebrew updated fine, cloudflared and every other cask in the same play installed; the identical URL returns HTTP 200 with the full 12 MB payload when retried by hand minutes later, and the per-build token keeps working across the failure | fixed (transient Heroku-fronted CDN flake, not our network/config — `which ngrok` pre-check plus `until` / `retries: 3` / `delay: 15` on the homebrew task; the existing `rescue:` still warns and continues so optional ngrok never fails the apply). Don't switch to a manual `unzip` (same CDN) or add a `wait_for` TCP probe (connects fine while HTTP returns 52) |
| [`raycast-library-files-not-portable-across-machines`](raycast-library-files-not-portable-across-machines.md) | rsynced / tar-piped / Dropbox-synced `~/Library/Application Support/com.raycast.macos/` onto a second Mac and Raycast launches with an EMPTY Extensions list — hotkeys, quicklinks, snippets, aliases all gone, as if a fresh install; or an opaque "failed to open database" style error on launch; `file raycast-enc.sqlite` reports a regular SQLite DB but `sqlite3 raycast-enc.sqlite .tables` fails `file is encrypted or is not a database`; same for `raycast-activities-enc.sqlite` / `raycast-emoji.sqlite`; symlinking the dir into iCloud Drive / Dropbox silently breaks Raycast on Sonoma+ (atomic rewrites orphan the link) | by design — the `-enc.sqlite` key is SHA-256-derived and lives in that machine's login Keychain, so the bytes are useless elsewhere; supported path = first-party `.rayconfig` Export/Import, wired here as `just raycast-export` / `just raycast-import` + `scripts/raycast_sync.py` (extension API keys / OAuth tokens are separately Keychain-scoped and must be re-entered per machine), or pay for Raycast Pro Cloud Sync |
| [`redact-secrets-loop-with-active-specstory-writer`](redact-secrets-loop-with-active-specstory-writer.md) | `git commit` of a SpecStory transcript loops: `Auto-redact secrets in agent artifacts...Failed` / `hook id: redact-agent-secrets` / `files were modified by this hook` / `Successfully redacted 1 file(s)` immediately followed by `Detect hardcoded secrets...Failed` / `hook id: gitleaks-system` / `RuleID: jwt`; the usual `git add file && git commit` re-run does NOT clear it — the redacted file on disk reverts to the original after each attempt; `grep -c "eyJhbGci" file` stays non-zero and *increases* per attempt; `just add-and-redact` reports N redacted and the next grep shows N back plus new ones; the leaked JWT is one the user never typed (came from `ps -axo args` capturing SpecStory's own `specstory_darwin_* watch --cloud-token …` daemon line); `lsof <transcript>` lists `specstory_darwin_*` as a live writer | no permanent fix — workaround = redact WITHOUT printing the match, then `git add` + `git commit` in one bash command so the index freezes before the daemon appends again; every diagnostic (`grep`, `cat\|head\|tail`, `sed -n Np`, `git diff` of the file) re-injects the secret into the live transcript. A real fix needs a SpecStory pause/resume API or a transactional pre-commit redact |
| [`resilio-sync-http-cdn-blocked-and-shell-pipefail-dash`](resilio-sync-http-cdn-blocked-and-shell-pipefail-dash.md) | `chezmoi apply` / `ansible-playbook --tags resilio_sync` fails at `resilio_sync : Add Resilio Sync GPG key (dearmored)` in two back-to-back modes: a ~30-minute hang (`Slow tasks (>5s): resilio_sync : Add Resilio Sync GPG key (dearmored) (29m32s)`) ending in `gpg: no valid OpenPGP data found.`, then after switching to curl an instant (~2 ms) `/bin/sh: 1: set: Illegal option -o pipefail`; the role's `rescue:` removes the apt source so the service tasks skip and `systemctl --user status resilio-sync` reports `Unit resilio-sync.service could not be found`; `curl http://linux-packages.resilio.com/resilio-sync/key.asc` returns `000` after 20 s (IPv6 Meta/Fastly endpoint, port 80 dead) while `https://` returns `200` in ~1 s | fixed (key **and** deb822 `uris:` moved to `https://`, fetched with `curl -fsSL --retry 3 --retry-delay 2 --max-time 60`, plus `args.executable: /bin/bash` on the shell task) — bare `wget` has no total timeout and retries ~20× into the dead endpoint, then hands empty stdin to `gpg`; and `ansible.builtin.shell` runs under Ubuntu's dash, where `set -o pipefail` is a bashism. Trap 2 applies to *every* `set -eo pipefail` shell task in this repo |
| [`tmux-appimage-terminfo-not-found`](tmux-appimage-terminfo-not-found.md) | `tmux` / `tmux attach` / `tmux new` exits `missing or unsuitable terminal: xterm-256color` while the SAME binary's `tmux ls` / `new-session -d` / `kill-session` work fine (only the client/attach path needs terminfo); `/bin/tmux` (apt 3.2a) works; `~/.local/bin/tmux -V` prints a version OK; `infocmp xterm-256color` from the host shell works; `$TERM` is the normal `xterm-256color`; `strace -f -e openat …/squashfs-root/usr/bin/tmux new-session` shows **zero** `openat` calls touching any terminfo path — lookup never even started | fixed (the `devtools` role's `~/.local/bin/tmux` wrapper now exports `TERMINFO_DIRS="${TERMINFO_DIRS:-/etc/terminfo:/lib/terminfo:/usr/share/terminfo}:$appimage_root/usr/share/terminfo"` before `exec`-ing the bundled binary — system paths first so a newer host ghostty/kitty terminfo wins) — the AppImage's bundled `libncurses.so.6` hardcodes the FUSE-mount terminfo path at build time and does NOT fall back to the standard dirs once `--appimage-extract`ed; generalises to any extracted AppImage bundling ncurses / gconv / locale data |
| [`tmux-continuum-restores-on-intentional-quit`](tmux-continuum-restores-on-intentional-quit.md) | closed tmux on purpose (`kill-server` / "Kill all sessions" / `prefix + M-x` on last session) and the just-killed sessions come back on next `tmux` start; `@continuum-restore 'on'` can't tell intent from crash; `~/.local/share/tmux/resurrect/last` symlink still points at the pre-kill snapshot | workaround documented (clean-quit wrappers `kill-server-clean.sh` / `kill-session-exit.sh` rm the `last` symlink before server dies; crash recovery preserved via untouched history) |
| [`tmux-copy-mode-double-free-3.6a`](tmux-copy-mode-double-free-3.6a.md) | `[server exited unexpectedly]` after long-running session when entering copy-mode; `.ips` shows `___BUG_IN_CLIENT_OF_LIBMALLOC_POINTER_BEING_FREED_WAS_NOT_ALLOCATED` with stack `cmd_copy_mode_exec → window_copy_clone_screen → screen_reinit → grid_clear_lines → grid_free_line`; homebrew bottle `slice_uuid: 1b2d8e7b-bb77-3465-8bab-73f5bbaffe49`; terminal-agnostic | fixed upstream ([`035a2f3`](https://github.com/tmux/tmux/commit/035a2f35d40628dcfe235179220fc0ede848a195) closing [#4777](https://github.com/tmux/tmux/issues/4777)) but not in any tagged release; workaround = `brew install --HEAD tmux` until 3.7 ships |
| [`mrun-autoname-collides-zsh-random-subshell`](mrun-autoname-collides-zsh-random-subshell.md) | back-to-back `mrun` / `tmrun` / `zjrun` without `-n NAME` produce the same `run-<dir>-<hex>` suffix; second call errors `tmux session 'run-X-NNNN' already exists`; `zsh -c 'echo $(echo $RANDOM) $(echo $RANDOM)'` returns identical numbers | fixed in `dot_config/zsh/tools/23_mrun.zsh` (read `$RANDOM` into a parent-shell local before the `$(…)` boundary) |
| [`niri-spawn-cargo-bin-not-on-session-path`](niri-spawn-cargo-bin-not-on-session-path.md) | niri `Mod+T` / any `spawn "<tool>"` opens nothing, no error; binary is in `~/.cargo/bin` (works in your shell) but not on the systemd/Wayland session PATH; the `ln -s … /usr/local/bin` "fix" breaks multi-user | fixed (per-user `environment { PATH … }` in `config.kdl.tmpl`, not a system symlink) |
| [`npm-postinstall-github-releases-hang`](npm-postinstall-github-releases-hang.md) | `npm install -g` hangs after "Downloading https://github.com/.../releases/..." | workaround documented |
| [`skilloverrides-does-not-hide-plugin-commands`](skilloverrides-does-not-hide-plugin-commands.md) | a Claude Code plugin's commands (`claude-hud:setup`, …) keep appearing in every session's `The following skills are available for use with the Skill tool:` listing after adding them to `skillOverrides`; **no error** — keys are accepted, persisted, JSON stays valid, `/doctor` clean; `"off"` and `"user-invocable-only"` both inert; the same mechanism demonstrably works for user skills in `~/.claude/skills/` | by design — `skillOverrides` is user-skills-only (the listing filter keys on a raw name ≠ the displayed `plugin:command`); workaround = `enabledPlugins: {"<plugin>@<marketplace>": false}`, verified safe for claude-hud (statusLine globs the cache path directly) |
| [`sudo-shared-setsid-macos`](sudo-shared-setsid-macos.md) | `[bootstrap]` / `[ansible]` / `[brew]` sudo prompts fire 3× in one `chezmoi apply` on macOS despite "entered once, reused" wording; `keepalive.pid` points to a dead PID; `which setsid` → not found | fixed (`_sudo_spawn_watchdog` falls back to `nohup` when `setsid` is absent) |
| [`nvidia-nvml-driver-library-mismatch-after-unattended-upgrade`](nvidia-nvml-driver-library-mismatch-after-unattended-upgrade.md) | `Failed to initialize NVML: Driver/library version mismatch`; `cuInit` returns 804 / `CUDA_ERROR_COMPAT_NOT_SUPPORTED_ON_DEVICE`; `torch.cuda.is_available()` suddenly False; a running training job is unaffected while every NEW CUDA process fails; `modprobe -r nvidia` → `Module nvidia is in use`; `/proc/driver/nvidia/version` disagrees with `dpkg -l \| grep nvidia-utils`; nobody ran an upgrade | prevented (`52unattended-upgrades-local` blacklist + `nvidia-driver-drift-check`); underlying behaviour is by design — only a reboot fixes it |
| [`nvim-fs-find-enoent-stale-cwd`](nvim-fs-find-enoent-stale-cwd.md) | nvim 0.12 startup `vim/fs.lua:0: ENOENT` from avante / lualine / lazy checker; `[C]: in function 'assert'` | workaround documented (`cd` to a real dir before launching) |
| [`ollama-brew-link-fails-cask-shadows-formula`](ollama-brew-link-fails-cask-shadows-formula.md) | `chezmoi apply` aborts at `llm_tools : Install Ollama via Homebrew formula` with `Error: The \`brew link\` step did not complete successfully` / `Could not symlink bin/ollama` / `Target /usr/local/bin/ollama already exists`; `ollama` formula installed-but-unlinked, `shadowed by /usr/local/bin/ollama`; the target is a symlink into `Ollama.app` (cask `ollama-app`); roles after `llm_tools` silently skipped | fixed (`which ollama` pre-check gates the formula install in `dot_ansible/roles/llm_tools`) |
| [`opencode-concurrent-launch-wal-pragma-fails`](opencode-concurrent-launch-wal-pragma-fails.md) | `Failed to run the query 'PRAGMA journal_mode = WAL'`; one of N simultaneously-launched `opencode` TUIs exits on startup (`svibe N opencode`); log shows several `opencode.db opening database` within ms | upstream concurrency limit; mitigation = stagger launches (`SVIBE_LAUNCH_STAGGER`) |
| [`opencode-docker-opentui-glibc-loader-missing`](opencode-docker-opentui-glibc-loader-missing.md) | `docker run ... ghcr.io/anomalyco/opencode` exits with `Failed to initialize OpenTUI render library: Failed to open library "/tmp/.<hex>-00000001.so": Error loading shared library ld-linux-x86-64.so.2: No such file or directory`; image is Alpine/musl, OpenTUI native lib needs glibc | workaround = `--entrypoint sh ... -c 'apk add --no-cache gcompat && exec opencode "$@"'`; upstream image bug as of 1.15.11 |
| [`opencode-tool-execution-aborted-on-long-write`](opencode-tool-execution-aborted-on-long-write.md) | OpenCode TUI repeats `~ Preparing write...` / `Tool execution aborted` (sometimes followed by `{"type":"api_error","message":"JSON error injected into SSE stream"}` and `Build · Claude Opus 4.x · interrupted`) when the agent tries to `write` a long file via `github-copilot` Claude; Claude Code does not reproduce | upstream root cause located 2026-05: post-write side-effects (`Effect.orDie` on LSP/format/BOM-sync) — PR #26347 ready for review, file may already be on disk despite abort message (verify before retrying); related orphan-tool prefill-400 cascade fixed in v1.15.11 |
| [`peon-hooks-wired-but-no-sound`](peon-hooks-wired-but-no-sound.md) | `agentSounds=peon`, apply clean, all 9 hook events present in `~/.claude/settings.json`, `peon status` healthy and `peon preview task.complete` audible by hand — yet nothing ever plays and Claude reports `… peon.sh … completed successfully`; `ls ~/.claude/hooks/peon-ping/` → `No such file or directory`; ansible log has `Error while resolving value for 'creates': 'ansible_env' is undefined` … `...ignoring` | fixed — `inject_facts_as_vars = False` means only `ansible_facts['env']['HOME']` exists (bare `ansible_env` is undefined and only fails at *run* time, not `--syntax-check`); plus `file: state: link` never creates parent dirs. Three silencers hid it: `ignore_errors`, the hook's own `\|\| true` guard, and `peon status` not covering the wiring. Guarded by the `ansible-no-injected-fact-vars` pre-commit hook |
| [`peon-ping-setup-escapes-home`](peon-ping-setup-escapes-home.md) | ran `peon-ping-setup` / `install.sh` with a faked `HOME=/tmp/...` to sandbox it, but it still created real files at `~/.config/opencode/plugins/peon-ping.ts`, `~/.config/opencode/peon-ping/config.json` and `peon-icon.png`; the installer's own summary prints absolute real paths outside the fake `HOME` | upstream behaviour — the OpenCode adapter root comes from `$XDG_CONFIG_HOME`, which doesn't follow a re-pointed `HOME`; override both (or use `--openpeon`), and never run the installer on a managed machine |
| [`pueue-macos-launchd-not-autostarted-no-socket`](pueue-macos-launchd-not-autostarted-no-socket.md) | macOS `pueue` / `pueue status` fails `Couldn't find a configuration file. Did you start the daemon yet?`, then after `brew services start pueue` (reports `Successfully started`) fails one step later `I/O error at path ".../Library/Application Support/pueue/pueue_<you>.socket" while connecting to daemon`; `ps` shows no `pueued`; `launchctl list homebrew.mxcl.pueue` = `"OnDemand" = true` + `"LastExitStatus" = 0` with NO `"PID"`; `~/.config/pueue/` doesn't exist (red herring — macOS uses `~/Library/Application Support/pueue/`); daemon runs fine by hand (`pueued -d`) | not a bug — Homebrew formula ships `keep_alive false` + RunAtLoad-only, launchd doesn't execute pueued after a `brew services` toggle that coincides with an upgrade/relink; workaround = `launchctl kickstart -k gui/$(id -u)/homebrew.mxcl.pueue` |
| [`raycast-clipboard-sqlcipher-key-not-recoverable`](raycast-clipboard-sqlcipher-key-not-recoverable.md) | Want to read Raycast clipboard history programmatically; `~/Library/Application Support/com.raycast.macos/raycast-enc.sqlite` is SQLCipher (binary contains `cipher_*` PRAGMAs); `security find-generic-password -s Raycast -a database_key -w` returns 64-hex; that key fails `Parse error: file is not a database (26)` for every `cipher_compatibility` / page_size / raw-vs-KDF combo on every `*-enc.sqlite` file; older plaintext `Clipboard.sqlite` schema (`ZCLIPBOARDHISTORYITEM`) no longer exists | UNSOLVED without RE'ing AppDelegate; workaround = switch to Maccy (open SQLite at `~/Library/Containers/org.p0deje.Maccy/`) |
| [`tailscale-another-copy-app-store-leftover`](tailscale-another-copy-app-store-leftover.md) | Tailscale debug → `Another Tailscale copy was found on this Mac.` / `/Applications/Tailscale.localized/Tailscale.app` / `onlyOneCopyExplainer` + `DNS Unavailable` `Code: dns-forward-failing`; `mdfind Tailscale.app` returns two paths (`io.tailscale.ipn.macsys` cask vs `io.tailscale.ipn.macos` App Store leftover); plus `brew upgrade` fights over `/usr/local/bin/tailscale` between the `tailscale` formula and the `tailscale-app` cask wrapper | fixed (`sudo rm -rf /Applications/Tailscale.localized` + `brew uninstall tailscale`; **cask-only** on macOS — the cask ships the CLI wrapper; never `brew services start tailscale`) |
| [`recovery-mode-resume-keeps-nomodeset-in-cmdline`](recovery-mode-resume-keeps-nomodeset-in-cmdline.md) | an iGPU / DRM device silently disappears after a troubled boot; `amdgpu … probe with driver amdgpu failed with error -22`; `card2` missing from `/sys/class/drm/`; module loaded with refcount 0 and nothing bound; the NVIDIA proprietary driver still works so nothing looks wrong; `amdgpu: ATPX version 1, functions 0x00000000` (red herring — appears on healthy boots too); `/proc/cmdline` has `recovery nomodeset dis_ucode_ldr` | not a bug — recovery-menu "resume" keeps the recovery cmdline; only `nomodeset` actually bites (`recovery`/`dis_ucode_ldr` aren't even recognised on kernel 7.0). Check `/proc/cmdline` BEFORE reading driver logs |
| [`specstory-custom-command-drops-configured-flags`](specstory-custom-command-drops-configured-flags.md) | `claude-copilot-once` (no args) runs in bypass-permissions mode but `claude-copilot-once --resume <id>` runs in `auto` mode; `/status` shows the permission mode coming from `~/.claude/settings.json` instead of the `--dangerously-skip-permissions` set in `claude_cmd`; no warning either way, `specstory check claude` reports `✅ All systems go!` in both | fixed (`_copilot_specstory_claude_cmd` rebuilds specstory's `-c` string from the configured `claude_cmd` — `-c` **replaces** that command, never appends to it) |
| [`specstory-run-default-agent-drift`](specstory-run-default-agent-drift.md) | `scode`/`svibe`/`hcode`/`hvibe`/yazi's "Claude (specstory)" opener silently launch **Antigravity CLI** instead of Claude after a specstory bump; `specstory run --help` now says `By default, launches Antigravity CLI.`; no error, `specstory check` reports `✅ All systems go!`, `claude_cmd` in `~/.specstory/cli/config.toml` untouched; no `default_provider` key exists anywhere and `[resume] last_agent` is a red herring; `specstory run --console --debug` shows `Provider registry initialized ... providers="[antigravity claude codex ...]"` then `Retrieved provider requested_id=antigravity` | fixed on our side (bare `specstory run` is never emitted — `_sesh_wrap_agent()` in `dot_config/shell/22_sesh.sh` names `claude` explicitly). Root cause: the no-arg default is the **alphabetically-first** registered provider, so 2.9.0 merely *adding* `antigravity` displaced `claude`; no upstream knob overrides it |
| [`tmux-display-menu-silent-fail`](tmux-display-menu-silent-fail.md) | `prefix + Space` / `prefix + e` doesn't open the popup menu, no error | workaround documented (script + height tiers) |
| [`tmux-extended-keys-always-paste`](tmux-extended-keys-always-paste.md) | pasting a multi-line snippet into bash + ble.sh under tmux mashes every line into one command with a literal `^[[106;5u` between them: `> uname -r^[[106;5uldd --version^[[106;5ugcc --version…`, then `uname: invalid option -- '['`, `bash: 5uldd: command not found...`, `[ble: exit 127]`; bracketed-paste markers are present but the inner newlines arrive CSI-u-encoded; zsh 5.9+ does not reproduce (its paste handler normalizes CSI-u back), so it only shows up with `primaryShell=bash` / ad-hoc bash + ble.sh | fixed (`set -s extended-keys on`, never `always`, in `dot_config/tmux/common.conf`) — `always` re-encodes EVERY modifier combo that already has a legacy encoding, including the Ctrl+J (LF) embedded in pasted text, as `ESC [ 106 ; 5 u`; `on` emits CSI-u only for keys with no legacy encoding so Shift+Enter / Ctrl+Enter / Ctrl+/ / Ctrl+digit still reach Claude Code, Neovim and tmux window-switching. Graduated to a CLAUDE.md Hard invariant |
| [`tmux-join-pane-numeric-target-pane-not-window`](tmux-join-pane-numeric-target-pane-not-window.md) | `Source and target panes must be different` in the status bar after answering a `command-prompt -p "Send pane to (target):"` with a bare window number (`1`), even though source and target are visibly in **different** windows; `join-pane` / `swap-pane` / `move-pane` all affected while `move-window -t` / `link-window -t` / `break-pane -t` accept the same bare `N` happily (the asymmetry is the confusion); a hand-typed `tmux join-pane -h -s @20 -t 1` may appear to work — an accident of which window was current; `#{window_id}` in the menu-row body is the red herring, `display-menu -t =` propagates the mouse target correctly | fixed (`command-prompt` → `choose-tree -Zw`, which returns an unambiguous `session:window`, plus pinning the source with `-s '#{pane_id}'`; `prefix + M/B/A` migrated to `choose-tree -Zs` in the same change) — a bare `N` on any `-t target-pane` resolves against the *current window's pane index*, so it often names the source pane itself |
| [`tmux-pane-bottom-blank-alacritty-macos-tabs`](tmux-pane-bottom-blank-alacritty-macos-tabs.md) | inside Alacritty + macOS native tabs **when window is maximised (green-button zoom)**, tmux status bar + a few rows of output paint at the top, then the entire bottom of the window stays empty; `tmux display -p '#{pane_height}'` reports the full height (e.g. 45); **un-maximise / drag corner** un-sticks instantly, **re-maximise reliably repros**; `tmux refresh-client` does NOT help (redraws using stale cached size); `$TERM = tmux-256color` (correct, not the cause) | confirmed 2026-05-10 (PTY size desync, maximise-state-specific); workaround = drop Alacritty native tabs / switch to Ghostty |
| [`tmux-submenu-flash-and-bottom-right`](tmux-submenu-flash-and-bottom-right.md) | nested `display-menu` via `run-shell` from right-click parent: flash, bottom-right placement, or selection silently does nothing on 2nd open | workaround documented (inline submenu items into parent) |
| [`tmux-pane-vanishes-on-ctrl-c-despite-shell-wrapper`](tmux-pane-vanishes-on-ctrl-c-despite-shell-wrapper.md) | tmux pane built with `cmd; exec $SHELL` wrapper closes on Ctrl+C inside btop/htop/less even though clean quit (`q`/`:q`) lands in shell as designed | fixed in `dot_config/zsh/tools/22_sesh.zsh` (`trap '' INT;` prefix in `_sesh_on_exit_wrap`) |
| [`tmux-resurrect-agents`](tmux-resurrect-agents.md) | tmux-resurrect doesn't restore coding-agent / TUI sessions after `kill-server` | known limitation |
| [`tmux-scrollback-tui-repaint-ghosting`](tmux-scrollback-tui-repaint-ghosting.md) | ghost / duplicated lines in tmux scrollback while Claude Code / OpenCode / live progress streams; flipbook frames visible in copy-mode | mitigated (`scroll-on-clear off` + `prefix + [` freeze workflow); fundamental limit |
| [`tmux2k-bandwidth-uint64-underflow`](tmux2k-bandwidth-uint64-underflow.md) | `18446744073709551615K` in tmux status bar | workaround documented |
| [`tmuxp-append-ignores-session-start-directory`](tmuxp-append-ignores-session-start-directory.md) | `sesh connect` opens repo session but `nvim`/`lazygit`/shell all start at `$HOME`; `#{session_path}` is correct, `#{pane_current_path}` is HOME; yaml `start_directory` silently dead | workaround documented (`cd <path> &&` prefix in `sesh.toml` startup_command) |
| [`tree-sitter-cli-empty-native-binary`](tree-sitter-cli-empty-native-binary.md) | `:TSInstall <lang>` fails with `spawn …/tree-sitter-cli/tree-sitter EACCES` on every parser; `tree-sitter --version` still prints a version; native binary is 0 bytes mode `0600` | workaround documented (stronger ansible check + `rm`+reinstall recovery) |
| [`tuna-nodejs-mirror-aggressive-gc`](tuna-nodejs-mirror-aggressive-gc.md) | every `mise use -g node@22` / `node@24` / `node@latest` dies with `mise ERROR Failed to install core:node@22:` + `HTTP status client error (404 Not Found) for url (https://mirrors.tuna.tsinghua.edu.cn/nodejs-release/v22.22.2/node-v22.22.2.tar.gz)` after `useChineseMirror=true` sets `MISE_NODE_MIRROR_URL` to TUNA; no version works, so it looks like a broken plugin rather than a missing file; browsing `nodejs-release/?C=M&O=D` shows only ~5 hand-picked versions instead of the full `nodejs.org/dist` archive (mise resolves the version list upstream but downloads from the mirror); on the rare version TUNA *does* have, a second failure: `mise ERROR gpg failed` / `gpg exited with non-zero status: exit code 2` / `gpg: key 0DDBF2B7: no valid user IDs` | workaround documented, deliberately no code fix (`export MISE_NODE_MIRROR_URL=https://mirrors.aliyun.com/nodejs-release/` in `~/.zshrc.adhoc` / `~/.bashrc.adhoc`, `MISE_NODE_VERIFY=0` for the gpg leg, or `unset MISE_NODE_MIRROR_URL`) — auto-fallback would need per-shell-startup HEAD probing of each candidate mirror; a completeness gap, not mirror lag |
| [`tv-binary-glibc-2-39-not-found`](tv-binary-glibc-2-39-not-found.md) | ``tv: /lib/x86_64-linux-gnu/libc.so.6: version `GLIBC_2.39' not found (required by tv)`` on every launch, and the same for ``tv --version`` (`required by /home/<user>/.local/bin/tv`) so the binary aborts before `main`; host is Ubuntu 22.04 jammy, `ldd --version` → `ldd (Ubuntu GLIBC 2.35-0ubuntu3.8) 2.35`; `file ~/.local/bin/tv` says `dynamically linked, interpreter /lib64/ld-linux-x86-64.so.2`; other Rust CLIs on the same box (`nvim`, `delta`, `yazi`) run fine, so it looks tv-specific | fixed (reinstall from the release page's musl artifact `tv-*-x86_64-unknown-linux-musl.tar.gz`; verify `static-pie linked` / `not a dynamic executable`) — not a tv bug but standard glibc forward-incompatibility from a 24.04 CI image; `do-release-upgrade` rejected as the wrong cost on a GPU box (see `docs/glibc-and-musl.md`) |
| [`tv-channel-bare-braces-break-substitution`](tv-channel-bare-braces-break-substitution.md) | TV channel preview shows literal `{split:\t:N}` instead of substituted values, no error | workaround documented (avoid bare `{...}` in heredocs; prefer external `executable_*.sh/py` helpers) |
| [`uv-self-update-homebrew-noop`](uv-self-update-homebrew-noop.md) | `just upgrade-tools uv` / `scripts/upgrade_tools.sh uv` prints `[INFO] Updating uv itself` then `warning: self-update is disabled for this build of uv` (version-dependent variants: `error: self-update is not available for this installation of uv`, ``error: uv was installed via Homebrew; use `brew upgrade uv` instead``) and **exits happily** while `uv --version` never moves — `uv 0.10.2 (Homebrew 2026-02-10)` stays months stale; a later `chezmoi apply` then trips the `python_uv_tools` role's `Fail if uv is too old after auto-upgrade attempt`; mixed-fleet tell: the same command upgrades the curl-installed Linux box fine and silently no-ops on the brew Mac; cheapest discriminator is the literal `(Homebrew YYYY-MM-DD)` in `uv --version` vs the curl build's `(<sha> <date> <triple>)` | fixed (`_uv_install_style` path-sniff dispatch → `brew upgrade uv` vs `uv self update`, in `scripts/upgrade_tools.sh::cat_uv()` and mirrored inline in `dot_ansible/roles/python_uv_tools/tasks/main.yml`); the refusal itself is by design for distro-managed uv builds, and the repo keeps both install styles on purpose (curl bootstrap needs no brew; brew formula lags Astral by days) |
| [`uv-tool-install-creates-guard-misses-executables-from`](uv-tool-install-creates-guard-misses-executables-from.md) | after adding `with_executables_from:` to a `python_uv_tools` entry, `chezmoi apply` reports success but the new shims never appear: `zsh: command not found: jupyter` while `which jupyter-lab` resolves and `ls ~/.local/bin/jupyter*` lists only `jupyter-lab` (no `jupyter` / `jupyter-notebook`); ansible log shows `TASK [python_uv_tools : Install Python CLI tools via uv]` → `ok: [localhost] => (item=jupyterlab)` (skipped) where a sibling item is `changed`; hand-running `uv tool install … --with-executables-from …` without `--force` half-applies then aborts `error: Executables already exist: jlpm, jupyter-lab, jupyter-labextension, jupyter-labhub (use --force to overwrite)` with no rollback | fixed (`extra_binaries:` now declared alongside every `with_executables_from:` in `defaults/main.yml`, and the single-path `creates:` guard replaced by a `stat` probe + `when:` that re-runs with `--force` when any extra shim is missing) — graduated to an AGENTS.md hard rule, with a `yq` one-liner audit for entries that declare one field but not the other |
| [`visidata-feather-stringdtype-numpy-dtype`](visidata-feather-stringdtype-numpy-dtype.md) | `visidata foo.feather` exits with `ValueError: Could not convert ... Name: <col>, dtype: string to a NumPy dtype (via .dtype value <StringDtype(na_value=<NA>)>)` (pandas 3) / `Cannot interpret 'string[python]' as a data type` (pandas 2); `pd.read_feather` itself works; status shows `raw() returned ERR`; only files whose pandas-metadata says `numpy_type: string` repro | workaround documented (`dot_visidatarc` reroutes `.feather` → `ArrowSheet`; `vd-arrow` alias as escape hatch); downgrade does NOT help (dtype identity is in the file's schema metadata) |
| [`wol-wakes-from-suspend-but-not-full-shutdown`](wol-wakes-from-suspend-but-not-full-shutdown.md) | `wake <host>` / `wakeonlan <MAC>` powers the box on after `sudo systemctl suspend` but does NOTHING after `sudo poweroff` (stays dark, needs a physical power-button press); sender prints `wake: sent 3x magic packet to …` and exits 0 — no error anywhere, target simply never reacts from S5; `sudo ethtool eno1 \| grep -i wake-on` shows `Wake-on: g` so the OS side looks correctly armed; seen on ASUS ROG STRIX B650-A + Intel I225/I226 (`igc` `eno1`) but vendor-agnostic | fixed in BIOS → APM Configuration (`ErP Ready` = **Disabled** is the load-bearing one; plus `Power On By PCI-E / PCI` = Enabled, `Deep Sleep` = Disabled) — ErP cuts +5VSB standby power to the NIC in S4/S5 only, which is why S3 works and S5 doesn't; OS-side `wol g` is necessary but not sufficient. Re-apply after any BIOS flash (updates reset to defaults); isolate with an S3 test first instead of chasing `ethtool`/driver |
| [`workmux-status-leak`](workmux-status-leak.md) | tmux window list stuck showing `1 zsh 🤖` long after the OpenCode/Claude session in that window died (Ctrl+C, kill pane, terminal crash); `tmux list-windows` shows `pane_current_command` back to `zsh`/`bash` yet the emoji persists; windows *currently* running an agent show no marker while old ghost windows do; `tmux show -gv window-status-format` contains no emoji literal and `grep -rln '🤖' ~/.config ~/.local` finds nothing tmux-related (it's a **per-window** user option — only `tmux show-options -wA -t <window>` reveals `@workmux_status`); only on machines where `workmux setup` / `wm setup` was once run, never fresh boxes | fixed (three chezmoi-managed layers: explicit `Stop`/`SubagentStop` → `workmux set-window-status done` hooks in `dot_claude/modify_settings.json`, the vendored OpenCode `session.idle` plugin, and a self-managed `#{?@workmux_status,…}` in catppuccin's window text + `status_format: false`) — upstream `working` NEVER auto-clears and upstream's Claude integration only ever *sets* it; residual crash-leak escape hatch = `wmclear` / `wmclear --all` / `wm sidebar`. Graduated to an AGENTS.md Hard invariant (6 surfaces move together) |
| [`x-copy-over-ssh-writes-remote-clipboard-not-osc52`](x-copy-over-ssh-writes-remote-clipboard-not-osc52.md) | working on a remote box **inside herdr** (or zellij / mosh / VS Code Remote-SSH); `abspath \| x copy` / `cref` / `printf x \| x copy` and Neovim `yy` all report success but nothing reaches the clipboard of the machine you're typing on — `x paste` on the *remote* shows the text; `echo "$SSH_CONNECTION\|$SSH_TTY\|$SSH_CLIENT"` in the pane is all empty while `$WAYLAND_DISPLAY`/`$DISPLAY` are set; a raw `printf '\033]52;c;…' > /dev/tty` DOES reach the local clipboard | fixed — `x`/nvim picked a backend by `$WAYLAND_DISPLAY`/`$DISPLAY` presence with no remote check, and the **herdr server is a persistent daemon that freezes its pane env** (never refreshes `SSH_*` like tmux's `update-environment`). Now `prefer_osc52` (SSH\|`HERDR_ENV`\|`ZELLIJ`) → OSC 52 first, `X_CLIPBOARD=osc52\|wl-copy\|…` override, `osc52_copy` opens `/dev/tty` for real before consuming stdin, `x copy-file` refuses |
| [`yazi-tmux-popup-crash`](yazi-tmux-popup-crash.md) | Yazi inside `display-popup` crashes tmux server, "Terminal response timeout" | workaround documented |
| [`zoxide-doctor-warning`](zoxide-doctor-warning.md) | `zoxide: detected a possible configuration issue.` / `Please ensure that zoxide is initialized right at the end of your shell configuration file (usually ~/.zshrc).` / `Disable this message by setting _ZO_DOCTOR=0.` printed on every prompt; coding-agent bash output mixes warning with unrelated `Exit code 2` from build script; agents start prefixing every `chezmoi apply` with `_ZO_DOCTOR=0` defensively | fixed (`export _ZO_DOCTOR=0` added to `dot_config/shell/20_zoxide.sh`) |
| [`zsh-insecure-directories-prompt-shared-linuxbrew`](zsh-insecure-directories-prompt-shared-linuxbrew.md) | new **nested** zsh blocks on `zsh compinit: insecure directories ... Ignore insecure directories and files and continue [y] or abort compinit [n]?` then `[oh-my-zsh] Insecure completion-dependent directories detected:` listing `/home/linuxbrew/.linuxbrew/share/zsh*` owned by another user; suggested `compaudit \| xargs chmod g-w,o-w` does nothing (perms already 755 — ownership is the failure); fresh login shells clean | fixed (`skip_global_compinit=1` in `dot_zshenv.tmpl` kills Ubuntu's `/etc/zsh/zshrc` bare compinit prompt + conditional `ZSH_DISABLE_COMPFIX=true` in `dot_zshrc.tmpl` when brew prefix isn't ours) |
| [`zsh-nvm-lazy-loader-orphan`](zsh-nvm-lazy-loader-orphan.md) | `zsh: command not found: _nvm_lazy_load` repeated 1–3× per `npx <pkg>` / `npm` / `node` / `nvm` call in **non-interactive** zsh (Claude Code's Bash tool, `zsh -c '…'`, CI runners); the command still completes and exit code is still 0, but the stderr noise breaks `tail -n` / output parsing and confuses agents; interactive shells look clean; `~/.config/zsh/tools/02_legacy.zsh` exists and defines `_nvm_lazy_load()` + `npx() { _nvm_lazy_load; npx "$@"; }`, but `chezmoi managed --include=files \| grep 02_legacy` lists ONLY `.config/zsh/02_legacy_tools.zsh` (different dir, `_tools` suffix) — the loading file is an unmanaged pre-chezmoi orphan | workaround documented (`rm ~/.config/zsh/tools/02_legacy.zsh`, ad-hoc per host) — deliberately **no** `.chezmoiremove` enforcement, since some fleet hosts may have hand-customised the orphan and a path-blind entry would silently nuke it; grep-on-symptom is the durable mechanism, with a content-aware audit-and-prune of `~/.config/zsh/tools/` queued in `TODO.md` |
| [`zsh-osc133-precmd-printf-a-not-stored`](zsh-osc133-precmd-printf-a-not-stored.md) | `tmux send-keys -X next-prompt` doesn't move cursor even though zsh precmd emits OSC 133 A; C markers from preexec work fine; shell-function cpout/cpcmd/cpblock returns 1 byte | workaround documented (embed A in `$PROMPT` via `%{...%}`, not printf from precmd) |
| [`zsh-autosuggestions-postdisplay-clobber`](zsh-autosuggestions-postdisplay-clobber.md) | custom ZLE widget sets `POSTDISPLAY=...` + `region_highlight` then exits; ghost text never renders; `add-zle-hook-widget line-pre-redraw` shows POSTDISPLAY=`[]` despite widget setting it microseconds earlier; affects any widget name not prefixed with `_` / `.` / `autosuggest-` | fixed in `dot_config/zsh/tools/05_aisuggest.zsh` (`ZSH_AUTOSUGGEST_IGNORE_WIDGETS+=(...)`) |
| [`zsh-parse-error-on-resource-after-bw-completion-aliased-name`](zsh-parse-error-on-resource-after-bw-completion-aliased-name.md) | ``source ~/.zshrc`` in an already-running interactive zsh fails with ``/home/<user>/.config/shell/10_aliases.sh:66: defining function based on alias `bw-update-completion'`` + ``10_aliases.sh:66: parse error near `()'``; the cited line is a perfectly normal, months-unchanged POSIX `name() { … }`; **first** source / fresh `zsh -l` / new tab are all fine — re-source only; bash never reproduces; deleting the function makes it vanish (red herring — the function is fine) | fixed (`bw-update-completion()` → `function bw-update-completion` in `dot_config/shell/10_aliases.sh:66`) — zsh's `ALIAS_FUNC_DEF` is off by default since 5.8, and post-2025 `bw completion --shell zsh` output emits `alias bw-update-completion=…` that `95_bitwarden.zsh` sources *after* the function is defined; in-shell escape = `unalias bw-update-completion 2>/dev/null; source ~/.zshrc`. Convention: use `function name { … }` for any shared `dot_config/shell/*.sh` helper a tool's completion could plausibly alias. Rejected: global `setopt ALIAS_FUNC_DEF`, enumerated defensive `unalias` |
| [`zsh-star-slice-substring-not-element`](zsh-star-slice-substring-not-element.md) | `zsh:1: command not found: o` from `svibe N opencode`; `[o exited — back in shell. Re-run with: o]`; only the **first** agent pane broken; agent name truncated to first char; zsh-only (bash fine) | fixed in `dot_config/shell/22_sesh.sh` (`${agents[*]:0:1}` → `${agents[@]:0:1}`) |
| [`zsh-tied-array-path-shadowing`](zsh-tied-array-path-shadowing.md) | `command not found` only inside one zsh function, works at prompt; `hash -r` / `rehash` doesn't help | fixed in `dot_config/zsh/tools/22_sesh.zsh` (rename local `path` → `target`) |
| [`tmux-resurrect-agents`](tmux-resurrect-agents.md) | reattach restores layout but agent/TUI panes come back as bare shell | workaround documented (`@resurrect-processes` whitelist) |
| [`yazi-tmux-popup-crash`](yazi-tmux-popup-crash.md) | `Terminal response timeout` from yazi → tmux server dies | workaround documented (use `new-window`, not `display-popup`) |
| [`zsh-local-path-blanks-PATH`](zsh-local-path-blanks-PATH.md) | `command not found: tr` / `sed` / `wc` / `launchctl` for tools that are demonstrably on `$PATH`, inside ONE function only; identical code works under bash and fails under zsh; worse and silent — a `command -v` guard flips to false so a branch changes with no error at all | fixed — zsh ties the `path` array to `PATH`, so `local path` blanks PATH for the rest of the function (same for fpath/cdpath/manpath/status/argv). Renamed in `51_docker_net.sh` + `29_log_tools.sh`; bats now runs the affected paths under zsh and statically rejects zsh-special `local` names |
| [`zsh-vim-mode-lost-after-source-rc`](zsh-vim-mode-lost-after-source-rc.md) | after `source-rc` / `reload` / manual `source ~/.zshrc` in a running interactive zsh, vi modal editing silently disappears — no `[NORMAL]`/`[INSERT]`, `Esc` + `hjkl` / `dd` / `cw` dead, cursor shape stops changing; `bindkey -lL main` now prints `bindkey -A emacs main` (was `bindkey -A viins main`); `echo $ZVM_INIT_DONE` still prints `true` while the keymap is emacs (the giveaway); fresh `zsh -l` / new tab / `exec zsh` / `cas` are fine — in-place re-sourcing only; only hosts with `enableVimMode = true` | fixed (`source-rc` in `dot_config/shell/10_aliases.sh` force-reinits in place: `ZVM_INIT_DONE=false; zvm_init` behind a `[ -n "$ZSH_VERSION" ]` + `command -v zvm_init` guard, which also replays the `zvm_after_init` fzf/atuin/aisuggest/keys-picker rebinds) — OMZ's `key-bindings.zsh` runs an unconditional `bindkey -e` on every source while zsh-vi-mode's `zvm_version` guard makes the plugin no-op on re-source, so nothing flips `main` back to `viins`; the guaranteed-clean alternative is `cas`/`cau`, which `exec`s a fresh login shell |

## Cross-referenced pitfalls (still in their original homes)

These traps are documented elsewhere and aren't duplicated here — the table
exists so grepping `pitfalls/` still finds them. Move into this folder only
if their original location stops being a natural reading flow.

| Trap | Lives in | Why not here |
|---|---|---|
| chezmoi `modify_` files clobbered by `chezmoi add` | `docs/tools/chezmoi-prefixes.md` → "Case studies in this repo" | Already part of the prefix-semantics narrative |
| chezmoi `create_` files silently skipped by `chezmoi re-add` | `docs/tools/chezmoi-prefixes.md` | Same as above |
| CodeIsland hook ping-pong with our overlay | `docs/tools/agent-overlays.md` → "CodeIsland integration" | Documented as part of overlay design |
| tmux 3.2a `display-menu` popup off-screen / silently suppressed | `AGENTS.md` Hard invariant + `dot_ansible/roles/devtools/` upgrade logic | Graduated to invariant + ansible mitigation |
| fleet-apply process substitution + sentinel ordering | `AGENTS.md` Hard invariant → "fleet-apply semantics" | Graduated to invariant |
| Sudo session `sudo -k` invalidates shared cache | `AGENTS.md` Hard invariant → "Sudo session" | Graduated to invariant |
| chezmoi `modify_settings.json` overlay vs Claude live edits | `docs/tools/agent-overlays.md` | Part of overlay design rationale |
| `copilot-here off` says already off but `/model` still shows `gpt-5.6-sol[1m]` / Terra / Luna | `docs/tools/copilot-claude-proxy.md` → "Do not use Claude Code's `/model` picker" | The launcher guard and four-layer launch audit are part of the proxy's normal settings design |
