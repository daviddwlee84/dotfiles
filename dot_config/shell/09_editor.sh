# Stable executable name; backend resolves the preference on every invocation.
# Shared/per-shell adhoc overrides still run after all managed fragments.
export EDITOR=dotfiles-editor
export VISUAL=dotfiles-editor
